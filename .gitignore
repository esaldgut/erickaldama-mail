# Brainstorm companion (local, ephemeral)
.superpowers/

# Python venv for the diagrams tooling (regenerable from architecture_icons.py)
docs/diagrams/.venv/
__pycache__/
*.pyc

# Go build artifacts (when the TUI client lands)
/bin/
*.exe
*.test
*.out

# CDK-Go verification cache (SP-0) — derived, machine-local, time-sensitive; never commit (stale-truth trap)
docs/cdk-verified.json

# CDK synth output (derived, machine-local)
cdk.out/

# OS / editor
.DS_Store
*.swp
*~
.idea/
.vscode/

# CDK CLI local cache (acknowledged notices, context lookups) — machine-local
cdk.context.json

# SP-4 client binaries (built locally; never committed)
/mail
/mail-tui
/mail-smoke
