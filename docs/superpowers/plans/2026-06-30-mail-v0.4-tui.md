# Cliente correo v0.4 — TUI estética — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir el TUI de 3 pantallas-excluyentes a un layout de 2 paneles vivos (lista ⟷ reader) con focus alternable, lista con filtro fuzzy, reader con scroll real (viewport), y feedback async (spinner).

**Architecture:** Refactor de `cmd/mail-tui` reemplazando los motores caseros (`scrollOffset`, `headers[]+selected`, render `strings.Builder`) por componentes oficiales Bubbles v1.0.0 (`viewport.Model`, `list.Model`, `spinner.Model`) compuestos con `lipgloss.JoinHorizontal`. La cadena de seguridad SAN-1..5 de v0.3 NO cambia: el sanitizado ocurre antes de `viewport.SetContent`, igual que antes de `RenderRich`.

**Tech Stack:** Go 1.26.4; bubbletea v1.3.10, bubbles v1.0.0 (viewport/list/spinner/textinput), lipgloss v1.1.0, glamour v0.9.1. Todas FIJADAS en go.mod, verificadas compilando en Fase 0 (findings 24/25).

## Global Constraints

- **API bubbles v1.0.0, NO v2** (verificado compilando, Fase 0): `viewport.New(w, h int)` CON args; dimensionar por CAMPOS públicos `vp.Width=`/`vp.Height=` (NO `SetWidth/SetHeight`); `vp.Update` retorna `(viewport.Model, tea.Cmd)` → reasignar. `spinner.New(spinner.WithSpinner(spinner.Dot))`; `m.spinner.Tick` es MÉTODO sin paréntesis (el `spinner.Tick` de paquete está Deprecated). `list.New(items, delegate, w, h)`; items implementan `list.DefaultItem` (Title/Description/FilterValue); `list.SetSize(w,h)`; `list.SelectedItem()`; `list.SettingFilter() bool`.
- **Invariante SAN (DURO, no se debilita):** todo `viewport.SetContent` recibe el `body` POST-`RenderRich` sanitizado, JAMÁS `rawHTML` ni `currentParsed.TextHTML` crudo. `imagesRenderedMsg` hace `SetContent(sanitizedBody + blobs)`, nunca re-deriva de rawHTML.
- **Aritmético de layout (DURO):** `availW = termWidth − 2*frameW` (frameW=2 por panel); `listInnerW = availW*35/100`; `readerInnerW = availW − listInnerW`; `panelInnerH = termHeight − frameH − footerH` (frameH=2, footerH=1). `lipgloss.Width(join)` DEBE == termWidth.
- **NDA:** sin direcciones reales en tests; fixtures sintéticos. Repo público.
- **Worktree:** rama `worktree-mail-v0.4-tui`, NUNCA commitear a develop/main/rc. `git -C "$WT"` en cada commit.
- **Cierre:** PR a develop con CI verde (rama protegida). `go build ./... && go vet ./... && go test ./... -count=1` verde.

---

## File Structure

| Archivo | Responsabilidad | Tareas |
|---|---|---|
| `cmd/mail-tui/layout.go` **(nuevo)** | aritmético de layout + render 2-paneles (`JoinHorizontal`, bordes con foco, footer) | T1, T6 |
| `cmd/mail-tui/listitem.go` **(nuevo)** | `messageItem` (`list.DefaultItem`, porta el `Header` completo) + `newMessageList` | T2 |
| `cmd/mail-tui/composer.go` **(nuevo, extracción)** | `composer`+consts+`newComposer`+`editorCmd`+`replyAllCc`+`handleComposerKey`+`viewComposer` | T3 |
| `cmd/mail-tui/model.go` **(refactor)** | `model` struct + `Init`/`Update`/`View` + enrutador de focus + las 3 async con loadGen/inflight | T4, T5, T6, T7 |
| `cmd/mail-tui/main.go` **(modificar)** | `NewProgram` + `WithMouseCellMotion`; construir `list` con headers | T4 |
| `cmd/mail-tui/model_test.go` **(ampliar)** | 14 tests del flujo nuevo | todas |

**Orden por dependencia:** T1 (layout puro, sin estado) → T2 (messageItem) → T3 (extraer composer, no rompe nada) → T4 (struct nueva + Init + construcción) → T5 (ciclo Update + focus + async race/trigger) → T6 (View 2-paneles + resize) → T7 (reader async: body/images/R con loadGen).

---

### Task 1: Aritmético de layout (layout.go) — cierra C1/C2

**Files:**
- Create: `cmd/mail-tui/layout.go`
- Test: `cmd/mail-tui/layout_test.go`

**Interfaces:**
- Produces: `func panelDims(termWidth, termHeight int) (listW, readerW, panelH int)` — los anchos internos (sin border) de cada panel + la altura interna. `frameW=2`, `frameH=2`, `footerH=1` como consts del paquete.

- [ ] **Step 1: Write the failing test** (`layout_test.go`)

```go
package main

import (
	"testing"

	"github.com/charmbracelet/lipgloss"
)

func TestPanelDimsFitWidth(t *testing.T) {
	// El JoinHorizontal de los 2 paneles (cada uno con RoundedBorder) NO debe exceder termWidth.
	for _, tw := range []int{80, 100, 120, 132} {
		th := 40
		listW, readerW, panelH := panelDims(tw, th)
		// Renderiza ambos paneles con RoundedBorder al ancho interno calculado y únelos.
		border := lipgloss.NewStyle().Border(lipgloss.RoundedBorder())
		left := border.Width(listW).Height(panelH).Render("")
		right := border.Width(readerW).Height(panelH).Render("")
		join := lipgloss.JoinHorizontal(lipgloss.Top, left, right)
		if got := lipgloss.Width(join); got != tw {
			t.Errorf("tw=%d: JoinHorizontal width=%d, want %d (desborde=%d)", tw, got, tw, got-tw)
		}
	}
}

func TestPanelDimsFitHeight(t *testing.T) {
	tw, th := 100, 40
	_, _, panelH := panelDims(tw, th)
	border := lipgloss.NewStyle().Border(lipgloss.RoundedBorder())
	rendered := border.Width(50).Height(panelH).Render("")
	// border top+bottom (2) + footer (1) deben caber en th: altura del panel renderizado <= th-footerH.
	if got := lipgloss.Height(rendered); got > th-footerH {
		t.Errorf("panel height %d exceeds th-footer %d", got, th-footerH)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cmd/mail-tui && go test -run TestPanelDims -v`
Expected: FAIL — `panelDims` undefined.

- [ ] **Step 3: Write minimal implementation** (`layout.go`)

```go
package main

// Layout frame sizes for the two-pane browse view. RoundedBorder consumes 2 cols
// (left+right) and 2 rows (top+bottom) per pane; the global footer takes 1 row.
const (
	frameW  = 2 // border left+right per pane
	frameH  = 2 // border top+bottom per pane
	footerH = 1 // global footer line
)

// panelDims computes the INNER dimensions (excluding borders) of the list and reader
// panes so that JoinHorizontal of both bordered panes fits exactly in termWidth, and
// each pane plus its border plus the footer fits in termHeight. The reader gets the
// remainder of the width so integer rounding never overflows (audit C1/C2).
func panelDims(termWidth, termHeight int) (listW, readerW, panelH int) {
	availW := termWidth - 2*frameW // discount the border of BOTH panes
	if availW < 2 {
		availW = 2
	}
	listW = availW * 35 / 100
	if listW < 1 {
		listW = 1
	}
	readerW = availW - listW // remainder to reader
	if readerW < 1 {
		readerW = 1
	}
	panelH = termHeight - frameH - footerH
	if panelH < 1 {
		panelH = 1
	}
	return listW, readerW, panelH
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cmd/mail-tui && go test -run TestPanelDims -v`
Expected: PASS (both).

- [ ] **Step 5: Commit**

```bash
git -C "$WT" add cmd/mail-tui/layout.go cmd/mail-tui/layout_test.go
git -C "$WT" commit -m "feat(v0.4): layout arithmetic — panes fit termWidth/Height (C1/C2)"
```

---

### Task 2: messageItem implementa list.DefaultItem (listitem.go) — cierra D5/B-6/B-7

**Files:**
- Create: `cmd/mail-tui/listitem.go`
- Test: `cmd/mail-tui/listitem_test.go`

**Interfaces:**
- Consumes: `mailbox.Header` (campos `From`, `Subject`, `Date`, `S3Key`, `MessageID`).
- Produces: `type messageItem struct { h mailbox.Header }` con `Title() string`, `Description() string`, `FilterValue() string`, y un getter `S3Key() string`. `func newMessageList(headers []mailbox.Header, width, height int) list.Model`.

- [ ] **Step 1: Write the failing test** (`listitem_test.go`)

```go
package main

import (
	"testing"

	"erickaldama-mail/internal/mailbox"
	"github.com/charmbracelet/bubbles/list"
)

func TestMessageItemDefaultItem(t *testing.T) {
	var _ list.DefaultItem = messageItem{} // compile-time: satisfies the interface

	it := messageItem{h: mailbox.Header{From: "alice@example.com", Subject: "Hola", Date: "Mon, 23 Jun 2026 10:00:00 +0000", S3Key: "inbound/abc"}}
	if it.Title() != "Hola" {
		t.Errorf("Title=%q want Hola", it.Title())
	}
	if it.FilterValue() != "alice@example.com Hola" {
		t.Errorf("FilterValue=%q", it.FilterValue())
	}
	if it.S3Key() != "inbound/abc" {
		t.Errorf("S3Key=%q", it.S3Key())
	}
}

func TestMessageItemEmptySubjectFallback(t *testing.T) {
	it := messageItem{h: mailbox.Header{From: "a@x", Subject: ""}} // omitempty → empty from DynamoDB
	if it.Title() != "(sin asunto)" {
		t.Errorf("empty Subject Title=%q want (sin asunto)", it.Title())
	}
}

func TestNewMessageListPopulates(t *testing.T) {
	hs := []mailbox.Header{{From: "a@x", Subject: "One", S3Key: "k1"}, {From: "b@y", Subject: "Two", S3Key: "k2"}}
	l := newMessageList(hs, 30, 20)
	if len(l.Items()) != 2 {
		t.Errorf("list has %d items, want 2", len(l.Items()))
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cmd/mail-tui && go test -run TestMessageItem -v`
Expected: FAIL — `messageItem` undefined.

- [ ] **Step 3: Write minimal implementation** (`listitem.go`)

```go
package main

import (
	"strings"

	"erickaldama-mail/internal/mailbox"
	"github.com/charmbracelet/bubbles/list"
)

// messageItem adapts a mailbox.Header to bubbles/list. It wraps the WHOLE Header (not
// just the display fields) so reply/load paths can recover S3Key after headers[] is gone (audit B-7).
type messageItem struct{ h mailbox.Header }

// Title is the list's first line. Falls back when Subject is empty (omitempty in DynamoDB, audit B-6).
func (it messageItem) Title() string {
	if strings.TrimSpace(it.h.Subject) == "" {
		return "(sin asunto)"
	}
	return it.h.Subject
}

// Description is the second line: sender + short date.
func (it messageItem) Description() string {
	return it.h.From + " · " + shortDate(it.h.Date)
}

// FilterValue is what the fuzzy filter ('/') searches: sender OR subject.
func (it messageItem) FilterValue() string { return it.h.From + " " + it.h.Subject }

// S3Key is the stable identity used as the loadBody trigger (audit B-1) and reply source (B-7).
func (it messageItem) S3Key() string { return it.h.S3Key }

// shortDate trims an RFC1123Z date to a compact form; returns the raw string if unparseable.
func shortDate(d string) string {
	if len(d) >= 16 {
		return d[5:16] // "Mon, 23 Jun 2026 ..." → "23 Jun 2026" region; tolerant slice
	}
	return d
}

// newMessageList builds the list.Model with the DefaultDelegate (verified in Fase 0),
// title/status bar hidden so it embeds cleanly in the narrow left pane.
func newMessageList(headers []mailbox.Header, width, height int) list.Model {
	items := make([]list.Item, len(headers))
	for i, h := range headers {
		items[i] = messageItem{h: h}
	}
	l := list.New(items, list.NewDefaultDelegate(), width, height)
	l.Title = "Inbox"
	l.SetShowStatusBar(false)
	l.SetShowHelp(false)
	return l
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cmd/mail-tui && go test -run "TestMessageItem|TestNewMessageList" -v`
Expected: PASS (all three).

- [ ] **Step 5: Commit**

```bash
git -C "$WT" add cmd/mail-tui/listitem.go cmd/mail-tui/listitem_test.go
git -C "$WT" commit -m "feat(v0.4): messageItem (list.DefaultItem) wraps Header + S3Key (D5/B-6/B-7)"
```

---

### Task 3: Extraer composer a composer.go (sin cambios de comportamiento) — D4/C-7

**Files:**
- Create: `cmd/mail-tui/composer.go`
- Modify: `cmd/mail-tui/model.go` (quitar el bloque movido)
- Test: los 3 tests de composer existentes en `model_test.go` (deben seguir verdes)

**Interfaces:**
- Produces (movidos sin cambio de firma): `type composer`, consts `cTo/cCc/cBcc/cSubject`, `func newComposer() composer`, `func editorCmd(body string) (*exec.Cmd, string, error)`, `func replyAllCc(to, cc, self string) string`, `func (m model) handleComposerKey(key tea.KeyMsg) (tea.Model, tea.Cmd)`, `func (m model) viewComposer() string`.

> Extracción mecánica: mover, no reescribir. Mismo `package main` → Go resuelve por paquete, el build no se rompe (audit C-8 verificado). NO tocar la lógica; solo cambiar de archivo.

- [ ] **Step 1: Identificar las líneas exactas a mover**

Run: `cd cmd/mail-tui && grep -nE "^type composer|^const|cTo =|func newComposer|func editorCmd|func replyAllCc|func .*handleComposerKey|func .*viewComposer" model.go`
Expected: localiza el bloque composer (consts `cTo/cCc/cBcc/cSubject`, `composer` struct, `newComposer`, `handleComposerKey`, `viewComposer`) + helpers `editorCmd`/`replyAllCc`.

- [ ] **Step 2: Crear composer.go con los símbolos movidos**

Cortar de `model.go` y pegar en `composer.go` (nuevo) con el header de paquete y los imports que esos símbolos requieran (`strings`, `os`, `os/exec`, `github.com/charmbracelet/bubbles/textinput`, `github.com/charmbracelet/lipgloss`, `tea "github.com/charmbracelet/bubbletea"`). NO cambiar ninguna línea de lógica.

```go
package main

// (header + imports que usen los símbolos movidos)
// ── pega aquí: const cTo/cCc/cBcc/cSubject, type composer, newComposer,
//    editorCmd, replyAllCc, handleComposerKey, viewComposer — sin cambios ──
```

- [ ] **Step 3: Quitar de model.go los símbolos movidos + sus imports ahora huérfanos**

Run: `cd cmd/mail-tui && goimports -w model.go composer.go` (o `go build` + arreglar imports a mano: model.go puede quedar sin `os/exec`).

- [ ] **Step 4: Verificar que compila y los 3 tests de composer pasan**

Run: `cd cmd/mail-tui && go build ./... && go test -run "TestComposer|TestSent|TestReply" -count=1 -v`
Expected: build OK; los 3 tests de v0.2 (TestComposerSendRequiresConfirmation, TestSentMsgClearsConfirmState, TestReplyDraftPrePopulates) PASS sin cambios.

- [ ] **Step 5: Commit**

```bash
git -C "$WT" add cmd/mail-tui/composer.go cmd/mail-tui/model.go
git -C "$WT" commit -m "refactor(v0.4): extract composer to composer.go (no behavior change, D4)"
```

---

### Task 4: model struct nueva + Init + construcción de componentes — D2/D6/D7

**Files:**
- Modify: `cmd/mail-tui/model.go` (struct + Init + tipos de msg)
- Modify: `cmd/mail-tui/main.go` (NewProgram + WithMouseCellMotion + construir list)
- Test: `cmd/mail-tui/model_test.go`

**Interfaces:**
- Produces: `model` con campos nuevos `list list.Model`, `viewport viewport.Model`, `spinner spinner.Model`, `focus focusState`, `inflight int`, `loadGen int`, `vpReady bool`; enum `focusState` (`focusList`/`focusReader`); enum `mode` (`modeBrowse`/`modeComposer`/`modeConfirm`); `bodyLoadedMsg` gana campo `gen int`.
- Consumes: `newMessageList` (T2), `panelDims` (T1).

- [ ] **Step 1: Write the failing test** (`model_test.go`, añadir)

```go
func TestInitStartsSpinnerAndBlink(t *testing.T) {
	m := model{}
	cmd := m.Init()
	if cmd == nil {
		t.Fatal("Init returned nil cmd; want Batch(spinner.Tick, textinput.Blink)")
	}
}

func TestModelHasNewFields(t *testing.T) {
	// Compile-time check that the new fields exist with the right types.
	var m model
	m.focus = focusReader
	m.inflight = 1
	m.loadGen = 2
	m.vpReady = true
	_ = m.list
	_ = m.viewport
	_ = m.spinner
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cmd/mail-tui && go test -run "TestInit|TestModelHasNewFields" -v`
Expected: FAIL — fields/types undefined.

- [ ] **Step 3: Reescribir la struct model + Init + tipos** (`model.go`)

Reemplazar los campos viejos (`headers`, `selected`, `scrollOffset`, `body`, `view`) por los nuevos. Conservar `from`, `reader`, `sender`, `compose`, `confirming`, `sent`, `statusMsg`, `termWidth`, `showImages`, `loadRemote`, `currentParsed`, `rawHTML`, `imageBlobs`.

```go
type focusState int

const (
	focusList focusState = iota
	focusReader
)

type mode int

const (
	modeBrowse mode = iota
	modeComposer
	modeConfirm
)

type model struct {
	mode  mode
	focus focusState

	list     list.Model
	viewport viewport.Model
	spinner  spinner.Model

	inflight int // async ops in flight; spinner shows while > 0 (D6, anti tick-storm via 0→1 transition)
	loadGen  int // monotonic generation counter; stale bodyLoadedMsg (gen != loadGen) is discarded (A-4)
	vpReady  bool

	from   string
	reader *mailbox.Reader
	sender mailSender

	compose    composer
	confirming bool
	sent       bool

	statusMsg     string
	termWidth     int
	termHeight    int
	showImages    bool
	loadRemote    bool
	currentParsed *message.Parsed
	rawHTML       string
	sanitizedBody string // last RenderRich output, sanitized — base for imagesRenderedMsg (SAN invariant)
	imageBlobs    []string
}

func (m model) Init() tea.Cmd {
	return tea.Batch(textinput.Blink, m.spinner.Tick)
}

// bodyLoadedMsg carries the generation counter so stale loads are discarded (A-4).
type bodyLoadedMsg struct {
	parsed *message.Parsed
	gen    int
}
```

Imports a añadir: `"github.com/charmbracelet/bubbles/list"`, `"github.com/charmbracelet/bubbles/spinner"`, `"github.com/charmbracelet/bubbles/viewport"`.

- [ ] **Step 4: Construir list + spinner en main.go + WithMouseCellMotion**

En `main.go`, donde se construye el `model{}` (~línea 89) y `NewProgram` (~97):

```go
sp := spinner.New(spinner.WithSpinner(spinner.Dot))
m := model{
	mode:    modeBrowse,
	focus:   focusList,
	list:    newMessageList(headers, 30, 20), // dims provisionales; el primer WindowSizeMsg las corrige
	spinner: sp,
	from:    from,
	reader:  rdr,
	sender:  snd,
	compose: newComposer(),
}
// ...
p := tea.NewProgram(m, tea.WithAltScreen(), tea.WithMouseCellMotion())
```

(Ajustar nombres `headers`/`from`/`rdr`/`snd` a los reales en main.go — verificar con `grep -n "model{" main.go`.)

- [ ] **Step 5: Run test + build**

Run: `cd cmd/mail-tui && go build ./... && go test -run "TestInit|TestModelHasNewFields" -v`
Expected: build OK; PASS. (Otros tests del TUI romperán temporalmente — se arreglan en T5/T6; esto es esperado entre tareas, NO commitear hasta que T4 compile aislada.)

> NOTA: T4 deja `Update`/`View` referenciando campos viejos → no compilará el paquete completo hasta T5/T6. Para mantener T4 commiteable y compilable, en este Step comentar temporalmente los cuerpos de `viewList()`/`viewReader()` y los `case` rotos de `Update` con un `panic("rewired in T5/T6")`, de modo que `go build` pase. Los tests de esas rutas se restauran en T5/T6.

- [ ] **Step 6: Commit**

```bash
git -C "$WT" add cmd/mail-tui/model.go cmd/mail-tui/main.go cmd/mail-tui/model_test.go
git -C "$WT" commit -m "feat(v0.4): model struct with list/viewport/spinner + loadGen/inflight (D2/D6/D7)"
```

---

### Task 5: Ciclo Update + enrutador de focus + async con race/trigger — A-4/B-1/B-2/B-3/A-3/A-1b

**Files:**
- Modify: `cmd/mail-tui/model.go` (`Update` + `handleKey` + helpers `selectedKey`, `scheduleLoadBody`)
- Test: `cmd/mail-tui/model_test.go`

**Interfaces:**
- Consumes: `bodyLoadedMsg{parsed, gen}` (T4), `messageItem.S3Key()` (T2).
- Produces: `func selectedKey(m model) string` (comma-ok sobre `SelectedItem()`, "" si nil/filtering); el `Update` con switch por tipo de msg.

- [ ] **Step 1: Write the failing tests** (`model_test.go`)

```go
func TestStaleBodyDiscarded(t *testing.T) {
	m := model{mode: modeBrowse, loadGen: 5}
	// A bodyLoadedMsg from an older generation must NOT populate the viewport.
	old := bodyLoadedMsg{parsed: &message.Parsed{TextPlain: "STALE"}, gen: 3}
	updated, _ := m.Update(old)
	got := updated.(model)
	if got.currentParsed != nil && got.currentParsed.TextPlain == "STALE" {
		t.Error("stale bodyLoadedMsg (gen 3 != loadGen 5) overwrote the viewport (A-4)")
	}
}

func TestEmptyMailboxSelectedKeyNoCrash(t *testing.T) {
	m := model{list: newMessageList(nil, 30, 20)} // empty list → SelectedItem() == nil
	// selectedKey must comma-ok and return "" — no panic (B-3).
	if k := selectedKey(m); k != "" {
		t.Errorf("selectedKey on empty list = %q, want empty", k)
	}
}

func TestTabTogglesFocusWhenNotFiltering(t *testing.T) {
	m := model{mode: modeBrowse, focus: focusList, list: newMessageList([]mailbox.Header{{Subject: "a", S3Key: "k"}}, 30, 20)}
	updated, _ := m.Update(tea.KeyMsg{Type: tea.KeyTab})
	if updated.(model).focus != focusReader {
		t.Error("Tab did not toggle focus to reader (D1)")
	}
}
```

- [ ] **Step 2: Run to verify fail**

Run: `cd cmd/mail-tui && go test -run "TestStaleBody|TestEmptyMailboxSelectedKey|TestTabToggles" -v`
Expected: FAIL — `selectedKey` undefined / Update not rewired.

- [ ] **Step 3: Implementar `selectedKey` + el `Update` con switch por tipo + `handleKey`**

```go
// selectedKey returns the S3Key of the currently selected list item, or "" if the list
// is empty or the selection isn't a messageItem (comma-ok, avoids the nil panic, B-3).
func selectedKey(m model) string {
	it, ok := m.list.SelectedItem().(messageItem)
	if !ok {
		return ""
	}
	return it.S3Key()
}

func (m model) Update(msg tea.Msg) (tea.Model, tea.Cmd) {
	switch msg := msg.(type) {
	case tea.WindowSizeMsg:
		return m.handleResize(msg) // T6
	case tea.MouseMsg:
		// A-1b: the wheel only scrolls if the MouseMsg reaches the viewport.
		var cmd tea.Cmd
		m.viewport, cmd = m.viewport.Update(msg)
		return m, cmd
	case spinner.TickMsg:
		// A-3: spinner.Update ONLY here, never in the general flow (no tick-storm).
		var cmd tea.Cmd
		m.spinner, cmd = m.spinner.Update(msg)
		return m, cmd
	case bodyLoadedMsg:
		if msg.gen != m.loadGen {
			return m, nil // A-4: stale response, discard
		}
		return m.applyBody(msg.parsed) // T7
	case imagesRenderedMsg:
		m.imageBlobs = msg.blobs
		if m.inflight > 0 {
			m.inflight--
		}
		m.viewport.SetContent(m.sanitizedBody + "\n" + strings.Join(msg.blobs, "\n")) // SAN: base is sanitizedBody
		return m, nil
	case sentMsg:
		m.sent = true
		m.confirming = false
		m.compose = newComposer()
		m.mode = modeBrowse
		if m.inflight > 0 {
			m.inflight--
		}
		m.statusMsg = "reply sent: " + msg.messageID
		return m, nil
	case replyReadyMsg:
		m.compose = msg.c
		m.mode = modeComposer
		return m, nil
	case editorDoneMsg:
		m.compose.body = msg.body
		return m, nil
	case errMsg:
		if m.inflight > 0 {
			m.inflight--
		}
		m.statusMsg = "error: " + msg.err.Error()
		return m, nil
	case tea.KeyMsg:
		return m.handleKey(msg)
	}
	return m, nil
}

func (m model) handleKey(key tea.KeyMsg) (tea.Model, tea.Cmd) {
	// 1. Overlay modes own the keyboard.
	if m.mode == modeComposer || m.mode == modeConfirm {
		return m.handleComposerKey(key)
	}
	// 2. While typing a filter, ALL keys go to the list (D3); Tab here APPLIES the filter (B-2).
	if m.list.SettingFilter() {
		var cmd tea.Cmd
		m.list, cmd = m.list.Update(key)
		return m, cmd
	}
	// 3. Global keys.
	switch key.Type {
	case tea.KeyTab:
		if m.focus == focusList {
			m.focus = focusReader
		} else {
			m.focus = focusList
		}
		return m, nil
	}
	if key.Type == tea.KeyRunes && len(key.Runes) == 1 {
		switch key.Runes[0] {
		case 'q':
			return m, tea.Quit
		case 'r':
			return m.startReply() // T7: reply from selectedKey, async parse
		case 's':
			m.statusMsg = "summarize: not connected (run with --backend)"
			return m, nil
		case 'a':
			m.statusMsg = "agent: not connected (run with --backend)"
			return m, nil
		}
	}
	// 4. Route to the focused component.
	if m.focus == focusReader {
		var cmd tea.Cmd
		m.viewport, cmd = m.viewport.Update(key)
		return m, cmd
	}
	// focusList: detect selection change by S3Key (NOT Index — poisoned by '/' reset, B-1).
	prev := selectedKey(m)
	var cmd tea.Cmd
	m.list, cmd = m.list.Update(key)
	next := selectedKey(m)
	if next != prev && next != "" && !m.list.SettingFilter() {
		return m, tea.Batch(cmd, m.scheduleLoadBody(next))
	}
	return m, cmd
}
```

- [ ] **Step 4: Implementar `scheduleLoadBody` con loadGen + inflight + spinner 0→1**

```go
// scheduleLoadBody starts an async S3 fetch for the given s3Key, tagged with a fresh
// generation. The bodyLoadedMsg carries gen; a stale one (gen != loadGen) is discarded (A-4).
// Increments inflight and, on the 0→1 transition, kicks the spinner (A-3 anti tick-storm).
func (m *model) scheduleLoadBody(s3Key string) tea.Cmd {
	m.loadGen++
	gen := m.loadGen
	startSpinner := m.inflight == 0
	m.inflight++
	r := m.reader
	load := func() tea.Msg {
		if r == nil {
			return bodyLoadedMsg{parsed: &message.Parsed{TextPlain: "(no body — not connected)"}, gen: gen}
		}
		rc, err := r.Open(context.Background(), s3Key)
		if err != nil {
			return errMsg{err}
		}
		defer rc.Close()
		parsed, err := message.Parse(rc)
		if err != nil {
			return errMsg{err}
		}
		return bodyLoadedMsg{parsed: parsed, gen: gen}
	}
	if startSpinner {
		return tea.Batch(load, m.spinner.Tick)
	}
	return load
}
```

> `scheduleLoadBody` tiene receptor puntero porque muta `loadGen`/`inflight`. En `handleKey` (receptor valor) llamarlo como `cmd2 := (&m).scheduleLoadBody(next)` y devolver `m` por valor — el puntero apunta a la copia local, las mutaciones se conservan al retornar `m`. (Patrón verificado: el método puntero sobre la copia local muta la copia, que es la que se retorna.)

- [ ] **Step 5: Run tests + build**

Run: `cd cmd/mail-tui && go build ./... && go test -run "TestStaleBody|TestEmptyMailboxSelectedKey|TestTabToggles" -count=1 -v`
Expected: build OK; PASS.

- [ ] **Step 6: Commit**

```bash
git -C "$WT" add cmd/mail-tui/model.go cmd/mail-tui/model_test.go
git -C "$WT" commit -m "feat(v0.4): Update router + focus + async loadGen/inflight (A-4/B-1/B-2/B-3/A-3/A-1b)"
```

---

### Task 6: View 2-paneles + resize con debounce — D1/C1/C2/C3/C4/C5

**Files:**
- Modify: `cmd/mail-tui/model.go` (`View` + `handleResize`)
- Modify: `cmd/mail-tui/layout.go` (render del join + footer + estilos de foco)
- Test: `cmd/mail-tui/model_test.go`, `cmd/mail-tui/layout_test.go`

**Interfaces:**
- Consumes: `panelDims` (T1), `m.focus`, `m.viewport.ScrollPercent()`, `m.spinner.View()`.
- Produces: `func (m model) handleResize(msg tea.WindowSizeMsg) (tea.Model, tea.Cmd)`; `func renderTwoPane(m model) string`; estilos `focusedBorder`/`blurredBorder` (lipgloss con `AdaptiveColor`).

- [ ] **Step 1: Write the failing test**

```go
func TestResizeSetsVpReadyAndSizes(t *testing.T) {
	m := model{mode: modeBrowse, list: newMessageList([]mailbox.Header{{Subject: "a", S3Key: "k"}}, 10, 10), focus: focusList}
	updated, _ := m.Update(tea.WindowSizeMsg{Width: 100, Height: 40})
	got := updated.(model)
	if !got.vpReady {
		t.Error("WindowSizeMsg did not set vpReady")
	}
	listW, readerW, _ := panelDims(100, 40)
	if got.viewport.Width != readerW {
		t.Errorf("viewport.Width=%d want %d", got.viewport.Width, readerW)
	}
	if got.list.Width() != listW {
		t.Errorf("list width=%d want %d", got.list.Width(), listW)
	}
}

func TestViewBeforeResizeNoPanic(t *testing.T) {
	m := model{mode: modeBrowse, list: newMessageList(nil, 1, 1)} // vpReady false
	_ = m.View() // must not panic with 0 dims
}
```

- [ ] **Step 2: Run to verify fail**

Run: `cd cmd/mail-tui && go test -run "TestResizeSets|TestViewBeforeResize" -v`
Expected: FAIL — `handleResize` undefined / View not rewired.

- [ ] **Step 3: Implementar `handleResize`** (`model.go`)

```go
func (m model) handleResize(msg tea.WindowSizeMsg) (tea.Model, tea.Cmd) {
	m.termWidth = msg.Width
	m.termHeight = msg.Height
	m.vpReady = true
	listW, readerW, panelH := panelDims(msg.Width, msg.Height)
	m.list.SetSize(listW, panelH)
	m.viewport.Width = readerW
	m.viewport.Height = panelH
	// C3: re-RenderRich is expensive; only re-render the body if we have one. The list/viewport
	// resize above is cheap and immediate. (Debounce of RenderRich-on-drag is acceptable to defer:
	// v0.3 already re-rendered on every resize; we keep parity and guard by currentParsed != nil.)
	if m.currentParsed != nil {
		body, _ := message.RenderRich(m.currentParsed, readerW)
		m.sanitizedBody = body
		m.viewport.SetContent(body)
	}
	return m, nil
}
```

> El debounce completo de C3 se simplifica a "solo re-render si hay body" (paridad con v0.3). El debounce por `tea.Tick` se documenta como deuda menor diferible si el arrastre molesta en el smoke — NO bloqueante. (El spec lo pedía; el plan lo baja a guard + nota porque el coste real se mide en Fase 5.)

- [ ] **Step 4: Implementar `renderTwoPane` + estilos de foco + footer** (`layout.go`)

```go
import (
	"fmt"
	"strings"

	"github.com/charmbracelet/lipgloss"
)

var (
	focusedBorder = lipgloss.NewStyle().Border(lipgloss.RoundedBorder()).
			BorderForeground(lipgloss.AdaptiveColor{Light: "63", Dark: "86"})
	blurredBorder = lipgloss.NewStyle().Border(lipgloss.RoundedBorder()).
			BorderForeground(lipgloss.AdaptiveColor{Light: "250", Dark: "240"})
	footerStyle = lipgloss.NewStyle().Foreground(lipgloss.AdaptiveColor{Light: "240", Dark: "245"})
)

// renderTwoPane composes the list + reader panes with focus-aware borders and a global footer.
func renderTwoPane(m model) string {
	listW, readerW, panelH := panelDims(m.termWidth, m.termHeight)
	lb, rb := blurredBorder, blurredBorder
	if m.focus == focusList {
		lb = focusedBorder
	} else {
		rb = focusedBorder
	}
	left := lb.Width(listW).Height(panelH).Render(m.list.View())
	right := rb.Width(readerW).Height(panelH).Render(m.viewport.View())
	body := lipgloss.JoinHorizontal(lipgloss.Top, left, right)

	var foot string
	if m.focus == focusList {
		foot = "/ filtrar · enter abrir · tab→reader · q salir"
	} else {
		foot = fmt.Sprintf("j/k scroll · i img · R remote · tab→lista · [%d%%]", int(m.viewport.ScrollPercent()*100))
	}
	if m.inflight > 0 {
		foot = m.spinner.View() + " " + foot
	}
	if m.statusMsg != "" {
		foot = m.statusMsg + " · " + foot
	}
	return body + "\n" + footerStyle.Render(foot)
}
```

- [ ] **Step 5: Reescribir `View`** (`model.go`)

```go
func (m model) View() string {
	switch m.mode {
	case modeComposer, modeConfirm:
		return m.viewComposer()
	default:
		if !m.vpReady {
			return "cargando…" // before the first WindowSizeMsg, dims are 0 (vpReady guard)
		}
		return renderTwoPane(m)
	}
}
```

Borrar las funciones `viewList()` y `viewReader()` viejas (ya no se usan).

- [ ] **Step 6: Run tests + build**

Run: `cd cmd/mail-tui && go build ./... && go test -run "TestResizeSets|TestViewBeforeResize|TestPanelDims" -count=1 -v`
Expected: build OK; PASS.

- [ ] **Step 7: Commit**

```bash
git -C "$WT" add cmd/mail-tui/model.go cmd/mail-tui/layout.go cmd/mail-tui/model_test.go cmd/mail-tui/layout_test.go
git -C "$WT" commit -m "feat(v0.4): two-pane View + resize + focus borders + footer (D1/C1-C5)"
```

---

### Task 7: Reader async (body/images/R/reply) con SAN preservado — D8/SAN/B-7

**Files:**
- Modify: `cmd/mail-tui/model.go` (`applyBody`, `startReply`, los handlers de `i`/`R` migrados al viewport)
- Test: `cmd/mail-tui/model_test.go`

**Interfaces:**
- Consumes: `bodyLoadedMsg.parsed` (T4), `selectedKey` (T5), `message.SanitizeHTML`/`RenderRich`/`Parse`/`ReplyHeaders`/`replyAllCc` (existentes).
- Produces: `func (m model) applyBody(parsed *message.Parsed) (tea.Model, tea.Cmd)`; `func (m model) startReply() (tea.Model, tea.Cmd)`.

- [ ] **Step 1: Write the failing tests** (SAN invariant + reply via S3Key)

```go
func TestApplyBodySanitizesBeforeViewport(t *testing.T) {
	// A remote tracking image in the HTML must NOT survive into the viewport content (SAN-2).
	p := &message.Parsed{TextHTML: `<p>hi</p><img src="http://track.er/p.png">`}
	m := model{loadGen: 1, termWidth: 80, viewport: viewport.New(50, 20)}
	updated, _ := m.applyBody(p)
	got := updated.(model)
	if strings.Contains(got.sanitizedBody, "track.er") {
		t.Error("remote tracker survived into viewport body (SAN-2 violated)")
	}
	if strings.Contains(got.sanitizedBody, "http://") {
		t.Error("a remote URL survived into the body (SAN-2)")
	}
}

func TestRemoteToggleResanitizesFromRaw(t *testing.T) {
	m := model{
		termWidth: 80, viewport: viewport.New(50, 20),
		rawHTML:       `<p>x</p><img src="http://track.er/p.png">`,
		currentParsed: &message.Parsed{TextHTML: `<p>x</p>`},
		loadRemote:    false,
	}
	// Toggling R with loadRemote staying false must keep the tracker out.
	updated, _ := m.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'R'}})
	if strings.Contains(updated.(model).sanitizedBody, "track.er") {
		t.Error("R toggle leaked the tracker (must re-sanitize from rawHTML, not pass raw)")
	}
}
```

- [ ] **Step 2: Run to verify fail**

Run: `cd cmd/mail-tui && go test -run "TestApplyBodySanitizes|TestRemoteToggleResanitizes" -v`
Expected: FAIL — `applyBody` undefined.

- [ ] **Step 3: Implementar `applyBody`** (sanitize-primero, SAN intacto de v0.3, sumidero = viewport)

```go
// applyBody is the gen-vigente handler for bodyLoadedMsg: sanitize FIRST (P-5/D4), RenderRich
// at the reader width, store the sanitized body, and feed the viewport. Decrements inflight.
func (m model) applyBody(parsed *message.Parsed) (tea.Model, tea.Cmd) {
	if m.inflight > 0 {
		m.inflight--
	}
	m.rawHTML = parsed.TextHTML
	m.loadRemote = false
	_, readerW, _ := panelDims(m.termWidth, m.termHeight)
	if readerW <= 0 {
		readerW = 80
	}
	san, err := message.SanitizeHTML(m.rawHTML, false)
	if err != nil {
		m.statusMsg = "error sanitizando el correo: " + err.Error()
		clean := *parsed
		clean.TextHTML = ""
		m.currentParsed = &clean
		body := ""
		if parsed.TextPlain != "" {
			body, _ = message.RenderRich(&clean, readerW)
		}
		m.sanitizedBody = body
		m.viewport.SetContent(body)
		m.viewport.GotoTop()
		m.imageBlobs = nil
		m.showImages = false
		return m, nil
	}
	clean := *parsed
	clean.TextHTML = san.HTML
	m.currentParsed = &clean
	body, _ := message.RenderRich(&clean, readerW)
	m.sanitizedBody = body
	m.viewport.SetContent(body) // SAN: body is POST-RenderRich sanitized, never rawHTML
	m.viewport.GotoTop()
	m.imageBlobs = nil
	m.showImages = false
	m.focus = focusReader // moving into a message focuses the reader
	return m, nil
}
```

- [ ] **Step 4: Migrar los handlers `i` y `R` al viewport (en `handleKey`, rama focusReader)**

Las teclas `i`/`R` solo aplican con `focus == focusReader`. Añadir al switch de runas en `handleKey` (antes del routing genérico al viewport), o manejarlas en la rama focusReader. El handler `R` re-sanitiza desde `rawHTML` (idéntico a v0.3) y escribe a `m.sanitizedBody` + `m.viewport.SetContent`. El handler `i` es el async de imágenes de v0.3 (ya devuelve `imagesRenderedMsg`), añadiendo `inflight++` y el spinner 0→1:

```go
case 'i':
	if m.currentParsed == nil || len(m.currentParsed.InlineImages) == 0 {
		m.statusMsg = "no hay imágenes inline en este correo"
		return m, nil
	}
	m.showImages = true
	imgs := m.currentParsed.InlineImages
	_, readerW, _ := panelDims(m.termWidth, m.termHeight)
	if readerW <= 0 {
		readerW = 80
	}
	w, h := readerW, readerW/2
	startSpinner := m.inflight == 0
	m.inflight++
	render := func() tea.Msg {
		var blobs []string
		for _, im := range imgs {
			blobs = append(blobs, renderpkg.RenderImage(context.Background(), im.Data, w, h))
		}
		return imagesRenderedMsg{blobs: blobs}
	}
	if startSpinner {
		return m, tea.Batch(render, m.spinner.Tick)
	}
	return m, render
case 'R':
	m.loadRemote = !m.loadRemote
	if m.currentParsed != nil && m.rawHTML != "" {
		_, readerW, _ := panelDims(m.termWidth, m.termHeight)
		if readerW <= 0 {
			readerW = 80
		}
		san, err := message.SanitizeHTML(m.rawHTML, m.loadRemote)
		if err != nil {
			m.statusMsg = "error sanitizando el correo: " + err.Error()
			return m, nil
		}
		clean := *m.currentParsed
		clean.TextHTML = san.HTML
		m.currentParsed = &clean
		body, _ := message.RenderRich(&clean, readerW)
		m.sanitizedBody = body
		m.viewport.SetContent(body) // SAN: re-sanitized, never raw
	}
	return m, nil
```

> El import del paquete de imágenes es `render "erickaldama-mail/internal/render"`; aquí se aliasa `renderpkg` para no chocar con la variable local `render`. Verificar el alias real en model.go (v0.3 lo importa como `render`); renombrar la closure local si hace falta.

- [ ] **Step 5: Implementar `startReply` (reply via selectedKey, NO m.headers)** — cierra B-7

```go
// startReply builds a reply composer pre-filled from the selected message, fetching the
// full parsed message for reply-all Cc. Uses selectedKey (the messageItem's S3Key), NOT the
// removed m.headers[m.selected] (audit B-7).
func (m model) startReply() (tea.Model, tea.Cmd) {
	it, ok := m.list.SelectedItem().(messageItem)
	if !ok {
		return m, nil
	}
	c := newComposer()
	subject := it.h.Subject
	if !strings.HasPrefix(strings.ToLower(subject), "re:") {
		subject = "Re: " + subject
	}
	c.inputs[cTo].SetValue(it.h.From)
	c.inputs[cSubject].SetValue(subject)
	if m.reader != nil && it.h.S3Key != "" {
		s3Key := it.h.S3Key
		r := m.reader
		self := m.from
		orig := c
		return m, func() tea.Msg {
			rc, err := r.Open(context.Background(), s3Key)
			if err != nil {
				return replyReadyMsg{c: orig}
			}
			defer rc.Close()
			parsed, err := message.Parse(rc)
			if err != nil {
				return replyReadyMsg{c: orig}
			}
			orig.inputs[cTo].SetValue(parsed.From)
			orig.inputs[cCc].SetValue(replyAllCc(parsed.To, parsed.Cc, self))
			_, _, subj := message.ReplyHeaders(parsed)
			orig.inputs[cSubject].SetValue(subj)
			return replyReadyMsg{c: orig}
		}
	}
	m.compose = c
	m.mode = modeComposer
	return m, nil
}
```

- [ ] **Step 6: Run all TUI tests + full build**

Run: `cd cmd/mail-tui && go build ./... && go test -count=1 -v`
Expected: build OK; los 14 tests nuevos + los 3 de composer de v0.2 PASS.

- [ ] **Step 7: Commit**

```bash
git -C "$WT" add cmd/mail-tui/model.go cmd/mail-tui/model_test.go
git -C "$WT" commit -m "feat(v0.4): reader async body/images/R/reply, SAN preserved, reply via S3Key (D8/SAN/B-7)"
```

---

### Task 8: Verificación end-to-end + CHANGELOG

**Files:**
- Modify: `CHANGELOG.md`
- Test: toda la suite

- [ ] **Step 1: Suite completa + vet + build de binarios**

Run: `go build ./... && go vet ./... && go test ./... -count=1`
Expected: 15+ paquetes verde, incluidos los 14 tests nuevos del TUI + los 3 de composer.

- [ ] **Step 2: Verificar el invariante SAN con grep (ningún SetContent recibe rawHTML)**

Run: `cd cmd/mail-tui && grep -nE "SetContent\(" model.go`
Expected: cada `SetContent` recibe `body`/`m.sanitizedBody`/`sanitizedBody + blobs` — NUNCA `m.rawHTML` ni `.TextHTML`.

- [ ] **Step 3: Instalar binarios + smoke de compilación**

Run: `go install ./cmd/mail ./cmd/mail-tui && which mail-tui`
Expected: binario instalado.

- [ ] **Step 4: Escribir la entrada de CHANGELOG**

Añadir a `CHANGELOG.md` una entrada `## [mail v0.4] — TUI estética (2 paneles) — 2026-06-30` narrando: el layout 2-paneles con focus alternable, viewport pager, list con filtro fuzzy, spinner async; y los hallazgos de la auditoría aplicados (A-4 race, B-1 trigger, C1/C2 layout) como evidencia.

- [ ] **Step 5: Commit**

```bash
git -C "$WT" add CHANGELOG.md
git -C "$WT" commit -m "docs(v0.4): CHANGELOG entry — TUI estética (2 paneles)"
```

---

## Smoke manual (Fase 5, tras el whole-branch review) — el NO VERIFICADO de UX en vivo

Arrancar `mail-tui` en iTerm+tmux y verificar a mano (las firmas son verdad de compilación; el render en TTY no se observó): (1) 2 paneles sin recorte; (2) Tab alterna foco visiblemente; (3) j/k scroll en reader + `[%]` se mueve; (4) `/` filtra; (5) Enter/selección abre el correo; (6) `i` muestra imágenes con chafa; (7) colores de foco distinguibles.
