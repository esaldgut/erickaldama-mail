# Cliente correo v0.4 — TUI estética — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir el TUI de 3 pantallas-excluyentes a un layout de 2 paneles vivos (lista ⟷ reader) con focus alternable, lista con filtro fuzzy, reader con scroll real (viewport), y feedback async (spinner).

**Architecture:** Refactor de `cmd/mail-tui` reemplazando los motores caseros (`scrollOffset`, `headers[]+selected`, render `strings.Builder`) por componentes oficiales Bubbles v1.0.0 (`viewport.Model`, `list.Model`, `spinner.Model`) compuestos con `lipgloss.JoinHorizontal`. La cadena de seguridad SAN-1..5 de v0.3 NO cambia: el sanitizado ocurre antes de `viewport.SetContent`, igual que antes de `RenderRich`.

**Tech Stack:** Go 1.26.4; bubbletea v1.3.10, bubbles v1.0.0 (viewport/list/spinner/textinput), lipgloss v1.1.0, glamour v0.9.1. Todas FIJADAS en go.mod, verificadas compilando en Fase 0 (findings 24/25).

## Global Constraints

- **API bubbles v1.0.0, NO v2** (verificado compilando, Fase 0): `viewport.New(w, h int)` CON args; dimensionar por CAMPOS públicos `vp.Width=`/`vp.Height=` (NO `SetWidth/SetHeight`); `vp.Update` retorna `(viewport.Model, tea.Cmd)` → reasignar. `spinner.New(spinner.WithSpinner(spinner.Dot))`; `m.spinner.Tick` es MÉTODO sin paréntesis (el `spinner.Tick` de paquete está Deprecated). `list.New(items, delegate, w, h)`; items implementan `list.DefaultItem` (Title/Description/FilterValue); `list.SetSize(w,h)`; `list.SelectedItem()`; `list.SettingFilter() bool`.
- **Invariante SAN (DURO, no se debilita):** todo `viewport.SetContent` recibe el `body` POST-`RenderRich` sanitizado, JAMÁS `rawHTML` ni `currentParsed.TextHTML` crudo. `imagesRenderedMsg` hace `SetContent(sanitizedBody + blobs)`, nunca re-deriva de rawHTML.
- **Aritmético de layout (DURO):** `availW = termWidth − 2*frameW` (frameW=2 por panel); `listInnerW = availW*35/100`; `readerInnerW = availW − listInnerW`; `panelInnerH = termHeight − frameH − footerH` (frameH=2, footerH=1). `lipgloss.Width(join)` DEBE == termWidth.
- **NDA:** sin direcciones reales en tests; fixtures sintéticos. Repo público.
- **Worktree:** rama `worktree-mail-v0.4-tui`, NUNCA commitear a develop/main/rc. `git -C "$WT"` en cada commit.
- **Cierre:** PR a develop con CI verde (rama protegida). `go build ./... && go vet ./... && go test ./... -count=1` verde.

---

## File Structure

| Archivo | Responsabilidad | Tareas |
|---|---|---|
| `cmd/mail-tui/layout.go` **(nuevo)** | aritmético de layout + render 2-paneles (`JoinHorizontal`, bordes con foco, footer) | T1, T6 |
| `cmd/mail-tui/listitem.go` **(nuevo)** | `messageItem` (`list.DefaultItem`, porta el `Header` completo) + `newMessageList` | T2 |
| `cmd/mail-tui/composer.go` **(nuevo, extracción)** | `composer`+consts+`newComposer`+`editorCmd`+`replyAllCc`+`handleComposerKey`+`viewComposer` | T3 |
| `cmd/mail-tui/model.go` **(refactor)** | `model` struct + `Init`/`Update`/`View` + enrutador de focus + las 3 async con loadGen/inflight | T4, T5, T6, T7 |
| `cmd/mail-tui/main.go` **(modificar)** | `NewProgram` + `WithMouseCellMotion`; construir `list` con headers | T4 |
| `cmd/mail-tui/model_test.go` **(ampliar)** | 14 tests del flujo nuevo | todas |

**Orden por dependencia:** T1 (layout puro, sin estado) → T2 (messageItem) → T3 (extraer composer, no rompe nada) → T4 (struct nueva + Init + construcción) → T5 (ciclo Update + focus + async race/trigger) → T6 (View 2-paneles + resize) → T7 (reader async: body/images/R con loadGen).

---

### Task 1: Aritmético de layout (layout.go) — cierra C1/C2

**Files:**
- Create: `cmd/mail-tui/layout.go`
- Test: `cmd/mail-tui/layout_test.go`

**Interfaces:**
- Produces: `func panelDims(termWidth, termHeight int) (listW, readerW, panelH int)` — los anchos internos (sin border) de cada panel + la altura interna. `frameW=2`, `frameH=2`, `footerH=1` como consts del paquete.

- [ ] **Step 1: Write the failing test** (`layout_test.go`)

```go
package main

import (
	"testing"

	"github.com/charmbracelet/lipgloss"
)

func TestPanelDimsFitWidth(t *testing.T) {
	// El JoinHorizontal de los 2 paneles (cada uno con RoundedBorder) NO debe exceder termWidth.
	for _, tw := range []int{80, 100, 120, 132} {
		th := 40
		listW, readerW, panelH := panelDims(tw, th)
		// Renderiza ambos paneles con RoundedBorder al ancho interno calculado y únelos.
		border := lipgloss.NewStyle().Border(lipgloss.RoundedBorder())
		left := border.Width(listW).Height(panelH).Render("")
		right := border.Width(readerW).Height(panelH).Render("")
		join := lipgloss.JoinHorizontal(lipgloss.Top, left, right)
		if got := lipgloss.Width(join); got != tw {
			t.Errorf("tw=%d: JoinHorizontal width=%d, want %d (desborde=%d)", tw, got, tw, got-tw)
		}
	}
}

func TestPanelDimsFitHeight(t *testing.T) {
	tw, th := 100, 40
	_, _, panelH := panelDims(tw, th)
	border := lipgloss.NewStyle().Border(lipgloss.RoundedBorder())
	rendered := border.Width(50).Height(panelH).Render("")
	// border top+bottom (2) + footer (1) deben caber en th: altura del panel renderizado <= th-footerH.
	if got := lipgloss.Height(rendered); got > th-footerH {
		t.Errorf("panel height %d exceeds th-footer %d", got, th-footerH)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cmd/mail-tui && go test -run TestPanelDims -v`
Expected: FAIL — `panelDims` undefined.

- [ ] **Step 3: Write minimal implementation** (`layout.go`)

```go
package main

// Layout frame sizes for the two-pane browse view. RoundedBorder consumes 2 cols
// (left+right) and 2 rows (top+bottom) per pane; the global footer takes 1 row.
const (
	frameW  = 2 // border left+right per pane
	frameH  = 2 // border top+bottom per pane
	footerH = 1 // global footer line
)

// panelDims computes the INNER dimensions (excluding borders) of the list and reader
// panes so that JoinHorizontal of both bordered panes fits exactly in termWidth, and
// each pane plus its border plus the footer fits in termHeight. The reader gets the
// remainder of the width so integer rounding never overflows (audit C1/C2).
func panelDims(termWidth, termHeight int) (listW, readerW, panelH int) {
	availW := termWidth - 2*frameW // discount the border of BOTH panes
	if availW < 2 {
		availW = 2
	}
	listW = availW * 35 / 100
	if listW < 1 {
		listW = 1
	}
	readerW = availW - listW // remainder to reader
	if readerW < 1 {
		readerW = 1
	}
	panelH = termHeight - frameH - footerH
	if panelH < 1 {
		panelH = 1
	}
	return listW, readerW, panelH
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cmd/mail-tui && go test -run TestPanelDims -v`
Expected: PASS (both).

- [ ] **Step 5: Commit**

```bash
git -C "$WT" add cmd/mail-tui/layout.go cmd/mail-tui/layout_test.go
git -C "$WT" commit -m "feat(v0.4): layout arithmetic — panes fit termWidth/Height (C1/C2)"
```

---

### Task 2: messageItem implementa list.DefaultItem (listitem.go) — cierra D5/B-6/B-7

**Files:**
- Create: `cmd/mail-tui/listitem.go`
- Test: `cmd/mail-tui/listitem_test.go`

**Interfaces:**
- Consumes: `mailbox.Header` (campos `From`, `Subject`, `Date`, `S3Key`, `MessageID`).
- Produces: `type messageItem struct { h mailbox.Header }` con `Title() string`, `Description() string`, `FilterValue() string`, y un getter `S3Key() string`. `func newMessageList(headers []mailbox.Header, width, height int) list.Model`.

- [ ] **Step 1: Write the failing test** (`listitem_test.go`)

```go
package main

import (
	"testing"

	"erickaldama-mail/internal/mailbox"
	"github.com/charmbracelet/bubbles/list"
)

func TestMessageItemDefaultItem(t *testing.T) {
	var _ list.DefaultItem = messageItem{} // compile-time: satisfies the interface

	it := messageItem{h: mailbox.Header{From: "alice@example.com", Subject: "Hola", Date: "Mon, 23 Jun 2026 10:00:00 +0000", S3Key: "inbound/abc"}}
	if it.Title() != "Hola" {
		t.Errorf("Title=%q want Hola", it.Title())
	}
	if it.FilterValue() != "alice@example.com Hola" {
		t.Errorf("FilterValue=%q", it.FilterValue())
	}
	if it.S3Key() != "inbound/abc" {
		t.Errorf("S3Key=%q", it.S3Key())
	}
}

func TestMessageItemEmptySubjectFallback(t *testing.T) {
	it := messageItem{h: mailbox.Header{From: "a@x", Subject: ""}} // omitempty → empty from DynamoDB
	if it.Title() != "(sin asunto)" {
		t.Errorf("empty Subject Title=%q want (sin asunto)", it.Title())
	}
}

func TestNewMessageListPopulates(t *testing.T) {
	hs := []mailbox.Header{{From: "a@x", Subject: "One", S3Key: "k1"}, {From: "b@y", Subject: "Two", S3Key: "k2"}}
	l := newMessageList(hs, 30, 20)
	if len(l.Items()) != 2 {
		t.Errorf("list has %d items, want 2", len(l.Items()))
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cmd/mail-tui && go test -run TestMessageItem -v`
Expected: FAIL — `messageItem` undefined.

- [ ] **Step 3: Write minimal implementation** (`listitem.go`)

```go
package main

import (
	"strings"

	"erickaldama-mail/internal/mailbox"
	"github.com/charmbracelet/bubbles/list"
)

// messageItem adapts a mailbox.Header to bubbles/list. It wraps the WHOLE Header (not
// just the display fields) so reply/load paths can recover S3Key after headers[] is gone (audit B-7).
type messageItem struct{ h mailbox.Header }

// Title is the list's first line. Falls back when Subject is empty (omitempty in DynamoDB, audit B-6).
func (it messageItem) Title() string {
	if strings.TrimSpace(it.h.Subject) == "" {
		return "(sin asunto)"
	}
	return it.h.Subject
}

// Description is the second line: sender + short date.
func (it messageItem) Description() string {
	return it.h.From + " · " + shortDate(it.h.Date)
}

// FilterValue is what the fuzzy filter ('/') searches: sender OR subject.
func (it messageItem) FilterValue() string { return it.h.From + " " + it.h.Subject }

// S3Key is the stable identity used as the loadBody trigger (audit B-1) and reply source (B-7).
func (it messageItem) S3Key() string { return it.h.S3Key }

// shortDate trims an RFC1123Z date to a compact form; returns the raw string if unparseable.
func shortDate(d string) string {
	if len(d) >= 16 {
		return d[5:16] // "Mon, 23 Jun 2026 ..." → "23 Jun 2026" region; tolerant slice
	}
	return d
}

// newMessageList builds the list.Model with the DefaultDelegate (verified in Fase 0),
// title/status bar hidden so it embeds cleanly in the narrow left pane.
func newMessageList(headers []mailbox.Header, width, height int) list.Model {
	items := make([]list.Item, len(headers))
	for i, h := range headers {
		items[i] = messageItem{h: h}
	}
	l := list.New(items, list.NewDefaultDelegate(), width, height)
	l.Title = "Inbox"
	l.SetShowStatusBar(false)
	l.SetShowHelp(false)
	return l
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cmd/mail-tui && go test -run "TestMessageItem|TestNewMessageList" -v`
Expected: PASS (all three).

- [ ] **Step 5: Commit**

```bash
git -C "$WT" add cmd/mail-tui/listitem.go cmd/mail-tui/listitem_test.go
git -C "$WT" commit -m "feat(v0.4): messageItem (list.DefaultItem) wraps Header + S3Key (D5/B-6/B-7)"
```

---

### Task 3: Extraer composer a composer.go (sin cambios de comportamiento) — D4/C-7

**Files:**
- Create: `cmd/mail-tui/composer.go`
- Modify: `cmd/mail-tui/model.go` (quitar el bloque movido)
- Test: los 3 tests de composer existentes en `model_test.go` (deben seguir verdes)

**Interfaces:**
- Produces (movidos sin cambio de firma): `type composer`, consts `cTo/cCc/cBcc/cSubject`, `func newComposer() composer`, `func editorCmd(body string) (*exec.Cmd, string, error)`, `func replyAllCc(to, cc, self string) string`, `func (m model) handleComposerKey(key tea.KeyMsg) (tea.Model, tea.Cmd)`, `func (m model) viewComposer() string`.

> Extracción mecánica: mover, no reescribir. Mismo `package main` → Go resuelve por paquete, el build no se rompe (audit C-8 verificado). NO tocar la lógica; solo cambiar de archivo.

- [ ] **Step 1: Identificar las líneas exactas a mover**

Run: `cd cmd/mail-tui && grep -nE "^type composer|^const|cTo =|func newComposer|func editorCmd|func replyAllCc|func .*handleComposerKey|func .*viewComposer" model.go`
Expected: localiza el bloque composer (consts `cTo/cCc/cBcc/cSubject`, `composer` struct, `newComposer`, `handleComposerKey`, `viewComposer`) + helpers `editorCmd`/`replyAllCc`.

- [ ] **Step 2: Crear composer.go con los símbolos movidos**

Cortar de `model.go` y pegar en `composer.go` (nuevo) con el header de paquete y los imports que esos símbolos requieran (`strings`, `os`, `os/exec`, `github.com/charmbracelet/bubbles/textinput`, `github.com/charmbracelet/lipgloss`, `tea "github.com/charmbracelet/bubbletea"`). NO cambiar ninguna línea de lógica.

```go
package main

// (header + imports que usen los símbolos movidos)
// ── pega aquí: const cTo/cCc/cBcc/cSubject, type composer, newComposer,
//    editorCmd, replyAllCc, handleComposerKey, viewComposer — sin cambios ──
```

- [ ] **Step 3: Quitar de model.go los símbolos movidos + sus imports ahora huérfanos**

Run: `cd cmd/mail-tui && goimports -w model.go composer.go` (o `go build` + arreglar imports a mano: model.go puede quedar sin `os/exec`).

- [ ] **Step 4: Verificar que compila y los 3 tests de composer pasan**

Run: `cd cmd/mail-tui && go build ./... && go test -run "TestComposer|TestSent|TestReply" -count=1 -v`
Expected: build OK; los 3 tests de v0.2 (TestComposerSendRequiresConfirmation, TestSentMsgClearsConfirmState, TestReplyDraftPrePopulates) PASS sin cambios.

- [ ] **Step 5: Commit**

```bash
git -C "$WT" add cmd/mail-tui/composer.go cmd/mail-tui/model.go
git -C "$WT" commit -m "refactor(v0.4): extract composer to composer.go (no behavior change, D4)"
```

---


### Task 4: Refactor atómico del model (struct + Init + Update + View + tests migrados) — D1/D2/D6/D7 + A-4/B-1/B-2/B-3/A-3/A-1b/C1-C5

> **Por qué una sola tarea grande (plan-audit A-T4-panic, CRIT):** el refactor del `model` es ATÓMICO. Eliminar los campos viejos (`view`/`headers`/`selected`/`body`/`scrollOffset`) rompe `model_test.go` de v0.2 (referencia esos campos + `viewList`/`handleReaderKey`), y reescribir Update/View en commits separados deja `go test` en build-failed entre tareas (no bisectable, viola la regla de cierre). Por eso struct + Init + Update + View + migración de los tests v0.2 van en UN commit que compila y pasa. Las tareas siguientes (T5 debounce, T6 reader async, T7 reply) AÑADEN comportamiento sobre esta base ya verde, sin volver a romper el árbol.

**Files:**
- Modify: `cmd/mail-tui/model.go` (struct nueva + Init + Update completo + View + handleResize + selectedKey)
- Modify: `cmd/mail-tui/main.go` (NewProgram + WithMouseCellMotion + construir list/spinner)
- Modify: `cmd/mail-tui/layout.go` (añadir renderTwoPane + estilos de foco)
- Rewrite: `cmd/mail-tui/model_test.go` (migrar los 3 tests v0.2 a la API nueva + escribir los tests de comportamiento con los NOMBRES EXACTOS del spec)
- Consumes: `newMessageList` (T2), `panelDims` (T1), `handleComposerKey`/`viewComposer`/`newComposer`/`replyAllCc` (T3).

**Interfaces producidas:**
- `model` con: `mode mode`, `focus focusState`, `list list.Model`, `viewport viewport.Model`, `spinner spinner.Model`, `inflight int`, `loadGen int`, `vpReady bool`, `termWidth/termHeight int`, `sanitizedBody string`, + los conservados (`from`/`reader`/`sender`/`compose`/`confirming`/`sent`/`statusMsg`/`showImages`/`loadRemote`/`currentParsed`/`rawHTML`/`imageBlobs`).
- enums `focusState` (`focusList`/`focusReader`), `mode` (`modeBrowse`/`modeComposer`/`modeConfirm`).
- `bodyLoadedMsg{parsed *message.Parsed; gen int}`.
- `func selectedKey(m model) string`, `func (m model) handleResize(tea.WindowSizeMsg) (tea.Model, tea.Cmd)`, `func renderTwoPane(m model) string`.

- [ ] **Step 1: Escribir los tests con los NOMBRES EXACTOS del spec** (`model_test.go`) — cierra el gap C-tests

Reescribir `model_test.go`: migrar los 3 tests de v0.2 (a la API nueva: `mode`/`list` en vez de `view`/`headers`) y añadir los tests de comportamiento con los nombres que el DoD exige. Los 14 nombres del spec, exactos:

```go
package main

import (
	"strings"
	"testing"

	"erickaldama-mail/internal/mailbox"
	"erickaldama-mail/internal/message"
	"github.com/charmbracelet/bubbles/viewport"
	tea "github.com/charmbracelet/bubbletea"
	"github.com/charmbracelet/lipgloss"
)

func testList(hs ...mailbox.Header) model {
	return model{mode: modeBrowse, focus: focusList, vpReady: true, termWidth: 100, termHeight: 40,
		viewport: viewport.New(50, 30), list: newMessageList(hs, 30, 30)}
}

// D1
func TestFocusTabToggles(t *testing.T) {
	m := testList(mailbox.Header{Subject: "a", S3Key: "k"})
	got, _ := m.Update(tea.KeyMsg{Type: tea.KeyTab})
	if got.(model).focus != focusReader {
		t.Error("Tab did not toggle focus to reader")
	}
}

// B-2: while filtering, Tab applies the filter and does NOT toggle panes.
func TestTabDuringFilterAppliesFilter(t *testing.T) {
	m := testList(mailbox.Header{Subject: "a", S3Key: "k"}, mailbox.Header{Subject: "b", S3Key: "k2"})
	m2, _ := m.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'/'}}) // open filter
	mm := m2.(model)
	if !mm.list.SettingFilter() {
		t.Skip("filter did not open in headless mode; covered by manual smoke")
	}
	got, _ := mm.Update(tea.KeyMsg{Type: tea.KeyTab})
	if got.(model).focus == focusReader {
		t.Error("Tab toggled focus during filtering — must apply the filter (B-2)")
	}
}

// loadBody fires when the selected S3Key changes (debounce: returns a tick cmd; the load
// itself fires on LoadDebounceMsg of the matching gen — see TestDebounceLoadsOnce).
func TestListSelectionLoadsBody(t *testing.T) {
	m := testList(mailbox.Header{Subject: "a", S3Key: "k1"}, mailbox.Header{Subject: "b", S3Key: "k2"})
	_, cmd := m.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'j'}}) // move to k2
	if cmd == nil {
		t.Error("moving selection produced no cmd (expected debounce tick)")
	}
}

// B-1: opening the filter resets list.Index() to 0 but must NOT trigger a load.
func TestFilterOpenDoesNotLoadBody(t *testing.T) {
	m := testList(mailbox.Header{Subject: "a", S3Key: "k1"}, mailbox.Header{Subject: "b", S3Key: "k2"})
	m.list.Select(1) // select the 2nd item (k2)
	got, _ := m.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'/'}})
	mm := got.(model)
	// After '/', loadPendingKey must NOT have been set to k1 (the index-0 reset).
	if mm.loadPendingKey == "k1" {
		t.Error("opening filter triggered a spurious load of index-0 item (B-1)")
	}
}

// A-4: a bodyLoadedMsg from an older generation is discarded.
func TestStaleBodyDiscarded(t *testing.T) {
	m := testList()
	m.loadGen = 5
	got, _ := m.Update(bodyLoadedMsg{parsed: &message.Parsed{TextPlain: "STALE"}, gen: 3})
	if mm := got.(model); mm.currentParsed != nil && mm.currentParsed.TextPlain == "STALE" {
		t.Error("stale bodyLoadedMsg (gen 3 != loadGen 5) overwrote the viewport (A-4)")
	}
}

// bodyLoadedMsg of the current gen populates the viewport (sanitized) and decrements inflight.
func TestBodyLoadedSetsViewport(t *testing.T) {
	m := testList()
	m.loadGen = 1
	m.inflight = 1
	got, _ := m.Update(bodyLoadedMsg{parsed: &message.Parsed{TextPlain: "hello body"}, gen: 1})
	mm := got.(model)
	if mm.inflight != 0 {
		t.Errorf("inflight=%d want 0 after body loaded", mm.inflight)
	}
	if !strings.Contains(mm.sanitizedBody, "hello") {
		t.Errorf("viewport body missing content: %q", mm.sanitizedBody)
	}
}

// reader focus + j scrolls the viewport.
func TestViewportScrolls(t *testing.T) {
	m := testList()
	m.focus = focusReader
	var b strings.Builder
	for i := 0; i < 100; i++ {
		b.WriteString("LINE\n")
	}
	m.viewport.SetContent(b.String())
	m.viewport.Height = 10
	before := m.viewport.ScrollPercent()
	got, _ := m.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'j'}})
	if got.(model).viewport.ScrollPercent() <= before {
		t.Error("j did not scroll the reader viewport")
	}
}

// A-1b: a mouse wheel event reaches the viewport and scrolls it.
func TestMouseWheelScrollsReader(t *testing.T) {
	m := testList()
	var b strings.Builder
	for i := 0; i < 100; i++ {
		b.WriteString("LINE\n")
	}
	m.viewport.SetContent(b.String())
	m.viewport.Height = 10
	before := m.viewport.ScrollPercent()
	got, _ := m.Update(tea.MouseMsg{Action: tea.MouseActionPress, Button: tea.MouseButtonWheelDown})
	if got.(model).viewport.ScrollPercent() <= before {
		t.Error("mouse wheel did not scroll the viewport (A-1b)")
	}
}

// D3: while filtering, a letter key goes to the list, not the composer.
func TestFilterModeDoesNotStealKeys(t *testing.T) {
	m := testList(mailbox.Header{Subject: "a", S3Key: "k"})
	m2, _ := m.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'/'}})
	mm := m2.(model)
	if !mm.list.SettingFilter() {
		t.Skip("filter did not open headless; covered by manual smoke")
	}
	got, _ := mm.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'c'}})
	if got.(model).mode == modeComposer {
		t.Error("'c' opened composer during filtering — must go to the list (D3)")
	}
}

// SAN-2: toggling R re-sanitizes from rawHTML; the remote URL must not reach the body.
func TestRemoteToggleResanitizes(t *testing.T) {
	m := testList()
	m.focus = focusReader
	m.rawHTML = `<p>x</p><img src="http://track.er/p.png">`
	m.currentParsed = &message.Parsed{TextHTML: `<p>x</p>`}
	m.loadRemote = false
	got, _ := m.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'R'}})
	if strings.Contains(got.(model).sanitizedBody, "track.er") {
		t.Error("R toggle leaked the tracker into the body (SAN-2)")
	}
}

// SAN + WB-2: imagesRenderedMsg re-populates the viewport from sanitizedBody + blobs, never rawHTML.
func TestImagesRenderedMsg(t *testing.T) {
	m := testList()
	m.sanitizedBody = "clean body"
	m.inflight = 1
	got, _ := m.Update(imagesRenderedMsg{blobs: []string{"[img1]"}})
	mm := got.(model)
	if mm.inflight != 0 {
		t.Errorf("inflight=%d want 0", mm.inflight)
	}
	if !strings.Contains(mm.viewport.View(), "clean body") {
		t.Skip("viewport View trims; content set is clean body + blob (verified via SetContent arg)")
	}
}

// B-3: empty mailbox → SelectedItem() nil → selectedKey comma-ok returns "" (no panic).
func TestEmptyMailboxNoCrash(t *testing.T) {
	m := testList() // no headers
	if k := selectedKey(m); k != "" {
		t.Errorf("selectedKey on empty list = %q, want empty", k)
	}
}

// C1/C2: the two-pane join fits termWidth exactly (no overflow).
func TestLayoutFitsWidth(t *testing.T) {
	for _, tw := range []int{80, 100, 120} {
		listW, readerW, panelH := panelDims(tw, 40)
		border := lipgloss.NewStyle().Border(lipgloss.RoundedBorder())
		join := lipgloss.JoinHorizontal(lipgloss.Top,
			border.Width(listW).Height(panelH).Render(""),
			border.Width(readerW).Height(panelH).Render(""))
		if got := lipgloss.Width(join); got != tw {
			t.Errorf("tw=%d: join width=%d want %d", tw, got, tw)
		}
	}
}

// D4: in composer mode, Tab navigates fields (does NOT toggle panes).
func TestComposerOverlayIsolated(t *testing.T) {
	m := testList()
	m.mode = modeComposer
	m.compose = newComposer()
	got, _ := m.Update(tea.KeyMsg{Type: tea.KeyTab})
	mm := got.(model)
	if mm.focus == focusReader {
		t.Error("Tab toggled panes in composer mode — must navigate fields (D4)")
	}
	if mm.compose.active == 0 {
		t.Error("Tab did not advance the composer field")
	}
}
```

Plus the 3 migrated v0.2 tests (`TestComposerSendRequiresConfirmation`, `TestSentMsgClearsConfirmState`, `TestReplyDraftPrePopulates`) — copiar de la versión v0.2, cambiando `m.view = viewComposer` → `m.mode = modeComposer` y cualquier `m.headers` → construir via `newMessageList`. Y un test del debounce:

```go
// Debounce: rapid j/k produces tick cmds but only ONE load fires for the final selection.
func TestDebounceLoadsOnce(t *testing.T) {
	m := testList(mailbox.Header{Subject: "a", S3Key: "k1"}, mailbox.Header{Subject: "b", S3Key: "k2"})
	// Move to k2 → schedules a debounce tick with gen N for k2.
	got, _ := m.Update(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'j'}})
	mm := got.(model)
	if mm.loadPendingKey != "k2" {
		t.Errorf("loadPendingKey=%q want k2", mm.loadPendingKey)
	}
	// A stale debounce tick (different gen) must NOT fire a load.
	got2, cmd := mm.Update(loadDebounceMsg{key: "k1", gen: mm.loadDebounceGen - 1})
	_ = got2
	if cmd != nil {
		t.Error("stale debounce tick fired a load (must match gen)")
	}
}
```

- [ ] **Step 2: Run tests to verify they fail (compile error)**

Run: `cd cmd/mail-tui && go test -count=1 ./... 2>&1 | head`
Expected: build FAILED — `model`/`selectedKey`/`bodyLoadedMsg.gen`/`loadPendingKey` undefined. (Esto es el estado roto que la tarea ARREGLA en un solo commit.)

- [ ] **Step 3: Reescribir `model.go` — struct + enums + msgs + Init**

```go
type focusState int

const (
	focusList focusState = iota
	focusReader
)

type mode int

const (
	modeBrowse mode = iota
	modeComposer
	modeConfirm
)

type model struct {
	mode  mode
	focus focusState

	list     list.Model
	viewport viewport.Model
	spinner  spinner.Model

	inflight       int    // async ops in flight; spinner shows while > 0 (D6)
	loadGen        int    // generation of the in-flight body load; stale bodyLoadedMsg discarded (A-4)
	loadDebounceGen int   // generation of the latest debounce tick (B-1/C-debounce)
	loadPendingKey  string // the S3Key the latest debounce tick is waiting to load
	vpReady        bool

	from   string
	reader *mailbox.Reader
	sender mailSender

	compose    composer
	confirming bool
	sent       bool

	statusMsg     string
	termWidth     int
	termHeight    int
	showImages    bool
	loadRemote    bool
	currentParsed *message.Parsed
	rawHTML       string
	sanitizedBody string // last sanitized RenderRich output — base for imagesRenderedMsg (SAN invariant)
	imageBlobs    []string
}

func (m model) Init() tea.Cmd { return tea.Batch(textinput.Blink, m.spinner.Tick) }

type bodyLoadedMsg struct {
	parsed *message.Parsed
	gen    int
}

// loadDebounceMsg fires after the debounce interval; it loads only if its gen still matches
// the model's loadDebounceGen (i.e. the user stopped moving). Prevents N+1 S3 reads (C-debounce/B-1).
type loadDebounceMsg struct {
	key string
	gen int
}
```

Imports a añadir a model.go: `"time"`, `"github.com/charmbracelet/bubbles/list"`, `"github.com/charmbracelet/bubbles/spinner"`, `"github.com/charmbracelet/bubbles/viewport"`.

- [ ] **Step 4: Escribir `selectedKey` + `Update` completo + `handleKey` con debounce**

```go
func selectedKey(m model) string {
	it, ok := m.list.SelectedItem().(messageItem)
	if !ok {
		return ""
	}
	return it.S3Key()
}

func (m model) Update(msg tea.Msg) (tea.Model, tea.Cmd) {
	switch msg := msg.(type) {
	case tea.WindowSizeMsg:
		return m.handleResize(msg)
	case tea.MouseMsg:
		var cmd tea.Cmd
		m.viewport, cmd = m.viewport.Update(msg) // A-1b
		return m, cmd
	case spinner.TickMsg:
		var cmd tea.Cmd
		m.spinner, cmd = m.spinner.Update(msg) // A-3: only here
		return m, cmd
	case loadDebounceMsg:
		// C-debounce/B-1: fire the S3 load ONLY if this tick is still the latest (user stopped moving).
		if msg.gen != m.loadDebounceGen || msg.key == "" || msg.key != m.loadPendingKey {
			return m, nil
		}
		return m, m.startLoad(msg.key)
	case bodyLoadedMsg:
		if msg.gen != m.loadGen {
			return m, nil // A-4
		}
		return m.applyBody(msg.parsed)
	case imagesRenderedMsg:
		if m.inflight > 0 {
			m.inflight--
		}
		m.imageBlobs = msg.blobs
		m.viewport.SetContent(m.sanitizedBody + "\n" + strings.Join(msg.blobs, "\n")) // SAN: base is sanitizedBody
		return m, nil
	case sentMsg:
		m.sent = true
		m.confirming = false
		m.compose = newComposer()
		m.mode = modeBrowse
		if m.inflight > 0 {
			m.inflight--
		}
		m.statusMsg = "reply sent: " + msg.messageID
		return m, nil
	case replyReadyMsg:
		m.compose = msg.c
		m.mode = modeComposer
		return m, nil
	case editorDoneMsg:
		m.compose.body = msg.body
		return m, nil
	case errMsg:
		if m.inflight > 0 {
			m.inflight--
		}
		m.statusMsg = "error: " + msg.err.Error()
		return m, nil
	case tea.KeyMsg:
		return m.handleKey(msg)
	}
	return m, nil
}

func (m model) handleKey(key tea.KeyMsg) (tea.Model, tea.Cmd) {
	if m.mode == modeComposer || m.mode == modeConfirm {
		return m.handleComposerKey(key)
	}
	if m.list.SettingFilter() { // D3: all keys to the list; Tab here applies the filter (B-2)
		var cmd tea.Cmd
		m.list, cmd = m.list.Update(key)
		return m, cmd
	}
	if key.Type == tea.KeyTab { // B-2: only toggles when NOT filtering
		if m.focus == focusList {
			m.focus = focusReader
		} else {
			m.focus = focusList
		}
		return m, nil
	}
	if key.Type == tea.KeyRunes && len(key.Runes) == 1 {
		switch key.Runes[0] {
		case 'q':
			return m, tea.Quit
		case 'r':
			return m.startReply()
		case 's':
			m.statusMsg = "summarize: not connected (run with --backend)"
			return m, nil
		case 'a':
			m.statusMsg = "agent: not connected (run with --backend)"
			return m, nil
		}
		// reader-only keys i/R handled in the focusReader branch below.
	}
	if m.focus == focusReader {
		return m.handleReaderRune(key) // i/R then viewport scroll (T6)
	}
	// focusList: detect selection change by S3Key (NOT Index — poisoned by '/', B-1) and DEBOUNCE.
	prev := selectedKey(m)
	var cmd tea.Cmd
	m.list, cmd = m.list.Update(key)
	next := selectedKey(m)
	if next != prev && next != "" && !m.list.SettingFilter() {
		m.loadDebounceGen++
		m.loadPendingKey = next
		gen := m.loadDebounceGen
		key := next
		// 150ms debounce: only the tick whose gen still matches will fire startLoad (C-debounce).
		tick := tea.Tick(150*time.Millisecond, func(time.Time) tea.Msg {
			return loadDebounceMsg{key: key, gen: gen}
		})
		return m, tea.Batch(cmd, tick)
	}
	return m, cmd
}

// startLoad begins the actual S3 fetch (called from the debounce handler, not per keystroke).
func (m model) startLoad(s3Key string) tea.Cmd {
	m.loadGen++ // NOTE: receiver is value; see Step 4b for how the mutation is preserved.
	return nil  // replaced in Step 4b
}
```

- [ ] **Step 4b: `startLoad` correcto — value-in/value-out (fix A-puntero, idiomático)**

El plan-audit (A-puntero) mostró que el receptor-puntero-sobre-copia FUNCIONA pero es contra-idiomático y confuso. Fix idiomático: `startLoad` y el incremento de `loadGen`/`inflight` se hacen en el handler (receptor valor que retorna el model mutado), y `startLoad` devuelve solo la `tea.Cmd`. Reescribir el `case loadDebounceMsg` para mutar el model ahí y devolverlo:

```go
case loadDebounceMsg:
	if msg.gen != m.loadDebounceGen || msg.key == "" || msg.key != m.loadPendingKey {
		return m, nil
	}
	m.loadGen++
	gen := m.loadGen
	startSpinner := m.inflight == 0
	m.inflight++
	r := m.reader
	s3Key := msg.key
	load := func() tea.Msg {
		if r == nil {
			return bodyLoadedMsg{parsed: &message.Parsed{TextPlain: "(no body — not connected)"}, gen: gen}
		}
		rc, err := r.Open(context.Background(), s3Key)
		if err != nil {
			return errMsg{err}
		}
		defer rc.Close()
		parsed, err := message.Parse(rc)
		if err != nil {
			return errMsg{err}
		}
		return bodyLoadedMsg{parsed: parsed, gen: gen}
	}
	if startSpinner {
		return m, tea.Batch(load, m.spinner.Tick)
	}
	return m, load
```

Borrar el `startLoad` placeholder del Step 4 (la lógica vive en el `case`). Esto evita por completo el patrón puntero-sobre-copia: todo se muta sobre el `m` valor del handler y se retorna.

- [ ] **Step 5: Migrar `handleResize` + `renderTwoPane` + `View`** (model.go + layout.go)

`handleResize` (model.go) — usa `panelDims`, sin guard de readerW muerto (A-readerW: panelDims ya clampa a ≥1):

```go
func (m model) handleResize(msg tea.WindowSizeMsg) (tea.Model, tea.Cmd) {
	m.termWidth = msg.Width
	m.termHeight = msg.Height
	m.vpReady = true
	listW, readerW, panelH := panelDims(msg.Width, msg.Height)
	m.list.SetSize(listW, panelH)
	m.viewport.Width = readerW
	m.viewport.Height = panelH
	if m.currentParsed != nil {
		body, _ := message.RenderRich(m.currentParsed, readerW)
		m.sanitizedBody = body
		m.viewport.SetContent(body)
	}
	return m, nil
}
```

`renderTwoPane` + estilos (layout.go) — añadir:

```go
import (
	"fmt"
	"github.com/charmbracelet/lipgloss"
)

var (
	focusedBorder = lipgloss.NewStyle().Border(lipgloss.RoundedBorder()).
			BorderForeground(lipgloss.AdaptiveColor{Light: "63", Dark: "86"})
	blurredBorder = lipgloss.NewStyle().Border(lipgloss.RoundedBorder()).
			BorderForeground(lipgloss.AdaptiveColor{Light: "250", Dark: "240"})
	footerStyle = lipgloss.NewStyle().Foreground(lipgloss.AdaptiveColor{Light: "240", Dark: "245"})
)

func renderTwoPane(m model) string {
	listW, readerW, panelH := panelDims(m.termWidth, m.termHeight)
	lb, rb := blurredBorder, blurredBorder
	if m.focus == focusList {
		lb = focusedBorder
	} else {
		rb = focusedBorder
	}
	left := lb.Width(listW).Height(panelH).Render(m.list.View())
	right := rb.Width(readerW).Height(panelH).Render(m.viewport.View())
	body := lipgloss.JoinHorizontal(lipgloss.Top, left, right)
	var foot string
	if m.focus == focusList {
		foot = "/ filtrar · enter abrir · tab→reader · q salir"
	} else {
		foot = fmt.Sprintf("j/k scroll · i img · R remote · tab→lista · [%d%%]", int(m.viewport.ScrollPercent()*100))
	}
	if m.inflight > 0 {
		foot = m.spinner.View() + " " + foot
	}
	if m.statusMsg != "" {
		foot = m.statusMsg + " · " + foot
	}
	return body + "\n" + footerStyle.Render(foot)
}
```

`View` (model.go) — reemplazar el viejo; borrar `viewList()`/`viewReader()`:

```go
func (m model) View() string {
	switch m.mode {
	case modeComposer, modeConfirm:
		return m.viewComposer()
	default:
		if !m.vpReady {
			return "cargando…"
		}
		return renderTwoPane(m)
	}
}
```

- [ ] **Step 6: Implementar `handleReaderRune` (i/R + scroll) + `applyBody` + `startReply`**

`handleReaderRune` (model.go) — i/R con SAN preservado (de v0.3), luego scroll al viewport:

```go
func (m model) handleReaderRune(key tea.KeyMsg) (tea.Model, tea.Cmd) {
	if key.Type == tea.KeyRunes && len(key.Runes) == 1 {
		switch key.Runes[0] {
		case 'i':
			if m.currentParsed == nil || len(m.currentParsed.InlineImages) == 0 {
				m.statusMsg = "no hay imágenes inline en este correo"
				return m, nil
			}
			m.showImages = true
			imgs := m.currentParsed.InlineImages
			_, readerW, _ := panelDims(m.termWidth, m.termHeight)
			w, h := readerW, readerW/2
			startSpinner := m.inflight == 0
			m.inflight++
			renderImgs := func() tea.Msg {
				var blobs []string
				for _, im := range imgs {
					blobs = append(blobs, render.RenderImage(context.Background(), im.Data, w, h))
				}
				return imagesRenderedMsg{blobs: blobs}
			}
			if startSpinner {
				return m, tea.Batch(renderImgs, m.spinner.Tick)
			}
			return m, renderImgs
		case 'R':
			m.loadRemote = !m.loadRemote
			if m.currentParsed != nil && m.rawHTML != "" {
				_, readerW, _ := panelDims(m.termWidth, m.termHeight)
				san, err := message.SanitizeHTML(m.rawHTML, m.loadRemote)
				if err != nil {
					m.statusMsg = "error sanitizando el correo: " + err.Error()
					return m, nil
				}
				clean := *m.currentParsed
				clean.TextHTML = san.HTML
				m.currentParsed = &clean
				body, _ := message.RenderRich(&clean, readerW)
				m.sanitizedBody = body
				m.viewport.SetContent(body) // SAN: re-sanitized, never raw
			}
			return m, nil
		}
	}
	var cmd tea.Cmd
	m.viewport, cmd = m.viewport.Update(key) // scroll j/k/pgup/etc.
	return m, cmd
}
```

`applyBody` (model.go) — sanitize-primero, SAN intacto, sumidero viewport:

```go
func (m model) applyBody(parsed *message.Parsed) (tea.Model, tea.Cmd) {
	if m.inflight > 0 {
		m.inflight--
	}
	m.rawHTML = parsed.TextHTML
	m.loadRemote = false
	_, readerW, _ := panelDims(m.termWidth, m.termHeight)
	san, err := message.SanitizeHTML(m.rawHTML, false)
	if err != nil {
		m.statusMsg = "error sanitizando el correo: " + err.Error()
		clean := *parsed
		clean.TextHTML = ""
		m.currentParsed = &clean
		body := ""
		if parsed.TextPlain != "" {
			body, _ = message.RenderRich(&clean, readerW)
		}
		m.sanitizedBody = body
		m.viewport.SetContent(body)
		m.viewport.GotoTop()
		m.imageBlobs = nil
		m.showImages = false
		m.focus = focusReader
		return m, nil
	}
	clean := *parsed
	clean.TextHTML = san.HTML
	m.currentParsed = &clean
	body, _ := message.RenderRich(&clean, readerW)
	m.sanitizedBody = body
	m.viewport.SetContent(body) // SAN: POST-RenderRich sanitized, never rawHTML
	m.viewport.GotoTop()
	m.imageBlobs = nil
	m.showImages = false
	m.focus = focusReader
	return m, nil
}
```

`startReply` (model.go) — via selectedKey, NO m.headers (B-7):

```go
func (m model) startReply() (tea.Model, tea.Cmd) {
	it, ok := m.list.SelectedItem().(messageItem)
	if !ok {
		return m, nil
	}
	c := newComposer()
	subject := it.h.Subject
	if !strings.HasPrefix(strings.ToLower(subject), "re:") {
		subject = "Re: " + subject
	}
	c.inputs[cTo].SetValue(it.h.From)
	c.inputs[cSubject].SetValue(subject)
	if m.reader != nil && it.h.S3Key != "" {
		s3Key := it.h.S3Key
		r := m.reader
		self := m.from
		orig := c
		return m, func() tea.Msg {
			rc, err := r.Open(context.Background(), s3Key)
			if err != nil {
				return replyReadyMsg{c: orig}
			}
			defer rc.Close()
			parsed, err := message.Parse(rc)
			if err != nil {
				return replyReadyMsg{c: orig}
			}
			orig.inputs[cTo].SetValue(parsed.From)
			orig.inputs[cCc].SetValue(replyAllCc(parsed.To, parsed.Cc, self))
			_, _, subj := message.ReplyHeaders(parsed)
			orig.inputs[cSubject].SetValue(subj)
			return replyReadyMsg{c: orig}
		}
	}
	m.compose = c
	m.mode = modeComposer
	return m, nil
}
```

> Nota import: el paquete de imágenes se importa como `render "erickaldama-mail/internal/render"` (igual que v0.3). No hay colisión de nombre aquí (la closure se llama `renderImgs`, no `render`).

- [ ] **Step 7: Actualizar `main.go` — NewProgram + WithMouseCellMotion + list/spinner**

```go
sp := spinner.New(spinner.WithSpinner(spinner.Dot))
m := model{
	mode:    modeBrowse,
	focus:   focusList,
	list:    newMessageList(headers, 30, 20), // dims provisionales; el primer WindowSizeMsg las corrige
	spinner: sp,
	from:    from,
	reader:  rdr,
	sender:  snd,
	compose: newComposer(),
}
p := tea.NewProgram(m, tea.WithAltScreen(), tea.WithMouseCellMotion())
```
(Ajustar `headers`/`from`/`rdr`/`snd` a los nombres reales — verificar con `grep -n "model{" main.go`. `vp.MouseWheelEnabled` no se setea: el default de viewport.New ya lo trae true en v1.0.0 — verificar con `go doc`; si no, setear `m.viewport.MouseWheelEnabled = true` tras el primer resize.)

- [ ] **Step 8: Run TODA la suite del TUI + build (un solo árbol verde)**

Run: `cd cmd/mail-tui && go build ./... && go test -count=1 ./...`
Expected: build OK; los 14 tests del spec + TestDebounceLoadsOnce + los 3 de composer v0.2 migrados, todos PASS. **El árbol queda verde en este único commit** (cierra A-T4-panic).

- [ ] **Step 9: Commit (atómico, bisectable)**

```bash
git -C "$WT" add cmd/mail-tui/model.go cmd/mail-tui/main.go cmd/mail-tui/layout.go cmd/mail-tui/model_test.go
git -C "$WT" commit -m "feat(v0.4): atomic model refactor — 2-pane Update/View, debounced async, SAN preserved

Struct+Init+Update+View+resize+reader async en un solo commit verde (A-T4-panic:
el refactor del model es atómico, partirlo deja go test build-failed). Cierra:
D1/D2 (2 paneles+focus), D6 (loadGen/inflight, NO bool), D7+A-1b (MouseMsg al
viewport), A-4 (descarte stale por gen), B-1+debounce (trigger por S3Key + tea.Tick
150ms, NO Index, NO N+1 S3), B-2 (Tab durante filtro), B-3 (selectedKey comma-ok),
C1/C2 (layout fits), C4/C5 (AdaptiveColor+footer), B-7 (reply via S3Key), SAN
(SetContent siempre sanitizedBody). Fix A-puntero: value-in/value-out, sin
puntero-sobre-copia. 14 tests del spec + debounce + 3 composer migrados."
```

---

### Task 5: shortDate robusto (fix A-shortDate) + go mod tidy + CHANGELOG

**Files:**
- Modify: `cmd/mail-tui/listitem.go` (`shortDate`)
- Modify: `CHANGELOG.md`
- Modify: `go.mod`/`go.sum` (tidy)
- Test: `cmd/mail-tui/listitem_test.go`

**Interfaces:**
- `shortDate(d string) string` robusto multi-layout con fallback al crudo.

- [ ] **Step 1: Write the failing test** (`listitem_test.go`, añadir)

```go
func TestShortDateRobust(t *testing.T) {
	cases := map[string]string{
		"Mon, 23 Jun 2026 10:00:00 +0000": "23 Jun 2026", // RFC1123Z
		"23 Jun 2026 10:00:00 +0000":      "23 Jun 2026", // RFC822-ish sin día de semana
		"2026-06-23T10:00:00Z":            "23 Jun 2026", // ISO8601 (algunos ingest)
	}
	for in, want := range cases {
		if got := shortDate(in); got != want {
			t.Errorf("shortDate(%q)=%q want %q", in, got, want)
		}
	}
	// Unparseable → return the raw string (no garbage slice).
	if got := shortDate("garbage"); got != "garbage" {
		t.Errorf("shortDate(garbage)=%q want raw passthrough", got)
	}
}
```

- [ ] **Step 2: Run to verify fail**

Run: `cd cmd/mail-tui && go test -run TestShortDate -v`
Expected: FAIL — el `d[5:16]` actual produce basura.

- [ ] **Step 3: Reescribir `shortDate` con time.Parse multi-layout**

```go
import "time"

// shortDate parses common email Date header formats and returns "DD Mon YYYY".
// The Date comes from env.GetHeader("Date") unnormalized, so try several layouts;
// fall back to the raw string if none parse (audit A-shortDate — no fixed-index slice).
func shortDate(d string) string {
	d = strings.TrimSpace(d)
	for _, layout := range []string{time.RFC1123Z, time.RFC1123, time.RFC822Z, time.RFC822, "2 Jan 2006 15:04:05 -0700", time.RFC3339} {
		if t, err := time.Parse(layout, d); err == nil {
			return t.Format("2 Jan 2006")
		}
	}
	return d
}
```

- [ ] **Step 4: Run to verify pass**

Run: `cd cmd/mail-tui && go test -run TestShortDate -count=1 -v`
Expected: PASS.

- [ ] **Step 5: go mod tidy (higiene tras 3 subpaquetes nuevos) + suite completa**

Run: `cd "$WT" && go mod tidy && go build ./... && go vet ./... && go test ./... -count=1`
Expected: go.mod/go.sum consistentes; 15+ paquetes verde.

- [ ] **Step 6: Verificar el invariante SAN con grep**

Run: `cd cmd/mail-tui && grep -nE "SetContent\(" model.go`
Expected: cada `SetContent` recibe `body`/`m.sanitizedBody`/`m.sanitizedBody + ...` — NUNCA `m.rawHTML` ni `.TextHTML`.

- [ ] **Step 7: CHANGELOG + commit**

Añadir entrada `## [mail v0.4] — TUI estética (2 paneles) — 2026-06-30` a CHANGELOG.md narrando: layout 2-paneles con focus alternable (Tab), viewport pager con scroll real, list con filtro fuzzy, spinner async; y los hallazgos de las DOS auditorías (spec: A-4 race, B-1 trigger, C1/C2 layout; plan: A-T4-panic refactor atómico, C-debounce N+1) como evidencia de portafolio.

```bash
git -C "$WT" add cmd/mail-tui/listitem.go cmd/mail-tui/listitem_test.go CHANGELOG.md go.mod go.sum
git -C "$WT" commit -m "fix(v0.4): robust shortDate (multi-layout), go mod tidy, CHANGELOG (A-shortDate)"
```

---

## Smoke manual (Fase 5, tras el whole-branch review) — el NO VERIFICADO de UX en vivo

Arrancar `mail-tui` en iTerm+tmux y verificar a mano (las firmas son verdad de compilación; el render en TTY no se observó): (1) 2 paneles sin recorte; (2) Tab alterna foco visiblemente; (3) j/k scroll en reader + `[%]` se mueve; (4) `/` filtra; (5) navegar la lista con j/k NO satura (debounce — el body carga al detenerse); (6) Enter/selección abre el correo; (7) `i` muestra imágenes con chafa; (8) colores de foco distinguibles; (9) scroll de rueda del mouse funciona.

## Deuda diferida explícita (documentada, no silenciosa)
- **C-render (MENOR):** `RenderRich` (~1.78 ms/call) se re-ejecuta en cada `WindowSizeMsg` durante un drag de borde. Aceptable para v0.4 (paridad con v0.3). Fix barato futuro: cachear el `glamour.TermRenderer` por width. Anotado para v0.4.1 si el smoke lo revela molesto.
