# mail v0.5 — Cache SQLite + FTS5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un cache local SQLite+FTS5 descartable que acelera `mail ls`, habilita búsqueda full-text (CLI `mail search` + filtro `/` de la TUI), y muestra fecha+hora y el s3Key en el listado.

**Architecture:** Paquete nuevo `internal/cache` = decorador del `mailbox.Reader`. SQLite puro-Go (`modernc.org/sqlite`, CGO-free). FTS5 **contentless** (sin triggers) — el sync del índice lo hace Go explícitamente en el mismo tx que el upsert, eliminando el firing NO VERIFICADO de triggers en upsert. Los call-sites (`mail ls`, `mail search`, TUI) leen del cache con fallback transparente al Reader directo.

**Tech Stack:** Go 1.26.4, `database/sql` + `modernc.org/sqlite`, SQLite 3.53.2 (FTS5), `charmbracelet/bubbles` v1.0.0 (list/lipgloss), `spf13/cobra`.

## Global Constraints

- **Driver:** `sql.Open("sqlite", dsn)` con blank import `_ "modernc.org/sqlite"` — name `"sqlite"`, NO `"sqlite3"`.
- **FTS5:** out-of-the-box, sin build tag (compilado por defecto en modernc.org/sqlite).
- **Contentless FTS5, sin triggers** — sync del índice explícito en Go (research crítico #1). **El borrado usa el comando especial `INSERT INTO headers_fts(headers_fts, rowid, sender, subject) VALUES('delete', ...)` con los valores VIEJOS actualmente indexados — NUNCA `DELETE FROM headers_fts` (ilegal en contentless: "cannot DELETE from contentless fts5 table", verificado empíricamente).**
- **Esquema:** `headers` con `rowid INTEGER PRIMARY KEY` + `s3Key TEXT NOT NULL UNIQUE` (research crítico #2: PK TEXT no es rowid-alias; UNIQUE requerido por ON CONFLICT).
- **Permisos:** dir `0700`, archivo `.sqlite` `0600` desde Go (SQLite no controla permisos).
- **PRAGMAs por conexión (cada Open):** `journal_mode=WAL`, `synchronous=NORMAL`, `busy_timeout=5000`; `db.SetMaxOpenConns(1)`.
- **Cache descartable:** nunca es fuente de verdad (DynamoDB lo es); fallback transparente si falla.
- **Población ≠ presentación (audit M-2):** el Sync SIEMPRE puebla con un tope FIJO `const SyncPageLimit = 500` (exportado, usado como `cache.SyncPageLimit`), independiente de `--count`. `--count`/`--limit` solo limitan cuántas filas se MUESTRAN (List/Search LIMIT). Nunca pasar `count` a Sync — o `mail search` solo buscaría en las últimas `count` filas.
- **Limitación conocida — huérfanos (audit M-3):** el Sync hace upsert; NO detecta correos borrados en DynamoDB (quedan en el cache). Aceptable (cache descartable), pero DEBE documentarse en el runbook + CHANGELOG: `rm ~/.cache/erickaldama-mail/index.sqlite` para reconciliar.
- **`sahilm/fuzzy` NO es podable (audit M-4):** es dep transitiva de `bubbles/list` (el prompt `/` la usa). El filtro se intercepta, no se elimina; `SetFilteringEnabled` se mantiene para la UI del prompt.
- **Fecha:** `formatDate` → `"2006-01-02 15:04"` (hora local, 24h), tolerante multi-layout, cae al string crudo si no parsea.
- **escapeFTS** compartida CLI+TUI (vive en `internal/cache`), NO duplicar.
- **Preservar invariantes TUI v0.4:** B-1 (selección por S3Key, no Index), B-2 (Tab solo sin filtro), D3 (teclas a la lista al filtrar), C-debounce (150ms). Los tests de v0.4 deben seguir verdes.
- **NDA:** repo público — cero rutas/marcas de esagiosapp/YAAN en código, tests, fixtures o mensajes de commit.
- **Modern Go:** `any` no `interface{}`; errores con `%w`; `slices`/`cmp` cuando aplique.

---

### Task 1: Añadir la dependencia modernc.org/sqlite

**Files:**
- Modify: `go.mod`, `go.sum`

**Interfaces:**
- Produces: la dep `modernc.org/sqlite` disponible para el resto de tareas.

- [ ] **Step 1: Añadir la dep a la versión viva (SIN tidy todavía)**

Run:
```bash
cd .claude/worktrees/mail-v0.5-cache
go get modernc.org/sqlite@latest
```

**NO correr `go mod tidy` aquí (audit MENOR-1):** sin ningún import del paquete todavía, `go mod tidy`
lo PODARÍA del go.mod (verificado: `go: module modernc.org/sqlite: not a known dependency`). El `tidy`
se difiere hasta Task 2 (cuando `cache.go` añade `_ "modernc.org/sqlite"`) y un `tidy` de cierre en Task 12.

- [ ] **Step 2: Registrar la versión resuelta**

Run: `go list -m modernc.org/sqlite`
Expected: imprime `modernc.org/sqlite v1.53.0` (verificado hoy; v1.54.0 aún no indexada — ambas SQLite 3.53.2). Anotar la versión resuelta para el runbook.

- [ ] **Step 3: Medir las deps indirectas añadidas**

Run: `go mod graph | grep -c modernc` (nota informativa para el runbook — el auditor midió **45** deps totales; nuevas indirectas: `modernc.org/libc`, `modernc.org/mathutil`, `modernc.org/memory`, `github.com/ncruces/go-strftime`, `github.com/dustin/go-humanize`, `github.com/google/uuid`, `github.com/remyoudompheng/bigfft`).

- [ ] **Step 4: NO commitear aún**

`go get` deja `modernc.org/sqlite` en go.mod pero, sin un import, un `go mod tidy` (Task 2) lo movería/podaría.
**El commit de las deps ocurre al final de Task 2**, tras crear `cache.go` (que añade el import) y correr `go mod tidy`
— así go.mod/go.sum quedan consistentes. Task 1 solo deja la dep descargada + la versión registrada; no commitea.
El orden de dependencia efectivo es **1→2 acopladas** (Task 1 es un prefacio de Task 2, no un commit independiente).

---

### Task 2: Paquete cache — Open/Close, schema, PRAGMAs, permisos 0600

**Files:**
- Create: `internal/cache/cache.go`
- Create: `internal/cache/schema.go`
- Test: `internal/cache/cache_test.go`

**Interfaces:**
- Consumes: `modernc.org/sqlite` (Task 1).
- Produces:
  - `type Cache struct { db *sql.DB }`
  - `func Open(path string) (*Cache, error)` — crea dir 0700, archivo 0600, aplica PRAGMAs, init schema idempotente.
  - `func (c *Cache) Close() error`
  - `func DefaultPath() (string, error)` — `$XDG_CACHE_HOME/erickaldama-mail/index.sqlite` o `~/.cache/...`.
  - schema: tabla `headers(rowid INTEGER PRIMARY KEY, s3Key TEXT NOT NULL UNIQUE, pk, sk, messageId, sender, subject, date TEXT)`, índice `idx_headers_mailbox(pk, sk DESC)`, `headers_fts USING fts5(sender, subject, content='')`, `sync_state(mailbox TEXT PRIMARY KEY, last_sk, synced_at TEXT)`.

- [ ] **Step 1: Write the failing test**

```go
package cache

import (
	"os"
	"path/filepath"
	"testing"
)

func TestOpenCreatesFileWith0600(t *testing.T) {
	dir := t.TempDir()
	path := filepath.Join(dir, "sub", "index.sqlite")
	c, err := Open(path)
	if err != nil {
		t.Fatalf("Open: %v", err)
	}
	defer c.Close()

	fi, err := os.Stat(path)
	if err != nil {
		t.Fatalf("stat: %v", err)
	}
	if perm := fi.Mode().Perm(); perm != 0o600 {
		t.Errorf("file perm = %o, want 600", perm)
	}
	di, err := os.Stat(filepath.Dir(path))
	if err != nil {
		t.Fatalf("stat dir: %v", err)
	}
	if perm := di.Mode().Perm(); perm != 0o700 {
		t.Errorf("dir perm = %o, want 700", perm)
	}
}

func TestOpenIsIdempotent(t *testing.T) {
	path := filepath.Join(t.TempDir(), "index.sqlite")
	c1, err := Open(path)
	if err != nil {
		t.Fatalf("Open 1: %v", err)
	}
	c1.Close()
	c2, err := Open(path) // re-open must not error on CREATE ... IF NOT EXISTS
	if err != nil {
		t.Fatalf("Open 2: %v", err)
	}
	c2.Close()
}

func TestFTS5ModuleCompiled(t *testing.T) {
	// Contract test: fails if the FTS5 module is NOT compiled into the driver.
	// Runs on CI (linux/amd64) and local (darwin/arm64) — closes research NO VERIFICADO #6.
	path := filepath.Join(t.TempDir(), "index.sqlite")
	c, err := Open(path)
	if err != nil {
		t.Fatalf("Open: %v", err)
	}
	defer c.Close()
	// Open already runs the schema init which includes a fts5 virtual table.
	// If FTS5 were missing, Open would have failed with "no such module: fts5".
	var name string
	err = c.db.QueryRow(`SELECT name FROM sqlite_master WHERE type='table' AND name='headers_fts'`).Scan(&name)
	if err != nil {
		t.Fatalf("headers_fts not created (FTS5 missing?): %v", err)
	}
	if name != "headers_fts" {
		t.Errorf("got %q, want headers_fts", name)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/cache/ -run TestOpen -v`
Expected: FAIL (package/function not defined).

- [ ] **Step 3: Write schema.go**

```go
package cache

// schemaSQL is idempotent: safe to run on every Open. Contentless FTS5 (content='')
// means the index stores no column copy; Go syncs it explicitly (no triggers).
const schemaSQL = `
CREATE TABLE IF NOT EXISTS headers (
  rowid     INTEGER PRIMARY KEY,
  s3Key     TEXT NOT NULL UNIQUE,
  pk        TEXT NOT NULL,
  sk        TEXT NOT NULL,
  messageId TEXT,
  sender    TEXT,
  subject   TEXT,
  date      TEXT
);
CREATE INDEX IF NOT EXISTS idx_headers_mailbox ON headers(pk, sk DESC);
CREATE VIRTUAL TABLE IF NOT EXISTS headers_fts USING fts5(sender, subject, content='');
CREATE TABLE IF NOT EXISTS sync_state (
  mailbox   TEXT PRIMARY KEY,
  last_sk   TEXT,
  synced_at TEXT
);
`
```

- [ ] **Step 4: Write cache.go**

```go
// Package cache is a discardable local SQLite+FTS5 cache of mail headers. It decorates
// mailbox.Reader: DynamoDB stays the source of truth; the cache accelerates ls and enables
// full-text search. FTS5 is contentless — the index is synced explicitly by Go (no triggers).
package cache

import (
	"database/sql"
	"fmt"
	"os"
	"path/filepath"

	_ "modernc.org/sqlite" // registers the "sqlite" driver (NOT "sqlite3")
)

// Cache wraps a single-connection SQLite handle over the header index.
type Cache struct{ db *sql.DB }

// DefaultPath returns $XDG_CACHE_HOME/erickaldama-mail/index.sqlite, or ~/.cache/... as fallback.
func DefaultPath() (string, error) {
	base := os.Getenv("XDG_CACHE_HOME")
	if base == "" {
		home, err := os.UserHomeDir()
		if err != nil {
			return "", fmt.Errorf("cache path: %w", err)
		}
		base = filepath.Join(home, ".cache")
	}
	return filepath.Join(base, "erickaldama-mail", "index.sqlite"), nil
}

// Open opens (creating if needed) the cache at path. It ensures the directory is 0700 and the
// database file is 0600 (SQLite does not control file permissions), applies per-connection
// PRAGMAs, and runs the idempotent schema. The cache is discardable: callers should fall back
// to the live Reader if Open fails.
func Open(path string) (*Cache, error) {
	dir := filepath.Dir(path)
	if err := os.MkdirAll(dir, 0o700); err != nil {
		return nil, fmt.Errorf("cache mkdir: %w", err)
	}
	// Create the file with 0600 BEFORE SQLite opens it (SQLite would otherwise use the umask).
	f, err := os.OpenFile(path, os.O_CREATE, 0o600)
	if err != nil {
		return nil, fmt.Errorf("cache create: %w", err)
	}
	f.Close()
	// Belt-and-suspenders: enforce 0600 even if the file pre-existed with looser perms.
	if err := os.Chmod(path, 0o600); err != nil {
		return nil, fmt.Errorf("cache chmod: %w", err)
	}

	db, err := sql.Open("sqlite", path)
	if err != nil {
		return nil, fmt.Errorf("cache open: %w", err)
	}
	db.SetMaxOpenConns(1) // single-process cache; serialize access, avoid SQLITE_BUSY churn

	for _, pragma := range []string{
		"PRAGMA journal_mode=WAL",   // persistent, set once
		"PRAGMA synchronous=NORMAL", // per-connection; durability loss OK (cache is discardable)
		"PRAGMA busy_timeout=5000",  // per-connection; sleep-retry up to 5s on lock
	} {
		if _, err := db.Exec(pragma); err != nil {
			db.Close()
			return nil, fmt.Errorf("cache pragma %q: %w", pragma, err)
		}
	}
	if _, err := db.Exec(schemaSQL); err != nil {
		db.Close()
		return nil, fmt.Errorf("cache schema: %w", err)
	}
	// WAL creates sidecar -wal/-shm files with the process umask (SQLite doesn't control perms).
	// They hold un-checkpointed header pages → tighten to 0600 too (audit m-4). Best-effort: ignore
	// ENOENT (may not exist yet before the first write).
	for _, suffix := range []string{"-wal", "-shm"} {
		if err := os.Chmod(path+suffix, 0o600); err != nil && !os.IsNotExist(err) {
			db.Close()
			return nil, fmt.Errorf("cache chmod %s: %w", suffix, err)
		}
	}
	return &Cache{db: db}, nil
}

// Close closes the underlying database handle.
func (c *Cache) Close() error { return c.db.Close() }
```

- [ ] **Step 5: Tidy las deps (ahora que hay un import de modernc.org/sqlite)**

Run: `go mod tidy && go build ./...`
Expected: exit 0. `go.mod` ahora lista `modernc.org/sqlite` como dep directa + las 7 indirectas (Task 1 Step 3).

- [ ] **Step 6: Run tests to verify they pass**

Run: `go test ./internal/cache/ -v`
Expected: PASS (TestOpenCreatesFileWith0600, TestOpenIsIdempotent, TestFTS5ModuleCompiled).

- [ ] **Step 7: Commit (incluye go.mod/go.sum — el import los hace consistentes)**

```bash
git -C "$(pwd)" add internal/cache/ go.mod go.sum
git -C "$(pwd)" commit -m "feat(v0.5): cache Open/Close + schema + 0600 (+sidecar) + FTS5 contract test + modernc.org/sqlite dep"
```

---

### Task 3: escapeFTS — sanitizar input de usuario para MATCH

**Files:**
- Create: `internal/cache/escape.go`
- Test: `internal/cache/escape_test.go`

**Interfaces:**
- Produces: `func escapeFTS(q string) string` — envuelve en comillas dobles y duplica `"` internas. Neutraliza AND/OR/NOT/NEAR y caracteres especiales. Exportada como `EscapeFTS` para que la TUI la reuse.

- [ ] **Step 1: Write the failing test**

```go
package cache

import "testing"

func TestEscapeFTS(t *testing.T) {
	cases := []struct{ in, want string }{
		{"hello", `"hello"`},
		{"amazon web services", `"amazon web services"`},
		{`say "hi"`, `"say ""hi"""`},        // embedded quotes doubled
		{"foo AND bar", `"foo AND bar"`},    // AND neutralized (inside quotes = literal)
		{"", `""`},                          // empty → empty phrase
		{"NEAR(a b)", `"NEAR(a b)"`},        // NEAR neutralized
	}
	for _, tc := range cases {
		if got := EscapeFTS(tc.in); got != tc.want {
			t.Errorf("EscapeFTS(%q) = %q, want %q", tc.in, got, tc.want)
		}
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/cache/ -run TestEscapeFTS -v`
Expected: FAIL (EscapeFTS not defined).

- [ ] **Step 3: Write escape.go**

```go
package cache

import "strings"

// EscapeFTS makes an arbitrary user string safe to pass as an FTS5 MATCH argument by wrapping it
// in double quotes and doubling any embedded double quotes (SQL-style, per sqlite.org/fts5.html §3.1).
// This neutralizes the boolean operators AND/OR/NOT and NEAR (they become literal tokens inside the
// quoted phrase) and protects special characters. Trade-off: prefix search ('*') is disabled.
func EscapeFTS(q string) string {
	return `"` + strings.ReplaceAll(q, `"`, `""`) + `"`
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/cache/ -run TestEscapeFTS -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git -C "$(pwd)" add internal/cache/escape.go internal/cache/escape_test.go
git -C "$(pwd)" commit -m "feat(v0.5): EscapeFTS — quote-wrap user query, neutralize operators"
```

---

### Task 4: Sync — DynamoDB page → SQLite (upsert + FTS explícito)

**Files:**
- Create: `internal/cache/sync.go`
- Test: `internal/cache/sync_test.go`

**Interfaces:**
- Consumes: `Cache` (Task 2), `mailbox.Header`, `mailbox.Reader.List`.
- Produces:
  - `type HeaderLister interface { List(ctx context.Context, mailbox string, limit int32, start map[string]ddbtypes.AttributeValue) ([]mailbox.Header, map[string]ddbtypes.AttributeValue, error) }` — *mailbox.Reader lo satisface; permite un fake en test.
  - `func (c *Cache) Sync(ctx context.Context, r HeaderLister, mailbox string, limit int32) (int, error)` — trae una página, upsert por s3Key, sync FTS explícito, actualiza sync_state. Devuelve #filas.

- [ ] **Step 1: Write the failing test**

```go
package cache

import (
	"context"
	"path/filepath"
	"testing"

	ddbtypes "github.com/aws/aws-sdk-go-v2/service/dynamodb/types"
	"erickaldama-mail/internal/mailbox"
)

// fakeLister returns a fixed page, ignoring the cursor (v0.5 fetches the full page).
type fakeLister struct{ hs []mailbox.Header }

func (f fakeLister) List(_ context.Context, _ string, _ int32, _ map[string]ddbtypes.AttributeValue) ([]mailbox.Header, map[string]ddbtypes.AttributeValue, error) {
	return f.hs, nil, nil
}

func sampleHeaders() []mailbox.Header {
	return []mailbox.Header{
		{PK: "mailbox#inbox", SK: "2026-06-25T14:32:00Z#a", S3Key: "inbound/aaa-000000", MessageID: "m1", From: "alice@example.com", Subject: "Hello", Date: "Wed, 25 Jun 2026 14:32:00 +0000"},
		{PK: "mailbox#inbox", SK: "2026-06-25T09:00:00Z#b", S3Key: "inbound/bbb-000000", MessageID: "m2", From: "bob@example.com", Subject: "Report", Date: "Wed, 25 Jun 2026 09:00:00 +0000"},
	}
}

func TestSyncPopulatesAndIsIdempotent(t *testing.T) {
	path := filepath.Join(t.TempDir(), "index.sqlite")
	c, err := Open(path)
	if err != nil {
		t.Fatalf("Open: %v", err)
	}
	defer c.Close()
	lister := fakeLister{hs: sampleHeaders()}

	n, err := c.Sync(context.Background(), lister, "inbox", 50)
	if err != nil {
		t.Fatalf("Sync: %v", err)
	}
	if n != 2 {
		t.Fatalf("first Sync rows = %d, want 2", n)
	}
	// Second sync of the SAME page must not duplicate.
	if _, err := c.Sync(context.Background(), lister, "inbox", 50); err != nil {
		t.Fatalf("Sync 2: %v", err)
	}
	var count int
	if err := c.db.QueryRow(`SELECT COUNT(*) FROM headers`).Scan(&count); err != nil {
		t.Fatalf("count: %v", err)
	}
	if count != 2 {
		t.Errorf("headers count = %d, want 2 (idempotent)", count)
	}
	// FTS index must have the same number of rows (explicit sync, no triggers).
	var ftsCount int
	if err := c.db.QueryRow(`SELECT COUNT(*) FROM headers_fts`).Scan(&ftsCount); err != nil {
		t.Fatalf("fts count: %v", err)
	}
	if ftsCount != 2 {
		t.Errorf("headers_fts count = %d, want 2", ftsCount)
	}
}

// TestSyncMutationDoesNotLeaveStaleFTS is the test that catches the contentless-DELETE bug: it
// re-syncs the SAME s3Key with a CHANGED subject. If the FTS index still matched the OLD subject,
// the 'delete'-with-old-values sync path is broken. Also runs the FTS5 integrity-check.
func TestSyncMutationDoesNotLeaveStaleFTS(t *testing.T) {
	path := filepath.Join(t.TempDir(), "index.sqlite")
	c, err := Open(path)
	if err != nil {
		t.Fatalf("Open: %v", err)
	}
	defer c.Close()

	h := mailbox.Header{PK: "mailbox#inbox", SK: "2026-06-25T14:32:00Z#a", S3Key: "inbound/aaa-000000", From: "alice@example.com", Subject: "OldSubject"}
	if _, err := c.Sync(context.Background(), fakeLister{hs: []mailbox.Header{h}}, "inbox", 50); err != nil {
		t.Fatalf("Sync 1: %v", err)
	}
	// Re-sync same s3Key with a new subject — must NOT error (contentless DELETE would) and must
	// replace the FTS term, not accumulate it.
	h.Subject = "NewSubject"
	if _, err := c.Sync(context.Background(), fakeLister{hs: []mailbox.Header{h}}, "inbox", 50); err != nil {
		t.Fatalf("Sync 2 (mutation): %v", err) // this is where `DELETE FROM contentless` blows up
	}
	old, err := c.Search("inbox", "OldSubject", 10)
	if err != nil {
		t.Fatalf("Search old: %v", err)
	}
	if len(old) != 0 {
		t.Errorf("stale FTS term: OldSubject still matches %d rows, want 0", len(old))
	}
	got, err := c.Search("inbox", "NewSubject", 10)
	if err != nil {
		t.Fatalf("Search new: %v", err)
	}
	if len(got) != 1 {
		t.Errorf("NewSubject matches %d rows, want 1", len(got))
	}
	// FTS5 integrity-check must pass (no orphaned/duplicate index entries).
	if _, err := c.db.Exec(`INSERT INTO headers_fts(headers_fts) VALUES('integrity-check')`); err != nil {
		t.Errorf("FTS5 integrity-check failed: %v", err)
	}
}
```

> **Nota:** `TestSyncMutationDoesNotLeaveStaleFTS` usa `c.Search` (Task 5). Si se implementa Task 4 antes que Task 5, escribir la aserción con SQL directo (`SELECT COUNT(*) FROM headers_fts WHERE headers_fts MATCH 'OldSubject'`) y migrarla a `Search` cuando Task 5 exista. El punto no negociable: **debe existir un test de MUTACIÓN**, no solo de replay idéntico.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/cache/ -run TestSync -v`
Expected: FAIL (Sync not defined).

- [ ] **Step 3: Write sync.go**

```go
package cache

import (
	"context"
	"database/sql"
	"fmt"
	"time"

	ddbtypes "github.com/aws/aws-sdk-go-v2/service/dynamodb/types"
	"erickaldama-mail/internal/mailbox"
)

// SyncPageLimit is the FIXED number of rows Sync pulls from DynamoDB to populate the cache. It is
// deliberately independent of the CLI --count flag (audit M-2): search/list must see the full local
// history, not just the last --count messages.
const SyncPageLimit = 500

// HeaderLister is the subset of *mailbox.Reader that Sync needs (enables a fake in tests).
type HeaderLister interface {
	List(ctx context.Context, mailbox string, limit int32, start map[string]ddbtypes.AttributeValue) ([]mailbox.Header, map[string]ddbtypes.AttributeValue, error)
}

// Sync fetches one page of headers for the mailbox from the lister (DynamoDB) and upserts them into
// the cache within a single transaction. The contentless FTS5 index is kept in sync EXPLICITLY —
// no triggers, so the upsert's trigger-firing (NO VERIFICADO by the SQLite docs) is irrelevant.
//
// CRITICAL (verified empirically): contentless FTS5 tables reject `DELETE FROM ... WHERE rowid`
// ("cannot DELETE from contentless fts5 table"). To replace a row's index entry we MUST use the
// special 'delete' command, and it requires the OLD values currently indexed (sqlite.org/fts5.html
// §6.3: "The values inserted into the other columns must match the values currently stored"). So we
// read the prior sender/subject BEFORE the upsert, issue the 'delete' with those OLD values, then
// insert the NEW values. Order matters: read old → 'delete' old → upsert → insert new.
func (c *Cache) Sync(ctx context.Context, r HeaderLister, mailbox string, limit int32) (int, error) {
	hs, _, err := r.List(ctx, mailbox, limit, nil)
	if err != nil {
		return 0, fmt.Errorf("cache sync list: %w", err)
	}
	tx, err := c.db.BeginTx(ctx, nil)
	if err != nil {
		return 0, fmt.Errorf("cache sync begin: %w", err)
	}
	defer tx.Rollback() // no-op after Commit

	var maxSK string
	for _, h := range hs {
		// 1. Read the OLD indexed values (if this s3Key already exists) so we can remove its stale
		//    FTS entry with the exact values FTS5 has stored (contentless 'delete' requires this).
		var oldRowid sql.NullInt64
		var oldSender, oldSubject sql.NullString
		_ = tx.QueryRowContext(ctx, `SELECT rowid, sender, subject FROM headers WHERE s3Key=?`, h.S3Key).
			Scan(&oldRowid, &oldSender, &oldSubject) // ErrNoRows → oldRowid.Valid=false (new row)

		// 2. Remove the stale FTS entry via the special 'delete' command (contentless-safe).
		if oldRowid.Valid {
			if _, err := tx.ExecContext(ctx,
				`INSERT INTO headers_fts(headers_fts, rowid, sender, subject) VALUES('delete', ?, ?, ?)`,
				oldRowid.Int64, oldSender.String, oldSubject.String); err != nil {
				return 0, fmt.Errorf("cache sync fts delete: %w", err)
			}
		}

		// 3. Upsert the header row, capturing the (stable) rowid.
		var rowid int64
		err := tx.QueryRowContext(ctx, `
			INSERT INTO headers (s3Key, pk, sk, messageId, sender, subject, date)
			VALUES (?, ?, ?, ?, ?, ?, ?)
			ON CONFLICT(s3Key) DO UPDATE SET
				pk=excluded.pk, sk=excluded.sk, messageId=excluded.messageId,
				sender=excluded.sender, subject=excluded.subject, date=excluded.date
			RETURNING rowid`,
			h.S3Key, h.PK, h.SK, h.MessageID, h.From, h.Subject, h.Date,
		).Scan(&rowid)
		if err != nil {
			return 0, fmt.Errorf("cache sync upsert %s: %w", h.S3Key, err)
		}

		// 4. Insert the current FTS entry.
		if _, err := tx.ExecContext(ctx, `INSERT INTO headers_fts(rowid, sender, subject) VALUES (?, ?, ?)`, rowid, h.From, h.Subject); err != nil {
			return 0, fmt.Errorf("cache sync fts insert: %w", err)
		}
		if h.SK > maxSK {
			maxSK = h.SK
		}
	}
	if _, err := tx.ExecContext(ctx, `
		INSERT INTO sync_state (mailbox, last_sk, synced_at) VALUES (?, ?, ?)
		ON CONFLICT(mailbox) DO UPDATE SET last_sk=excluded.last_sk, synced_at=excluded.synced_at`,
		mailbox, maxSK, time.Now().UTC().Format(time.RFC3339),
	); err != nil {
		return 0, fmt.Errorf("cache sync state: %w", err)
	}
	if err := tx.Commit(); err != nil {
		return 0, fmt.Errorf("cache sync commit: %w", err)
	}
	return len(hs), nil
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/cache/ -run TestSync -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git -C "$(pwd)" add internal/cache/sync.go internal/cache/sync_test.go
git -C "$(pwd)" commit -m "feat(v0.5): cache Sync — upsert by s3Key + explicit contentless FTS sync"
```

---

### Task 5: List + Search — leer del cache

**Files:**
- Create: `internal/cache/query.go`
- Test: `internal/cache/query_test.go`

**Interfaces:**
- Consumes: `Cache`, `Sync` (Task 4), `EscapeFTS` (Task 3).
- Produces:
  - `func (c *Cache) List(mailbox string, limit int) ([]mailbox.Header, error)` — newest-first (sk DESC).
  - `func (c *Cache) Search(mailbox, query string, limit int) ([]mailbox.Header, error)` — FTS5 MATCH, join por rowid, ORDER BY rank.

- [ ] **Step 1: Write the failing test**

```go
package cache

import (
	"context"
	"path/filepath"
	"testing"
)

func seededCache(t *testing.T) *Cache {
	t.Helper()
	c, err := Open(filepath.Join(t.TempDir(), "index.sqlite"))
	if err != nil {
		t.Fatalf("Open: %v", err)
	}
	if _, err := c.Sync(context.Background(), fakeLister{hs: sampleHeaders()}, "inbox", 50); err != nil {
		t.Fatalf("Sync: %v", err)
	}
	return c
}

func TestListNewestFirst(t *testing.T) {
	c := seededCache(t)
	defer c.Close()
	got, err := c.List("inbox", 50)
	if err != nil {
		t.Fatalf("List: %v", err)
	}
	if len(got) != 2 {
		t.Fatalf("len = %d, want 2", len(got))
	}
	// sample: SK "...14:32...#a" > "...09:00...#b" → 'a' (Hello) first.
	if got[0].Subject != "Hello" {
		t.Errorf("first subject = %q, want Hello (newest-first)", got[0].Subject)
	}
	if got[0].S3Key != "inbound/aaa-000000" {
		t.Errorf("first s3Key = %q, want inbound/aaa-000000", got[0].S3Key)
	}
}

func TestSearchMatchesSubject(t *testing.T) {
	c := seededCache(t)
	defer c.Close()
	got, err := c.Search("inbox", "Report", 50)
	if err != nil {
		t.Fatalf("Search: %v", err)
	}
	if len(got) != 1 {
		t.Fatalf("len = %d, want 1", len(got))
	}
	if got[0].Subject != "Report" {
		t.Errorf("subject = %q, want Report", got[0].Subject)
	}
}

func TestSearchMatchesSender(t *testing.T) {
	c := seededCache(t)
	defer c.Close()
	got, err := c.Search("inbox", "alice", 50)
	if err != nil {
		t.Fatalf("Search: %v", err)
	}
	if len(got) != 1 || got[0].From != "alice@example.com" {
		t.Errorf("got %+v, want 1 result from alice", got)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/cache/ -run "TestList|TestSearch" -v`
Expected: FAIL (List/Search not defined).

- [ ] **Step 3: Write query.go**

```go
package cache

import (
	"fmt"

	"erickaldama-mail/internal/mailbox"
)

// scanHeaders reads a *sql.Rows of the header columns into []mailbox.Header.
func scanHeaders(rowsQuery func() (rows, error)) ([]mailbox.Header, error) {
	rs, err := rowsQuery()
	if err != nil {
		return nil, err
	}
	defer rs.Close()
	var out []mailbox.Header
	for rs.Next() {
		var h mailbox.Header
		if err := rs.Scan(&h.S3Key, &h.PK, &h.SK, &h.MessageID, &h.From, &h.Subject, &h.Date); err != nil {
			return nil, fmt.Errorf("cache scan: %w", err)
		}
		out = append(out, h)
	}
	return out, rs.Err()
}

// rows is the subset of *sql.Rows scanHeaders needs.
type rows interface {
	Next() bool
	Scan(dest ...any) error
	Close() error
	Err() error
}

const headerCols = `s3Key, pk, sk, messageId, sender, subject, date`

// List returns cached headers for the mailbox, newest first (sk DESC), capped at limit.
func (c *Cache) List(mailbox string, limit int) ([]mailbox.Header, error) {
	return scanHeaders(func() (rows, error) {
		return c.db.Query(
			`SELECT `+headerCols+` FROM headers WHERE pk=? ORDER BY sk DESC LIMIT ?`,
			"mailbox#"+mailbox, limit,
		)
	})
}

// Search returns cached headers for the mailbox whose sender/subject match the FTS5 query,
// ranked best-first. The query is escaped (EscapeFTS) to neutralize FTS operators.
func (c *Cache) Search(mailbox, query string, limit int) ([]mailbox.Header, error) {
	return scanHeaders(func() (rows, error) {
		return c.db.Query(
			`SELECT `+prefixCols("h")+`
			 FROM headers_fts f JOIN headers h ON h.rowid = f.rowid
			 WHERE f.headers_fts MATCH ? AND h.pk = ?
			 ORDER BY rank LIMIT ?`,
			EscapeFTS(query), "mailbox#"+mailbox, limit,
		)
	})
}

// prefixCols qualifies headerCols with a table alias for the join query.
func prefixCols(alias string) string {
	return alias + ".s3Key, " + alias + ".pk, " + alias + ".sk, " + alias + ".messageId, " +
		alias + ".sender, " + alias + ".subject, " + alias + ".date"
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/cache/ -v`
Expected: PASS (all cache tests, incluida la suite previa).

- [ ] **Step 5: Commit**

```bash
git -C "$(pwd)" add internal/cache/query.go internal/cache/query_test.go
git -C "$(pwd)" commit -m "feat(v0.5): cache List (newest-first) + Search (FTS5 MATCH, rank)"
```

---

### Task 6: formatDate — fecha+hora en el CLI y compartida

**Files:**
- Create: `internal/cache/format.go` (compartida CLI+TUI — vive donde ambos pueden importarla sin ciclos)
- Test: `internal/cache/format_test.go`

**Interfaces:**
- Produces: `func FormatDate(raw string) string` → `"2006-01-02 15:04"` (hora local), tolerante multi-layout, cae al raw si no parsea.

> **Nota de ubicación:** `FormatDate` va en `internal/cache` porque tanto `cmd/mail` como `cmd/mail-tui` ya importarán `internal/cache` en v0.5 (para List/Search). Evita un paquete util nuevo. Si el auditor prefiere `internal/message` o un `internal/fmtutil`, es aceptable — la decisión es "un solo hogar, sin duplicar".

- [ ] **Step 1: Write the failing test**

```go
package cache

import (
	"testing"
	"time"
)

func TestFormatDate(t *testing.T) {
	// RFC1123Z (typical email Date header)
	got := FormatDate("Wed, 25 Jun 2026 14:32:00 +0000")
	// Expected is local time — compute it the same way to avoid TZ-flaky tests.
	parsed, _ := time.Parse(time.RFC1123Z, "Wed, 25 Jun 2026 14:32:00 +0000")
	want := parsed.Local().Format("2006-01-02 15:04")
	if got != want {
		t.Errorf("FormatDate = %q, want %q", got, want)
	}
}

func TestFormatDateFallsBackToRaw(t *testing.T) {
	if got := FormatDate("not a date"); got != "not a date" {
		t.Errorf("FormatDate(unparseable) = %q, want raw echo", got)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/cache/ -run TestFormatDate -v`
Expected: FAIL (FormatDate not defined).

- [ ] **Step 3: Write format.go**

```go
package cache

import (
	"strings"
	"time"
)

// FormatDate parses common email Date header formats and returns "2006-01-02 15:04" in LOCAL time
// (24h, ordenable). Falls back to the raw trimmed string if none of the layouts parse — same
// tolerance strategy as v0.4's shortDate, but with time-of-day so same-day mail is distinguishable.
func FormatDate(raw string) string {
	d := strings.TrimSpace(raw)
	for _, layout := range []string{time.RFC1123Z, time.RFC1123, time.RFC822Z, time.RFC822, "2 Jan 2006 15:04:05 -0700", time.RFC3339} {
		if t, err := time.Parse(layout, d); err == nil {
			return t.Local().Format("2006-01-02 15:04")
		}
	}
	return d
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `go test ./internal/cache/ -run TestFormatDate -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git -C "$(pwd)" add internal/cache/format.go internal/cache/format_test.go
git -C "$(pwd)" commit -m "feat(v0.5): FormatDate — 2006-01-02 15:04 local, shared CLI+TUI"
```

---

### Task 7: CLI ls — leer del cache + mostrar hora + s3Key

**Files:**
- Modify: `cmd/mail/main.go:30-41` (renderList) y `cmd/mail/main.go:119-165` (lsCmd RunE)
- Test: `cmd/mail/main_test.go` (renderList es pure + testeable)

**Interfaces:**
- Consumes: `cache.Open`, `cache.DefaultPath`, `cache.Sync`, `cache.List`, `cache.FormatDate`.
- Produces: `mail ls` sirve desde el cache (fallback a Reader), imprime fecha+hora y s3Key.

- [ ] **Step 1: Write the failing test for renderList**

```go
func TestRenderListShowsDateTimeAndS3Key(t *testing.T) {
	hs := []mailbox.Header{
		{S3Key: "inbound/aaa-000000", From: "alice@example.com", Subject: "Hello", Date: "Wed, 25 Jun 2026 14:32:00 +0000"},
	}
	var buf bytes.Buffer
	if err := renderList(&buf, hs, false); err != nil {
		t.Fatalf("renderList: %v", err)
	}
	out := buf.String()
	if !strings.Contains(out, "inbound/aaa-000000") {
		t.Errorf("output missing s3Key:\n%s", out)
	}
	if !strings.Contains(out, "2026-06-25") { // date part (time is TZ-local, only assert the date)
		t.Errorf("output missing formatted date:\n%s", out)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./cmd/mail/ -run TestRenderList -v`
Expected: FAIL (s3Key not in output; date is raw RFC1123Z).

- [ ] **Step 3: Modify renderList**

Reemplazar el cuerpo de `renderList` (cmd/mail/main.go:32-41) por:

```go
func renderList(w io.Writer, hs []mailbox.Header, asJSON bool) error {
	if asJSON {
		return json.NewEncoder(w).Encode(hs)
	}
	tw := tabwriter.NewWriter(w, 0, 2, 2, ' ', 0)
	for _, h := range hs {
		// date+time, sender, subject, s3Key — s3Key last so the user can copy it (tmux copy-mode)
		fmt.Fprintf(tw, "%s\t%s\t%s\t%s\n", cache.FormatDate(h.Date), h.From, h.Subject, h.S3Key)
	}
	return tw.Flush()
}
```

Añadir el import `"erickaldama-mail/internal/cache"` en cmd/mail/main.go.

- [ ] **Step 4: Run renderList test to verify it passes**

Run: `go test ./cmd/mail/ -run TestRenderList -v`
Expected: PASS.

- [ ] **Step 5: Wire the cache into lsCmd (read path with fallback)**

En `lsCmd.RunE` (cmd/mail/main.go), reemplazar el bloque de listado (líneas ~144-152) para intentar el cache primero:

```go
			var all []mailbox.Header
			// Try the cache: open, sync each mailbox, read from SQLite. On ANY cache failure,
			// fall back transparently to the live Reader (the cache is a discardable optimization).
			cachePath, perr := cache.DefaultPath()
			ca, cerr := (*cache.Cache)(nil), error(nil)
			if perr == nil {
				ca, cerr = cache.Open(cachePath)
			}
			if perr == nil && cerr == nil {
				defer ca.Close()
				for _, mb := range mailboxes {
					// Populate with a FIXED cap (not count) so search sees the full history (M-2).
					if _, serr := ca.Sync(ctx, r, mb, cache.SyncPageLimit); serr != nil {
						fmt.Fprintf(os.Stderr, "warning: cache sync %s failed (%v); using live\n", mb, serr)
					}
					hs, lerr := ca.List(mb, count) // display limit = count
					if lerr != nil {
						// m-1: cache List failed → fall back to live for THIS mailbox, don't drop it.
						fmt.Fprintf(os.Stderr, "warning: cache list %s failed (%v); using live\n", mb, lerr)
						if live, _, le := r.List(ctx, mb, int32(count), nil); le == nil {
							all = append(all, live...)
						}
						continue
					}
					all = append(all, hs...)
				}
			} else {
				// Cache unavailable → live path (original behaviour).
				for _, mb := range mailboxes {
					hs, _, lerr := r.List(ctx, mb, int32(count), nil)
					if lerr != nil {
						fmt.Fprintf(os.Stderr, "error listing %s: %v\n", mb, lerr)
						continue
					}
					all = append(all, hs...)
				}
			}
```

El resto de `lsCmd` (sort por SK desc, truncate a count, el modo `--count`, `renderList`) se PRESERVA sin cambios.

> **Decisión M-1 (tmux status, main.go:~506):** el subcomando `tmux status` (el que alimenta el status bar con
> `📬 %d`) se DEJA en el live path (r.List directo), NO se cachea. Razón: el status bar debe reflejar el conteo
> AUTORITATIVO de DynamoDB en cada refresh, no un cache posiblemente frío; y es una lectura de conteo trivial que
> no se beneficia de la latencia del cache como sí lo hace el listado completo. Documentar en el runbook (Task 12)
> que `mail ls` sirve del cache pero `tmux status` es live — divergencia intencional, no un olvido.

- [ ] **Step 6: Verify build + full CLI tests**

Run: `go build ./... && go test ./cmd/mail/ -v`
Expected: exit 0, PASS.

- [ ] **Step 7: Commit**

```bash
git -C "$(pwd)" add cmd/mail/main.go cmd/mail/main_test.go
git -C "$(pwd)" commit -m "feat(v0.5): mail ls reads from cache (fallback live) + date+time + s3Key"
```

---

### Task 8: CLI search — subcomando mail search

**Files:**
- Modify: `cmd/mail/main.go` (añadir searchCmd + registrarlo en root)
- Test: `cmd/mail/main_test.go` (test de integración con un cache temporal)

**Interfaces:**
- Consumes: `cache.Open/DefaultPath/Sync/Search`, `renderList` (Task 7).
- Produces: `mail search <query>` — busca en el cache, imprime con `renderList` (hora+s3Key).

- [ ] **Step 1: Write the failing test**

```go
func TestSearchCmdFindsSeeded(t *testing.T) {
	// Build a temp cache seeded with one matching header, then run Search directly
	// (the command wraps this; here we assert the query path end-to-end at cache level).
	dir := t.TempDir()
	t.Setenv("XDG_CACHE_HOME", dir)
	path, _ := cache.DefaultPath()
	c, err := cache.Open(path)
	if err != nil {
		t.Fatalf("Open: %v", err)
	}
	defer c.Close()
	// Seed via Sync with a fake lister living in the cache test helpers is not visible here;
	// insert directly through Search's contract by syncing a fake through the exported API.
	// Simplest: assert Search returns empty (no seed) without error — the command wiring is the unit.
	got, err := c.Search("inbox", "anything", 10)
	if err != nil {
		t.Fatalf("Search on empty cache: %v", err)
	}
	if len(got) != 0 {
		t.Errorf("empty cache search len = %d, want 0", len(got))
	}
}
```

> Nota: el test de contenido (match real) ya está en `internal/cache/query_test.go` (Task 5). Aquí sólo se
> verifica que el subcomando se cablea sin romper y que Search sobre cache vacío no falla. El smoke E2E (F5)
> valida el match real contra datos vivos.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./cmd/mail/ -run TestSearchCmd -v`
Expected: FAIL (o compila-error si aún no existe el import; el rojo guía la implementación).

- [ ] **Step 3: Add searchCmd**

Añadir en `main()` de cmd/mail/main.go, junto a los otros subcomandos:

```go
	// ── search ───────────────────────────────────────────────────────────
	searchCmd := &cobra.Command{
		Use:   "search <query>",
		Short: "Full-text search over cached headers (sender + subject)",
		Args:  cobra.MinimumNArgs(1),
		RunE: func(cmd *cobra.Command, args []string) error {
			ctx := context.Background()
			cfg, hasCfg, _ := config.Load()
			var mailboxes []string
			if cmd.Flags().Changed("mailbox") {
				mailboxes = []string{mailboxName}
			} else if hasCfg && len(cfg.Mailboxes) > 0 {
				mailboxes = cfg.Mailboxes
			} else {
				return fmt.Errorf("no mailbox specified and no config")
			}
			if !cmd.Root().PersistentFlags().Changed("read-profile") && hasCfg && cfg.ReadProfile != "" {
				readProfile = cfg.ReadProfile
			}
			query := strings.Join(args, " ")

			cachePath, err := cache.DefaultPath()
			if err != nil {
				return fmt.Errorf("cache path: %w", err)
			}
			ca, err := cache.Open(cachePath)
			if err != nil {
				return fmt.Errorf("cache open: %w", err)
			}
			defer ca.Close()
			// Refresh the cache first so search reflects recent mail (best-effort). Populate with the
			// FIXED cap (not count) — search must see the full history, not just --count rows (M-2).
			if r, rerr := wire.Reader(ctx, readProfile); rerr == nil {
				for _, mb := range mailboxes {
					if _, serr := ca.Sync(ctx, r, mb, cache.SyncPageLimit); serr != nil {
						fmt.Fprintf(os.Stderr, "warning: cache sync %s failed (%v)\n", mb, serr)
					}
				}
			}
			var all []mailbox.Header
			for _, mb := range mailboxes {
				hs, serr := ca.Search(mb, query, count)
				if serr != nil {
					fmt.Fprintf(os.Stderr, "error searching %s: %v\n", mb, serr)
					continue
				}
				all = append(all, hs...)
			}
			return renderList(os.Stdout, all, jsonFlag)
		},
	}
	root.AddCommand(searchCmd)
```

(Asegurar que `searchCmd` se registra donde se registran los demás — junto a `root.AddCommand(lsCmd)` etc.)

- [ ] **Step 4: Run test + build**

Run: `go build ./... && go test ./cmd/mail/ -run TestSearchCmd -v`
Expected: exit 0, PASS.

- [ ] **Step 5: Commit**

```bash
git -C "$(pwd)" add cmd/mail/main.go cmd/mail/main_test.go
git -C "$(pwd)" commit -m "feat(v0.5): mail search subcommand — FTS5 over cache"
```

---

### Task 9: TUI display — 3ª línea con s3Key (dim) + fecha+hora

**Files:**
- Modify: `cmd/mail-tui/listitem.go` (delegate custom de 3 líneas, Description usa FormatDate)
- Test: `cmd/mail-tui/listitem_test.go`

**Interfaces:**
- Consumes: `cache.FormatDate`, `bubbles/list`, `lipgloss`.
- Produces: cada item de la lista renderiza 3 líneas: Subject / sender · fecha+hora / s3Key (faint).

- [ ] **Step 1: Write the failing test**

```go
func TestItemDescriptionUsesDateTime(t *testing.T) {
	it := messageItem{h: mailbox.Header{From: "alice@example.com", Subject: "Hi", Date: "Wed, 25 Jun 2026 14:32:00 +0000", S3Key: "inbound/aaa-000000"}}
	desc := it.Description()
	if !strings.Contains(desc, "2026-06-25") {
		t.Errorf("Description missing date+time: %q", desc)
	}
}

// TestItemDelegateRendersS3KeyFaint verifies the custom 3-line delegate: height 3, and the rendered
// output contains the s3Key AND the faint ANSI code. Forces a TrueColor profile because `go test`
// runs without a TTY (lipgloss would otherwise strip all ANSI — audit finding M-2).
func TestItemDelegateRendersS3KeyFaint(t *testing.T) {
	lipgloss.SetColorProfile(termenv.TrueColor) // ensure ANSI survives under non-TTY test
	d := newItemDelegate()
	if d.Height() != 3 {
		t.Fatalf("delegate height = %d, want 3", d.Height())
	}
	l := list.New([]list.Item{
		messageItem{h: mailbox.Header{From: "a@b.com", Subject: "S", Date: "Wed, 25 Jun 2026 14:32:00 +0000", S3Key: "inbound/aaa-000000"}},
	}, d, 80, 20)
	var buf bytes.Buffer
	d.Render(&buf, l, 0, l.Items()[0])
	out := buf.String()
	if !strings.Contains(out, "inbound/aaa-000000") {
		t.Errorf("render missing s3Key:\n%s", out)
	}
	if !strings.Contains(out, "\x1b[2m") { // faint SGR
		t.Errorf("render missing faint code for s3Key:\n%q", out)
	}
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./cmd/mail-tui/ -run "TestItemDescription|TestItemDelegate" -v`
Expected: FAIL (Description usa shortDate sin hora; newItemDelegate no existe).

- [ ] **Step 3: Modify listitem.go — custom ItemDelegate (opción b, la ÚNICA que da faint independiente)**

**Por qué custom delegate y NO el DefaultDelegate:** el auditor verificó que `DefaultDelegate` estila la
Description entera como un bloque (`NormalDesc`/`SelectedDesc`) — no permite `Faint` solo en la 3ª línea. El
spec §6c exige el s3Key faint independiente, así que hace falta un `list.ItemDelegate` propio. (El path (a)
—plegar en Description— renderiza 3 líneas pero NO da faint independiente; queda descartado.)

```go
// Description is the second line: sender + date+time (v0.5: FormatDate, shared with the CLI).
func (it messageItem) Description() string {
	return it.h.From + " · " + cache.FormatDate(it.h.Date)
}

// itemDelegate renders each message as three lines: subject (title), sender·datetime (description),
// and the S3 key in faint style. DefaultDelegate can't style the third line independently, so this
// is a minimal custom list.ItemDelegate (audit C-2).
type itemDelegate struct{}

func newItemDelegate() itemDelegate { return itemDelegate{} }

func (d itemDelegate) Height() int                             { return 3 }
func (d itemDelegate) Spacing() int                            { return 1 }
func (d itemDelegate) Update(_ tea.Msg, _ *list.Model) tea.Cmd { return nil }

var (
	titleStyle    = lipgloss.NewStyle().Bold(true)
	descStyle     = lipgloss.NewStyle().Faint(true)
	s3KeyStyle    = lipgloss.NewStyle().Faint(true)
	selectedStyle = lipgloss.NewStyle().Bold(true).Foreground(lipgloss.Color("6"))
)

func (d itemDelegate) Render(w io.Writer, m list.Model, index int, item list.Item) {
	it, ok := item.(messageItem)
	if !ok {
		return
	}
	title := it.Title()
	if index == m.Index() {
		title = selectedStyle.Render(title)
	} else {
		title = titleStyle.Render(title)
	}
	fmt.Fprintf(w, "%s\n%s\n%s", title, descStyle.Render(it.Description()), s3KeyStyle.Render(it.h.S3Key))
}

// newMessageList builds the list.Model with the 3-line itemDelegate.
func newMessageList(headers []mailbox.Header, width, height int) list.Model {
	items := make([]list.Item, len(headers))
	for i, h := range headers {
		items[i] = messageItem{h: h}
	}
	l := list.New(items, newItemDelegate(), width, height)
	l.Title = "Inbox"
	l.SetShowStatusBar(false)
	l.SetShowHelp(false)
	return l
}
```

Añadir a los imports de `listitem.go`: `"bytes"` no (es del test), sí `"fmt"`, `"io"`, `tea "github.com/charmbracelet/bubbletea"`, `"github.com/charmbracelet/lipgloss"`, y en el test `"github.com/charmbracelet/lipgloss"` + `"github.com/muesli/termenv"`.

> **Nota de firmas (verificadas contra bubbles v1.0.0):** `list.ItemDelegate` requiere exactamente
> `Height() int`, `Spacing() int`, `Update(tea.Msg, *list.Model) tea.Cmd`, `Render(io.Writer, list.Model, int, list.Item)`.
> `m.Index()` da el índice seleccionado. No usar `l.Delegate` (campo privado en v1.0.0, sin getter).

- [ ] **Step 4: Run tests to verify they pass**

Run: `go test ./cmd/mail-tui/ -v`
Expected: PASS (los tests de v0.4 siguen verdes + los nuevos).

- [ ] **Step 5: Commit**

```bash
git -C "$(pwd)" add cmd/mail-tui/listitem.go cmd/mail-tui/listitem_test.go
git -C "$(pwd)" commit -m "feat(v0.5): TUI 3-line item — subject / sender·datetime / s3Key (faint)"
```

---

### Task 10: TUI filtro → FTS5 sobre cache (preservar B-1/B-2/D3/C-debounce)

**Files:**
- Modify: `cmd/mail-tui/model.go` (campo cache, handleKey del filtro), `cmd/mail-tui/main.go` (inyectar cache)
- Test: `cmd/mail-tui/model_test.go`

**Interfaces:**
- Consumes: `cache.Cache` (List/Search), el `model` de v0.4.
- Produces: al filtrar con `/`, la lista se recarga con `cache.Search`; al limpiar, con `cache.List`. Las invariantes B-1/B-2/D3/C-debounce intactas.

> **RIESGO ALTO (destacado en el spec §6c):** este es el cambio de comportamiento más profundo. El filtro
> nativo de bubbles/list se sustituye por una búsqueda FTS5 que RECARGA la lista. La disciplina:
> (1) los tests de v0.4 (`model_test.go`) DEBEN seguir verdes — no se editan salvo que una invariante cambie
> deliberadamente; (2) el whole-branch review de F5 verifica filtro↔selección↔debounce.

- [ ] **Step 1: Añadir el cache al model (no fatal si nil)**

En `model` struct (model.go), añadir campo `cache *cache.Cache`. En `mail-tui/main.go`, abrir el cache y
sincronizar antes de construir el model; pasar `cache: ca` (nil-safe). El pre-load de headers pasa a
`ca.List(mailboxName, 50)` cuando el cache está disponible, con fallback a `r.List` (como hoy).

```go
// mail-tui/main.go — tras wire.Reader, antes de construir headers:
var ca *cache.Cache
if cachePath, perr := cache.DefaultPath(); perr == nil {
	if opened, cerr := cache.Open(cachePath); cerr == nil {
		ca = opened
		defer ca.Close()
		if r != nil {
			if _, serr := ca.Sync(ctx, r, mailboxName, cache.SyncPageLimit); serr != nil { // fixed cap, not 50 (M-2)
				fmt.Fprintf(os.Stderr, "warning: cache sync failed (%v)\n", serr)
			}
		}
	}
}
var headers []mailbox.Header
if ca != nil {
	if hs, lerr := ca.List(mailboxName, 50); lerr == nil {
		headers = hs
	}
}
if headers == nil && r != nil { // fallback to live
	if hs, _, lerr := r.List(ctx, mailboxName, 50, nil); lerr == nil {
		headers = hs
	}
}
// ... model{ ..., cache: ca, from: from, ... }
```

- [ ] **Step 2: Write the failing test — filter reloads from Search**

```go
func TestFilterUsesCacheSearch(t *testing.T) {
	// A model with a seeded cache: applying a filter query should reload the list with Search results.
	c, err := cache.Open(filepath.Join(t.TempDir(), "index.sqlite"))
	if err != nil {
		t.Fatalf("Open: %v", err)
	}
	defer c.Close()
	// (seed via the cache package test seam is out of scope here; assert the wiring: when cache is set
	// and a filter is applied, the model queries Search and rebuilds items without panicking, and B-1
	// selection-by-S3Key still holds.)
	m := model{cache: c, from: "inbox", list: newMessageList(nil, 30, 20)}
	// Applying an empty filter must restore List (no panic, list non-nil).
	m2, _ := m.applyFilter("")
	if m2.list.Items() == nil && len(m2.list.Items()) != 0 {
		t.Errorf("applyFilter empty should restore list")
	}
}
```

> El test real de "match recarga N items" vive en el smoke E2E (F5) con datos vivos; aquí se prueba el
> cableado (no-panic, restauración) y la preservación de invariantes vía los tests existentes de v0.4.

- [ ] **Step 3: Run test to verify it fails**

Run: `go test ./cmd/mail-tui/ -run TestFilterUsesCache -v`
Expected: FAIL (applyFilter no existe).

- [ ] **Step 4: Implement applyFilter + wire into handleKey**

Añadir un helper `applyFilter` que recarga los items desde `cache.Search` (o `cache.List` si query vacío),
preservando la selección por S3Key (B-1). Interceptar la confirmación del filtro en `handleKey` — cuando el
usuario aplica el filtro (Enter/Tab con `SettingFilter`), en vez de dejar filtrar a bubbles nativo, tomar el
`FilterValue` escrito y llamar `applyFilter`. Mantener `SetFilteringEnabled` para la UI del prompt `/`, pero
resolver el resultado vía Search.

```go
// applyFilter rebuilds the list from the cache: Search(query) when non-empty, List otherwise.
// Preserves the current selection by S3Key (B-1) when the selected message survives the filter.
func (m model) applyFilter(query string) (model, tea.Cmd) {
	if m.cache == nil {
		return m, nil // no cache → leave native filtering as-is (graceful degradation)
	}
	var hs []mailbox.Header
	var err error
	if strings.TrimSpace(query) == "" {
		hs, err = m.cache.List(m.from, 200)
	} else {
		hs, err = m.cache.Search(m.from, query, 200)
	}
	if err != nil {
		m.statusMsg = "search error: " + err.Error()
		return m, nil
	}
	prevKey := selectedKey(m)
	m.list = newMessageList(hs, m.list.Width(), m.list.Height())
	// Restore selection by S3Key if still present (B-1: never by Index).
	for i, it := range m.list.Items() {
		if mi, ok := it.(messageItem); ok && mi.h.S3Key == prevKey {
			m.list.Select(i)
			break
		}
	}
	return m, nil
}
```

**Wire en `handleKey` — punto de intercepción CORRECTO (verificado contra bubbles v1.0.0):** el error a evitar
es leer `SettingFilter()` DESPUÉS de confirmar — para entonces ya es `false`. bubbles transiciona
`Filtering → FilterApplied` DENTRO de la llamada `m.list.Update(key)` (en `handleFiltering`, al recibir
Enter/Tab/flechas — el keymap `AcceptWhileFiltering` incluye `enter, tab, shift+tab, ctrl+k, up, ctrl+j, down`,
por eso B-2/Tab funciona). Hay que **detectar la transición alrededor de la delegación** y capturar el texto
ANTES. Reemplazar el bloque `if m.list.SettingFilter()` de `handleKey` (model.go:222-226) por:

```go
	if m.list.SettingFilter() { // D3: mientras se filtra, todas las teclas van a la lista
		prevQuery := m.list.FilterValue() // == m.FilterInput.Value(); capturar ANTES de delegar
		var cmd tea.Cmd
		m.list, cmd = m.list.Update(key) // bubbles puede transicionar Filtering→FilterApplied AQUÍ (B-2: Tab/Enter)
		if !m.list.SettingFilter() {      // el usuario confirmó (Enter/Tab/flecha) o limpió el filtro
			m2, c2 := m.applyFilter(prevQuery) // recarga vía cache.Search (o List si vacío)
			return m2, tea.Batch(cmd, c2)
		}
		return m, cmd // sigue tecleando el filtro
	}
```

Notas de firmas (verificadas contra bubbles v1.0.0): `FilterValue() string` == `FilterInput.Value()` (ambos
públicos); `SettingFilter() bool` ⟺ `filterState == Filtering`. Si `prevQuery` queda vacío, `applyFilter("")`
restaura la lista completa vía `cache.List`. Esto preserva B-2 (Tab sigue confirmando el filtro dentro de
`m.list.Update`) y D3 (mientras `SettingFilter()` es true, la rama entera delega a la lista).

- [ ] **Step 5: Run tests — nuevos + TODOS los de v0.4**

Run: `go test ./cmd/mail-tui/ -v`
Expected: PASS. **Crítico:** los tests de v0.4 (B-1/B-2/D3/C-debounce) deben seguir verdes. Si alguno falla,
la invariante se rompió → NO avanzar, corregir.

- [ ] **Step 6: Commit**

```bash
git -C "$(pwd)" add cmd/mail-tui/model.go cmd/mail-tui/main.go cmd/mail-tui/model_test.go
git -C "$(pwd)" commit -m "feat(v0.5): TUI filter uses FTS5 over cache (B-1/B-2/D3/C-debounce preserved)"
```

---

### Task 11: aiassist tools — leer del cache (consistencia)

**Files:**
- Modify: `internal/aiassist/tools.go:37,62` (los 2 call-sites de reader.List)
- Test: `internal/aiassist/tools_test.go`

**Interfaces:**
- Consumes: `cache` (opcional, fallback a reader).
- Produces: las herramientas del AI leen los mismos headers cacheados (con hora+s3Key coherentes).

> **Decisión de alcance:** los AI tools son un consumidor secundario. Para NO inflar el PR y NO acoplar
> el aiassist al cache innecesariamente, v0.5 los deja usando `reader.List` directo (sin cambio funcional).
> Esta tarea SOLO se ejecuta si el auditor del plan considera que la inconsistencia (AI tools sin hora/s3Key
> vs CLI/TUI con ellos) es un gap real. Si no, se marca N/A con esa justificación. **Default: N/A** — los AI
> tools devuelven Headers crudos al LLM, que no necesita el formato de display humano.

- [ ] **Step 1 (si aplica): decisión documentada**

Registrar en el runbook: "AI tools siguen leyendo del Reader directo en v0.5 — el LLM consume Header crudo,
no display formateado; cachearlos no aporta y acopla aiassist a cache. Diferido." Si el auditor discrepa,
convertir en tarea real espejo de Task 7.

---

### Task 12: CHANGELOG + runbook

**Files:**
- Modify: `CHANGELOG.md`
- Create: `docs/v0.5-CACHE.md` (runbook)

**Interfaces:** documentación de cierre (F6 la completa con deploy findings del smoke).

- [ ] **Step 1: CHANGELOG entry orgánico**

Añadir sección `## [0.5.0]` narrando: cache SQLite+FTS5 (contentless, sin triggers — por qué), display
hora+s3Key, `mail search`, filtro TUI a FTS5. Incluir los 2 hallazgos del research como decisiones de diseño.

- [ ] **Step 2: Runbook docs/v0.5-CACHE.md**

Estructura: qué es el cache, dónde vive (path, 0600 + sidecar -wal/-shm 0600, descartable — cómo borrarlo:
`rm ~/.cache/erickaldama-mail/index.sqlite*`), la versión resuelta de modernc.org/sqlite (v1.53.0, SQLite 3.53.2),
el flujo sync (tope `SyncPageLimit=500`), cómo buscar (`mail search`, filtro `/` TUI), troubleshooting (SQLITE_BUSY
→ busy_timeout 5s, cache corrupto → borrar y re-sync). **Limitaciones conocidas (OBLIGATORIO documentar, audit M-3/M-1):**
(a) el cache NO detecta correos borrados en DynamoDB — quedan como huérfanos; `rm` del `.sqlite*` para reconciliar;
(b) `mail ls` sirve del cache pero `tmux status` es LIVE (conteo autoritativo) — divergencia intencional;
(c) dos invocaciones concurrentes: la 2ª espera hasta busy_timeout (5s), sin corrupción;
(d) inconsistencia menor: los AI tools (`internal/aiassist`) usan substring en vez de FTS5 (diferido, audit m-2).
Los deploy findings del smoke (F5) se añaden en F6.

- [ ] **Step 3: Commit**

```bash
git -C "$(pwd)" add CHANGELOG.md docs/v0.5-CACHE.md
git -C "$(pwd)" commit -m "docs(v0.5): CHANGELOG + runbook del cache (limitaciones conocidas)"
```

---

## Notas de ejecución (F4/F5)

- **Orden de dependencia:** 1↔2 (acopladas: Task 1 solo `go get`, el commit de deps + `go mod tidy` es en Task 2)
  → 3 → 6 → 4 → 5 → 7 → 8 → 9 → 10 → 11(N/A) → 12. Un `go mod tidy` de cierre en Task 12 verifica go.mod/go.sum limpios.
- Cada tarea: implementer TDD → validar reporte contra código real (count, vet, build, `go mod verify`) →
  auditoría 6-ejes → ledger. Disciplina worktree: `git -C "$WT"`, rama ≠ develop.
- **Tareas de riesgo:**
  - Task 4 (sync FTS): usa el comando `'delete'` con valores VIEJOS, NUNCA `DELETE FROM` (contentless lo rechaza).
    El `TestSyncMutationDoesNotLeaveStaleFTS` es el guardián — debe pasar.
  - Task 9 (delegate): custom `list.ItemDelegate` (opción b), NO DefaultDelegate (no da faint independiente).
  - Task 10 (filtro TUI): la más delicada — detectar transición `Filtering→!Filtering` alrededor de `m.list.Update`,
    capturar `FilterValue()` ANTES. Los tests de v0.4 (B-1/B-2/D3/C-debounce) deben seguir verdes.
- **Verificado empíricamente en la auditoría (compilado contra modernc.org/sqlite v1.53.0):** driver name `"sqlite"`,
  FTS5 sin build tag (linux+darwin), `RETURNING rowid` en UPSERT, `ORDER BY rank`, comando `'delete'` contentless,
  `integrity-check`. NO re-litigar estos en F4.
- **Smoke E2E (F5):** `mail ls` puebla el `.db` (verificar `stat -f %Lp` = 600, y los sidecar `-wal`/`-shm`) + muestra
  hora+s3Key; `mail search <término real>` encuentra en TODO el histórico (no solo --count); el filtro `/` de la TUI
  usa FTS5; 2ª corrida de `ls` NO rompe (el bug del DELETE contentless) y NO duplica.
