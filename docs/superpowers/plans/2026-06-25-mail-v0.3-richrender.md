# Cliente correo v0.3 — Render rico HTML+imágenes — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development con auditoría 6-ejes por
> tarea. Steps usan checkbox (`- [ ]`). Spec: `docs/superpowers/specs/2026-06-25-mail-v0.3-richrender-design.md` (dd5878b).
> Findings de auditoría del spec: `~/.claude/plans/email-project-research/22-mail-v0.3-spec-audit-findings.md`.

**Goal:** El reader del correo muestra el body con imágenes (chafa `-f symbols`) y HTML sanitizado (sin tracking pixels),
en terminal, sin fallar si falta chafa.

**Architecture:** 4 piezas — (1) parse.go expone las imágenes inline cid: del MIME; (2) sanitize.go limpia el HTML y
bloquea remotas; (3) images.go renderiza cid: con chafa; (4) render.go/model.go/main.go wirean ancho dinámico + teclas.

**Tech Stack:** Go 1.26, enmime/v2 (ya), bluemonday v1.0.27 (ya indirect→directo), golang.org/x/net/html (ya indirect),
glamour v0.9.1 (ya), chafa 1.18.2 (binario externo, degrada si falta), golang.org/x/term (para CLI width).

## Global Constraints
- modern-go: `any` no `interface{}` salvo firmas SDK. Errores tipados (avoid-string-match-error-silencing).
- **INVARIANTE SAN (seguridad, testear):** SAN-1 (sin script/on*/iframe/style/link), SAN-2 (ningún `<img>` http/https
  sobrevive con allowRemote=false; cid: sí), SAN-3 (chafa via exec.CommandContext argv-slice + stdin, NUNCA sh -c ni
  temp file), SAN-4 (chafa ausente→mensaje, no crash), SAN-4b (imagen inválida exit2→placeholder, no crash), SAN-5
  (bytes cid: vienen de Parsed.InlineImages, NUNCA del HTML).
- **chafa SIEMPRE `-f symbols`** en v0.3 (sobrevive cambio de pane tmux; sin passthrough). Pixel = v0.4+.
- **glamour:** NewTermRenderer(WithAutoStyle(), WithWordWrap(width)); fallback 80 si width==0 (WithWordWrap(0) rompe).
- NDA: el fixture de test es un `.eml` SINTÉTICO (no correos reales). Gate humano para smoke con correo real.
- Repo público Git Flow: cierre por PR a develop, CI verde. Cada commit con `git -C "$WT"`, rama ≠ develop/main.

---

### Task 1: `Parsed.InlineImages` — exponer imágenes cid: del MIME

**Files:**
- Modify: `internal/message/parse.go` (struct Parsed + Parse())
- Test: `internal/message/parse_test.go`

**Interfaces:**
- Produces: `type InlineImage struct { ContentID, ContentType string; Data []byte }`; campo `Parsed.InlineImages []InlineImage`

- [ ] **Step 1: Test que falla** — añade a parse_test.go:

```go
func TestParseExtractsInlineImages(t *testing.T) {
	// raw MIME con multipart/related: 1 text/html + 1 image/png inline (cid:logo@x)
	raw := "MIME-Version: 1.0\r\n" +
		"Content-Type: multipart/related; boundary=b1\r\n\r\n" +
		"--b1\r\n" +
		"Content-Type: text/html\r\n\r\n" +
		"<html><body><img src=\"cid:logo@x\"></body></html>\r\n" +
		"--b1\r\n" +
		"Content-Type: image/png\r\n" +
		"Content-ID: <logo@x>\r\n" +
		"Content-Transfer-Encoding: base64\r\n\r\n" +
		"iVBORw0KGgo=\r\n" + // PNG header bytes (base64)
		"--b1--\r\n"
	p, err := Parse(strings.NewReader(raw)) // P-4: Parse toma io.Reader, NO []byte
	if err != nil {
		t.Fatalf("Parse: %v", err)
	}
	if len(p.InlineImages) != 1 {
		t.Fatalf("want 1 inline image, got %d", len(p.InlineImages))
	}
	img := p.InlineImages[0]
	if img.ContentID != "logo@x" { // sin <>
		t.Errorf("ContentID = %q, want logo@x", img.ContentID)
	}
	if img.ContentType != "image/png" {
		t.Errorf("ContentType = %q, want image/png", img.ContentType)
	}
	if len(img.Data) == 0 {
		t.Error("Data empty — base64 not decoded")
	}
}
```

- [ ] **Step 2: Run → FAIL** — `go test ./internal/message/ -run TestParseExtractsInlineImages` → falla (InlineImages no existe).

- [ ] **Step 3: Implementar** — en parse.go:
  - Añade el tipo y el campo (cerca de la struct Parsed):
```go
// InlineImage is an embedded image part referenced by the HTML via cid:.
// Data is already base64/QP-decoded by enmime.
type InlineImage struct {
	ContentID   string // without angle brackets (enmime strips them)
	ContentType string
	Data        []byte
}
```
  Añade `InlineImages []InlineImage` a la struct `Parsed` (junto a TextHTML).
  - En `Parse()`, tras construir el resto del Parsed, recolecta de `env.Inlines` Y `env.OtherParts` (Gmail/Outlook usan
    multipart/related sin Content-Disposition: inline → caen en OtherParts). Reemplaza el comentario TODO existente:
```go
	for _, parts := range [][]*enmime.Part{env.Inlines, env.OtherParts} {
		for _, ip := range parts {
			if ip.ContentID == "" || !strings.HasPrefix(ip.ContentType, "image/") {
				continue
			}
			parsed.InlineImages = append(parsed.InlineImages, InlineImage{
				ContentID:   ip.ContentID, // enmime ya quitó los <>
				ContentType: ip.ContentType,
				Data:        ip.Content, // ya decodificado
			})
		}
	}
```
  (Verifica el nombre real de la variable del Envelope y del Parsed en el código.) **P-4: `parse.go` NO importa
  `strings` hoy (solo `io` + enmime/v2) — AÑADIR `"strings"` al import block.** El test también necesita `"strings"`.

- [ ] **Step 4: Run → PASS** — `go test -count=1 ./internal/message/ -run TestParseExtractsInlineImages` → PASS.

- [ ] **Step 5: Commit** — `git -C "$WT" add internal/message/parse.go internal/message/parse_test.go && git -C "$WT" commit -m "feat(message): Parsed.InlineImages — surface cid: images from MIME (Inlines+OtherParts)"`

---

### Task 2: `sanitize.go` — limpiar HTML + bloquear remotas

**Files:**
- Create: `internal/message/sanitize.go`
- Test: `internal/message/sanitize_test.go`

**Interfaces:**
- Produces: `type SanitizeResult struct { HTML string; CIDRefs []string; BlockedRemotes []string }`;
  `func SanitizeHTML(rawHTML string, allowRemote bool) (SanitizeResult, error)`
- Consumes: bluemonday, golang.org/x/net/html

- [ ] **Step 1: Tests que fallan** — sanitize_test.go con los invariantes SAN-1/SAN-2:

```go
package message

import (
	"regexp"
	"strings"
	"testing"
)

func TestSanitizeBlocksXSS(t *testing.T) { // SAN-1
	in := `<p>ok</p><script>alert(1)</script><a href="javascript:bad()">x</a>` +
		`<img onerror="evil()" src="cid:a@b"><iframe src="x"></iframe>` +
		`<style>body{}</style><p style="color:red">y</p><link rel="stylesheet" href="x">`
	r, err := SanitizeHTML(in, false)
	if err != nil {
		t.Fatalf("SanitizeHTML: %v", err)
	}
	// P-9: incluir <object>, <embed>, data: (UGCPolicy los bloquea — el test lo asegura ante regresiones)
	for _, bad := range []string{"<script", "javascript:", "onerror", "<iframe", "<style", "style=", "<link", "<object", "<embed", "data:"} {
		if strings.Contains(r.HTML, bad) {
			t.Errorf("SAN-1: %q survived: %s", bad, r.HTML)
		}
	}
}

func TestSanitizeBlocksRemoteImages(t *testing.T) { // SAN-2 (forma canónica)
	in := `<img src="http://track.er/p.png"><img src="cid:logo@x">`
	r, err := SanitizeHTML(in, false)
	if err != nil {
		t.Fatalf("SanitizeHTML: %v", err)
	}
	// P-3: assert robusto — NINGÚN src apunta a red (no solo buscar "http://" literal)
	if regexp.MustCompile(`src=["'][^"']*://`).MatchString(r.HTML) {
		t.Errorf("SAN-2: remote URL with scheme survived: %s", r.HTML)
	}
	if !strings.Contains(r.HTML, "cid:logo@x") {
		t.Errorf("SAN-2: cid: image lost: %s", r.HTML)
	}
	if len(r.BlockedRemotes) != 1 {
		t.Errorf("want 1 blocked remote, got %d", len(r.BlockedRemotes))
	}
}

func TestSanitizeBlocksRemoteImagesBypasses(t *testing.T) { // P-3: los bypasses que el test canónico no veía
	cases := []string{
		`<img src="HTTP://track.er/p.png">`,  // mayúsculas
		`<img src="HTTPS://track.er/p.png">`, // mayúsculas
		`<img src="//track.er/p.png">`,       // protocol-relative
		`<img src="  http://track.er/p.png">`, // espacios al inicio
	}
	reScheme := regexp.MustCompile(`src=["'][^"']*://`)
	reRel := regexp.MustCompile(`src=["']\s*//`)
	for _, in := range cases {
		r, err := SanitizeHTML(in, false)
		if err != nil {
			t.Fatalf("SanitizeHTML(%q): %v", in, err)
		}
		if reScheme.MatchString(r.HTML) || reRel.MatchString(r.HTML) {
			t.Errorf("SAN-2 bypass: remote URL survived in %q → %s", in, r.HTML)
		}
	}
}

func TestSanitizeAllowRemote(t *testing.T) { // escape consciente
	in := `<img src="http://track.er/p.png">`
	r, err := SanitizeHTML(in, true)
	if err != nil {
		t.Fatalf("SanitizeHTML: %v", err)
	}
	if !strings.Contains(r.HTML, "http://track.er/p.png") {
		t.Errorf("allowRemote=true should keep remote img: %s", r.HTML)
	}
}
```

- [ ] **Step 2: Run → FAIL** — `go test ./internal/message/ -run TestSanitize` → falla (sanitize.go no existe).

- [ ] **Step 3: Implementar** — sanitize.go:

```go
package message

import (
	"bytes"
	"regexp"
	"strings"

	"github.com/microcosm-cc/bluemonday"
	"golang.org/x/net/html"
)

// SanitizeResult carries the cleaned HTML plus the cid: refs found and the remote URLs blocked.
type SanitizeResult struct {
	HTML           string
	CIDRefs        []string
	BlockedRemotes []string
}

const remotePlaceholder = "[imagen remota bloqueada]"

// isRemoteURL reports whether a URL points to the network: http(s), or protocol-relative (//host).
// Case-insensitive (P-2: HTTP:// bypass). Covers the SAN-2 bypasses the audit found.
func isRemoteURL(v string) bool {
	lv := strings.ToLower(strings.TrimSpace(v))
	return strings.HasPrefix(lv, "http://") || strings.HasPrefix(lv, "https://") || strings.HasPrefix(lv, "//")
}

// SanitizeHTML cleans untrusted email HTML. With allowRemote=false (default), <img> with a remote src
// (http/https/protocol-relative, any case) is replaced by a text placeholder (anti tracking-pixel);
// cid: images survive. The HARD invariant SAN-2 is enforced by bluemonday restricting img.src to the
// cid: scheme via regexp (P-2) — NOT by the fragile manual HasPrefix. Pass 1 only collects refs and
// inserts the UX placeholder; bluemonday is the security boundary.
func SanitizeHTML(rawHTML string, allowRemote bool) (SanitizeResult, error) {
	var res SanitizeResult

	// Pass 1: preprocess with x/net/html — collect cid:/remotes, insert placeholder for blocked remotes (UX).
	root, err := html.Parse(strings.NewReader(rawHTML))
	if err != nil {
		return res, err
	}
	var walk func(*html.Node)
	walk = func(n *html.Node) {
		if n.Type == html.ElementNode && n.Data == "img" {
			for _, a := range n.Attr {
				if a.Key != "src" {
					continue
				}
				switch {
				case strings.HasPrefix(strings.ToLower(strings.TrimSpace(a.Val)), "cid:"):
					res.CIDRefs = append(res.CIDRefs, strings.TrimPrefix(strings.TrimSpace(a.Val), "cid:"))
				case isRemoteURL(a.Val):
					res.BlockedRemotes = append(res.BlockedRemotes, a.Val)
					if !allowRemote {
						// P-8: turn <img> into a <span>[placeholder] WITHOUT corrupting the tree.
						n.Data = "span"
						n.DataAtom = 0
						n.Attr = nil
						n.AppendChild(&html.Node{Type: html.TextNode, Data: remotePlaceholder})
					}
				}
			}
		}
		for c := n.FirstChild; c != nil; c = c.NextSibling {
			walk(c)
		}
	}
	walk(root)
	var buf bytes.Buffer
	if err := html.Render(&buf, root); err != nil {
		return res, err
	}

	// Pass 2: bluemonday IS the security boundary. Base UGCPolicy + restrict img.src to cid: via regexp.
	p := bluemonday.UGCPolicy()
	p.AllowAttrs("alt", "width", "height").OnElements("img")
	if allowRemote {
		// escape consciente: allow http/https/cid in img.src
		p.AllowAttrs("src").Matching(regexp.MustCompile(`(?i)^(cid:|https?://|//)`)).OnElements("img")
		p.AllowURLSchemes("cid", "http", "https")
	} else {
		// P-2 HARD: only cid: survives in img.src — any other scheme/relative is stripped by bluemonday.
		p.AllowAttrs("src").Matching(regexp.MustCompile(`(?i)^cid:`)).OnElements("img")
		p.AllowURLSchemes("cid")
	}
	res.HTML = p.Sanitize(buf.String())
	return res, nil
}
```
NOTA al implementer: bluemonday es la frontera de seguridad (el `Matching(^cid:)` garantiza SAN-2 — ningún src remoto
sobrevive, sin depender de HasPrefix manual). El pass 1 con x/net/html solo recolecta CIDRefs/BlockedRemotes e inserta
el placeholder textual (UX). `n.AppendChild` (no asignar FirstChild a mano) mantiene el árbol consistente (P-8).
- [ ] **Step 4: Run → PASS** — `go test -count=1 ./internal/message/ -run TestSanitize` → todos PASS. `go mod tidy` (promueve bluemonday/x-net a directo).

- [ ] **Step 5: Commit** — `git -C "$WT" add internal/message/sanitize.go internal/message/sanitize_test.go go.mod go.sum && git -C "$WT" commit -m "feat(message): SanitizeHTML — block remote images, strip XSS (SAN-1/SAN-2)"`

---

### Task 3: `internal/render/images.go` — render cid: con chafa

**Files:**
- Create: `internal/render/images.go`
- Test: `internal/render/images_test.go`

**Interfaces:**
- Produces: `func ChafaAvailable() bool`; `func RenderImage(ctx context.Context, data []byte, cols, rows int) string`

- [ ] **Step 1: Tests que fallan** — images_test.go (SAN-3/SAN-4/SAN-4b):

```go
package render

import (
	"context"
	"strings"
	"testing"
)

func TestRenderImageDegradesWhenAbsent(t *testing.T) { // SAN-4
	// si chafa no está, ChafaAvailable() es false y RenderImage devuelve un placeholder, no panic
	if ChafaAvailable() {
		t.Skip("chafa present; this test covers the absent path")
	}
	out := RenderImage(context.Background(), []byte("anything"), 40, 20)
	if !strings.Contains(out, "chafa") {
		t.Errorf("absent chafa should mention install hint, got %q", out)
	}
}

func TestRenderImageInvalidData(t *testing.T) { // SAN-4b
	if !ChafaAvailable() {
		t.Skip("chafa absent; this test needs chafa to test the invalid-image path")
	}
	out := RenderImage(context.Background(), []byte("not an image"), 40, 20)
	if !strings.Contains(out, "no reconocida") && !strings.Contains(out, "inválid") {
		t.Errorf("invalid image should yield placeholder, got %q", out)
	}
}
```

- [ ] **Step 2: Run → FAIL** — `go test ./internal/render/` → falla (paquete no existe).

- [ ] **Step 3: Implementar** — images.go:

```go
package render

import (
	"bytes"
	"context"
	"errors"
	"fmt"
	"os/exec"
	"time"
)

// ChafaAvailable reports whether the chafa binary is in PATH.
func ChafaAvailable() bool {
	_, err := exec.LookPath("chafa")
	return err == nil
}

// RenderImage renders image bytes to ANSI symbols (half-blocks) via chafa, sized to cols×rows cells.
// Uses -f symbols (survives tmux pane switches, no passthrough needed). Degrades to a placeholder on
// any failure (chafa absent, invalid image, timeout) — never panics. SAN-3: argv-slice + stdin, no sh -c.
func RenderImage(ctx context.Context, data []byte, cols, rows int) string {
	if !ChafaAvailable() {
		return "[chafa no instalado — brew install chafa para ver imágenes]"
	}
	ctx, cancel := context.WithTimeout(ctx, 5*time.Second)
	defer cancel()
	size := fmt.Sprintf("%dx%d", cols, rows)
	cmd := exec.CommandContext(ctx, "chafa", "--size", size, "-f", "symbols", "-")
	cmd.Stdin = bytes.NewReader(data)
	out, err := cmd.Output()
	if err != nil {
		var exitErr *exec.ExitError
		if errors.As(err, &exitErr) {
			return "[imagen no reconocida — formato inválido o vacío]" // SAN-4b
		}
		return "[error renderizando imagen]"
	}
	return string(out)
}
```

- [ ] **Step 4: Run → PASS** — `go test -count=1 ./internal/render/` → PASS (uno de los 2 tests corre según chafa presente/ausente).

- [ ] **Step 5: Commit** — `git -C "$WT" add internal/render/ && git -C "$WT" commit -m "feat(render): chafa image rendering (-f symbols, stdin, degrade gracefully) — SAN-3/SAN-4/SAN-4b"`

---

### Task 4: `render.go` — ancho dinámico en RenderRich

**Files:**
- Modify: `internal/message/render.go` (firma RenderRich + glamour)
- Modify: `internal/message/render_test.go` (call-site :17)
- Modify: `cmd/mail-tui/model.go:275` (call-site)
- Test: render_test.go

**Interfaces:**
- Produces: `func RenderRich(p *Parsed, width int) (string, error)` (firma cambia: +width)

- [ ] **Step 1: Actualizar test** — render_test.go:17 cambia `RenderRich(p)` → `RenderRich(p, 80)`; añade:

```go
func TestRenderRichRespectsWidth(t *testing.T) {
	p := &Parsed{TextHTML: "<p>" + strings.Repeat("word ", 40) + "</p>"}
	narrow, err := RenderRich(p, 40)
	if err != nil {
		t.Fatalf("RenderRich: %v", err)
	}
	// con width 40, alguna línea del output no debe exceder ~40 cols visibles (heurística: hay más de 1 línea)
	if strings.Count(narrow, "\n") < 2 {
		t.Errorf("expected wrapping at width 40, got few lines")
	}
}

func TestRenderRichWidthZeroFallback(t *testing.T) {
	p := &Parsed{TextHTML: "<p>hello</p>"}
	out, err := RenderRich(p, 0) // width 0 → fallback 80, NO output roto
	if err != nil {
		t.Fatalf("RenderRich width 0: %v", err)
	}
	if out == "" {
		t.Error("width 0 produced empty output — fallback to 80 failed")
	}
}
```

- [ ] **Step 2: Run → FAIL** — `go test ./internal/message/ -run TestRenderRich` → falla (firma vieja).

- [ ] **Step 3: Implementar** — render.go, reemplaza el cuerpo de RenderRich:

```go
// RenderRich converts the HTML body to markdown then renders it to ANSI for the TUI/CLI --rich, wrapped
// to width columns. width<=0 falls back to 80 (glamour's WithWordWrap(0) produces broken output).
// P-1 CRÍTICO: DEBE convertir HTML→markdown con htmltomd ANTES de glamour. Si se pasa HTML crudo a glamour,
// goldmark (Unsafe=false default) lo borra → texto sin formato, SIN error. El test pasaría igual.
func RenderRich(p *Parsed, width int) (string, error) {
	if width <= 0 {
		width = 80
	}
	src := p.TextHTML
	if src == "" {
		return p.TextPlain, nil
	}
	md, err := htmltomd.NewConverter("", true, nil).ConvertString(src) // ← conversión HTML→markdown OBLIGATORIA
	if err != nil {
		return "", err
	}
	r, err := glamour.NewTermRenderer(
		glamour.WithAutoStyle(),
		glamour.WithWordWrap(width),
	)
	if err != nil {
		return "", err
	}
	return r.Render(md)
}
```
(El código actual de render.go YA usa `htmltomd.NewConverter("", true, nil).ConvertString(src)` — CONSÉRVALO. Solo
cambia `glamour.Render(md, "auto")` por `NewTermRenderer(WithAutoStyle, WithWordWrap(width))`. Verifica el nombre real
del import htmltomd en render.go y reúsalo.)

- [ ] **Step 4: Actualizar el call-site del TUI** — P-7: para evitar un estado no-compilable entre T4 y T5, **añade
  `termWidth int` a la struct `model` en model.go EN ESTE PASO** (3 líneas, no requiere T5). Luego cambia el call-site
  `cmd/mail-tui/model.go:275`: `message.RenderRich(parsed)` → `message.RenderRich(parsed, m.termWidth)`. Con termWidth
  ya en la struct (zero-value 0 → RenderRich hace fallback a 80), `go build ./...` queda VERDE tras T4. T5 añade el
  resto (rawMD, WindowSizeMsg, teclas).

- [ ] **Step 5: Run → PASS + commit** — `go test -count=1 ./internal/message/` PASS Y **`go build ./...` VERDE COMPLETO**
  (P-7: con termWidth en la struct, cmd/mail-tui compila). `git -C "$WT" commit -am "feat(message): RenderRich(p, width) — dynamic glamour width, fallback 80 (D6); add termWidth to model"`

---

### Task 5: TUI — termWidth, rawMD, WindowSizeMsg, teclas i/R

**Files:**
- Modify: `cmd/mail-tui/model.go` (struct + Update + handleReaderKey + View del reader)
- Test: `cmd/mail-tui/model_test.go`

**Interfaces:**
- Consumes: message.RenderRich(p, width), message.SanitizeHTML, render.RenderImage, Parsed.InlineImages

- [ ] **Step 1: Tests** — model_test.go:

```go
func TestModelCapturesWindowSize(t *testing.T) {
	m := model{} // ajustar al constructor real
	updated, _ := m.Update(tea.WindowSizeMsg{Width: 120, Height: 40})
	if updated.(model).termWidth != 120 {
		t.Errorf("termWidth not captured from WindowSizeMsg")
	}
}

func TestReaderKeyIRendersImages(t *testing.T) {
	// con un Parsed que tiene InlineImages y view==viewReader, la tecla 'i' marca showImages=true
	// (test del estado, no del render real de chafa)
	m := model{view: viewReader, termWidth: 80} // + un parsed con InlineImages
	updated, _ := m.handleReaderKey(tea.KeyMsg{Type: tea.KeyRunes, Runes: []rune{'i'}})
	if !updated.(model).showImages {
		t.Errorf("'i' should toggle showImages")
	}
}
```

- [ ] **Step 2: Run → FAIL.**

- [ ] **Step 3: Implementar** — en model.go (test imports: `tea "github.com/charmbracelet/bubbletea"`, `testing`):
  - Struct `model`: añade `termWidth int`, `showImages bool`, `loadRemote bool`, `currentParsed *message.Parsed`,
    `rawHTML string`, `imageBlobs []string`. (P-7: NO usar un `rawMD` sin asignar como guard — usar `currentParsed != nil`.
    `rawHTML` guarda el HTML ORIGINAL sin sanitizar, para que el toggle `R` re-sanitice limpio N veces.)
  - Nuevos msgs: `type imagesRenderedMsg struct{ blobs []string }`. El `bodyLoadedMsg` debe transportar el
    `*message.Parsed` completo (no solo el string body) — ajustarlo si hoy solo lleva el string.
  - En `Update()`, añade el case de resize (P-7: guard por currentParsed, no rawMD):
```go
	case tea.WindowSizeMsg:
		m.termWidth = msg.Width
		if m.view == viewReader && m.currentParsed != nil {
			body, _ := message.RenderRich(m.currentParsed, m.termWidth)
			m.body = body
		}
		return m, nil
	case imagesRenderedMsg:
		m.imageBlobs = msg.blobs
		return m, nil
```
  - **P-5 (D4): el body load DEBE sanitizar primero.** En el handler de `bodyLoadedMsg`:
```go
	case bodyLoadedMsg:
		m.rawHTML = msg.parsed.TextHTML // HTML original sin sanitizar, para el toggle R
		m.loadRemote = false
		san, _ := message.SanitizeHTML(m.rawHTML, false)
		clean := *msg.parsed
		clean.TextHTML = san.HTML
		m.currentParsed = &clean
		body, _ := message.RenderRich(&clean, m.termWidth)
		m.body = body
		m.imageBlobs = nil
		m.showImages = false
		return m, nil
```
  - En `handleReaderKey` (P-6: imágenes ASÍNCRONAS vía tea.Cmd — NUNCA bloquear el event loop):
```go
	case 'i':
		if m.currentParsed == nil || len(m.currentParsed.InlineImages) == 0 {
			m.statusMsg = "no hay imágenes inline en este correo"
			return m, nil
		}
		m.showImages = true
		imgs := m.currentParsed.InlineImages
		w, h := m.termWidth, m.termWidth/2
		return m, func() tea.Msg {
			var blobs []string
			for _, im := range imgs {
				blobs = append(blobs, render.RenderImage(context.Background(), im.Data, w, h))
			}
			return imagesRenderedMsg{blobs: blobs}
		}
	case 'R': // P-5: re-sanitiza desde el HTML ORIGINAL (m.rawHTML) con el toggle — limpio N veces
		m.loadRemote = !m.loadRemote
		if m.currentParsed != nil && m.rawHTML != "" {
			san, _ := message.SanitizeHTML(m.rawHTML, m.loadRemote)
			clean := *m.currentParsed
			clean.TextHTML = san.HTML
			m.currentParsed = &clean
			body, _ := message.RenderRich(&clean, m.termWidth)
			m.body = body
		}
		return m, nil
```
  - **View del reader:** cuando `m.showImages`, tras `m.body` concatenar `strings.Join(m.imageBlobs, "\n")`.

- [ ] **Step 4: Run → PASS.** `go build ./... && go test -count=1 ./cmd/mail-tui/`. Imports nuevos en model.go:
  `"context"`, `"strings"`, `"erickaldama-mail/internal/render"` (verificar que no estén ya).

- [ ] **Step 5: Commit** — `git -C "$WT" commit -am "feat(mail-tui): termWidth/rawMD/WindowSizeMsg + keys i (images) R (load-remote) — B-3/D2/D3"`

---

### Task 6: CLI — flag `--rich` en mail read

**Files:**
- Modify: `cmd/mail/main.go` (read command)
- Test: smoke manual (read-only)

**Interfaces:**
- Consumes: message.SanitizeHTML, message.RenderRich, golang.org/x/term

- [ ] **Step 1: Implementar** — en el `read` command de main.go:
  - Añade flags `--rich` (bool) y `--load-remote` (bool).
  - Si `--rich`: obtener ancho con `width, _, _ := term.GetSize(int(os.Stdout.Fd()))` (si falla/no-TTY → 80); sanitizar
    el HTML (`message.SanitizeHTML(parsed.TextHTML, loadRemote)`), pasar el HTML limpio a un Parsed y `RenderRich(p, width)`;
    imprimir. Los placeholders de remotas ya vienen en el HTML sanitizado.
  - Sin `--rich`: comportamiento actual (`RenderPlain`) — pipe-friendly.

- [ ] **Step 2: Build + smoke** — `go build ./...`; `go run ./cmd/mail read <key> --rich` (manual, read-only) muestra el body con ancho. `go run ./cmd/mail read <key>` sin --rich → texto plano como antes.

- [ ] **Step 3: Commit** — `git -C "$WT" commit -am "feat(mail): read --rich flag — sanitize+RenderRich with term width; --load-remote"`

---

### Task 7: Fixture `.eml` + tests de integración SAN-5 + docs + smoke

**Files:**
- Create: `internal/message/testdata/inline-image.eml`
- Test: `internal/message/render_integration_test.go`
- Modify: `README.md`, `docs/SP-4-DEPLOY.md`, `CHANGELOG.md`

- [ ] **Step 1: Fixture** — crear `testdata/inline-image.eml` (el MIME sintético del Task 1 con cid: + una img remota), y un test SAN-5 que verifica que `Parse(fixture).InlineImages` tiene los bytes y que el HTML sanitizado bloqueó la remota.

- [ ] **Step 2: go test ./... verde** — toda la suite, incluido el fixture.

- [ ] **Step 3 (GATE HUMANO): smoke con correo real** — el agente NO ejecuta el render de un correo real (NDA + interactivo).
  DETENERSE y entregar al humano: `mail read <key> --rich` y abrir el TUI + tecla 'i'. El humano confirma.

- [ ] **Step 4: Docs** — README: deps opcionales (chafa), política de privacidad (remotas bloqueadas por default, `--load-remote`/`R`),
  flujo de imágenes (tecla `i`). CHANGELOG `[mail v0.3]`. docs/SP-4-DEPLOY.md sección render rico. Gate NDA (0 hits).

- [ ] **Step 5: Commit** — `git -C "$WT" commit -m "test+docs(v0.3): synthetic .eml fixture, SAN-5 integration, README/CHANGELOG"`

---

## Self-Review del plan (post-escritura + post-audit)
- Cobertura spec: D2(T3,T5)·D3(T2,T5,T6)·**D4: T2 crea SanitizeHTML, pero T5 (body load) Y T6 (--rich) lo INVOCAN
  primero — P-13**·D5(T3)·D6(T4)·SAN-1..5(T2,T3,T7). ✓
- Firmas consistentes: RenderRich(p,width) en T4/T5/T6. SanitizeHTML(html,allowRemote) en T2/T5/T6. ✓
- Call-sites de RenderRich: model.go:275 (T4 step4 — termWidth añadido AHÍ, P-7), render_test.go:17 (T4 step1). ✓
- Orden: T1→T2→T3→T4(añade termWidth→build verde)→T5(wirea, sanitiza en load, imágenes async)→T6(CLI --rich)→T7(fixture+docs+gate).
- **Hallazgos de auditoría aplicados (findings 23):** P-1 (htmltomd conservado), P-2 (regexp `^cid:` en bluemonday),
  P-3 (test de bypasses), P-4 (Parse io.Reader + import strings), P-5 (sanitize en body load + rawHTML para toggle R),
  P-6 (imágenes async tea.Cmd), P-7 (termWidth en T4 + guard currentParsed), P-8 (AppendChild), P-9 (object/embed/data).
  Deuda menor consciente: P-10 (CLI Parsed parcial — ok), P-11 (doble parse — aceptable v0.3), P-12 (ctx Background — ok v0.3).
