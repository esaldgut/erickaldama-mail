# Cliente correo v0.2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan
> task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que el cliente de correo sea consumible de verdad — `mail ls` lista todos tus mailboxes (config.toml), y
`send`/`reply`/el composer del TUI soportan CC/BCC con privacidad correcta + reply-all.

**Architecture:** Un paquete `internal/config` nuevo (TOML) alimenta el CLI. El núcleo (`message.Build`) gana Cc/Bcc y
computa las `destinations` para el envelope SES (BCC fuera del header MIME). El composer del TUI pasa de un solo string
a campos `bubbles/textinput`. `message.Parse` gana Cc/To para reply-all.

**Tech Stack:** Go 1.26.4 · enmime/v2 v2.4.1 · aws-sdk-go-v2/ses · BurntSushi/toml (nueva) · bubbles/textinput (ya indirect) · cobra · bubbletea.

## Global Constraints

- **Go 1.26.4.** modern-go: `any` no `interface{}`, SALVO donde la firma del SDK lo exija.
- **Repo PÚBLICO Git Flow:** cierre = PR a develop, NO merge local. Gate NDA sobre todo el output (account 367707589526 publicable).
- **Invariante de privacidad (CRÍTICO):** el BCC NUNCA en el header MIME — solo en `SendRawEmailInput.Destinations`.
  NO llamar `.BCC()` en el builder. Un test asegura que el raw NO contiene `"Bcc:"`.
- **config path: XDG explícito, NO `os.UserConfigDir()`** (en macOS devuelve `~/Library/Application Support`).
- **ls multi-mailbox: ordenar por `SK` desc** (ISO8601), NUNCA por `Date` (RFC1123Z ordena mal). `limit` COMPLETO por
  mailbox, truncar el merge a `limit` (no repartir).
- **`--cc`/`--bcc` direcciones PELADAS** (`a@x,b@y`); `"Nombre <addr>"` no soportado en v0.2 (nota en --help).
- **subagent-driven en worktree:** commit con `git -C "$WT"`, rama = worktree-mail-v0.2 (≠develop/main), `go mod tidy`
  tras tocar deps. Validar cada reporte contra el código real (count=1, vet, build, go mod verify).

**Hechos verificados (no re-investigar):** `Header.SK` ya existe (reader.go:21, dynamodbav+json tag — el Query ya lo
trae). `enmime MailBuilder.CC(name,addr) MailBuilder` encadena. `ses.SendRawEmailInput.Destinations []string`. `bubbles
v1.0.0` ya en go.mod indirect. `Parsed` (parse.go) NO tiene Cc/To hoy.

---

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `internal/config/config.go` + `_test.go` (new) | Cargar config.toml (XDG path), struct Config |
| `internal/message/build.go` (mod) | BuildOpts +Cc/Bcc; Build retorna (raw, destinations, err) |
| `internal/message/build_test.go` (mod) | Cc en header, Bcc NO en header, destinations=To+Cc+Bcc |
| `internal/message/parse.go` + `parse_test.go` (mod) | Parsed +Cc/To capturados de los headers |
| `internal/mailbox/sender.go` (mod) | Send(ctx, raw, destinations) pasa Destinations al SES input |
| `cmd/mail/main.go` (mod) | ls multi-mailbox (sort SK) + fallback error; flags --cc/--bcc; 4 call-sites; reply-all CLI |
| `cmd/mail-tui/model.go` (mod) | composer multi-campo (textinput To/Cc/Bcc/Subject + body $EDITOR); reply-all |
| `cmd/mail-tui/model_test.go` (mod) | actualizar los 3 tests que el refactor rompe |
| `docs/SP-4-DEPLOY.md` (mod) | config.toml de ejemplo + CC/BCC en el runbook |

---

## Task 1: `internal/config` — config.toml (XDG path)

**Files:** Create `internal/config/config.go`, `internal/config/config_test.go`. Modify `go.mod` (add BurntSushi/toml).

**Interfaces:**
- Produces: `type Config struct { Mailboxes []string; DefaultFrom, ReadProfile, SendProfile string }`;
  `func LoadFrom(path string) (*Config, bool, error)`; `func Load() (*Config, bool, error)`; `func Path() string`.

- [ ] **Step 1: Añadir la dependencia BurntSushi/toml**

Run:
```bash
go get github.com/BurntSushi/toml@latest
go mod tidy
grep BurntSushi go.mod   # debe aparecer como require directo
```
Expected: `github.com/BurntSushi/toml vX.Y.Z` en go.mod (no `// indirect`).

- [ ] **Step 2: Escribir el test que falla**

`internal/config/config_test.go`:
```go
package config

import (
	"os"
	"path/filepath"
	"testing"
)

func TestLoadFromParses(t *testing.T) {
	dir := t.TempDir()
	p := filepath.Join(dir, "config.toml")
	os.WriteFile(p, []byte(`
mailboxes    = ["erick@erickaldama.com", "test@erickaldama.com"]
default_from = "erick@erickaldama.com"
read_profile = "mail-client-read"
`), 0o600)
	cfg, ok, err := LoadFrom(p)
	if err != nil || !ok {
		t.Fatalf("LoadFrom: ok=%v err=%v", ok, err)
	}
	if len(cfg.Mailboxes) != 2 || cfg.Mailboxes[0] != "erick@erickaldama.com" {
		t.Fatalf("mailboxes: %v", cfg.Mailboxes)
	}
	if cfg.DefaultFrom != "erick@erickaldama.com" || cfg.ReadProfile != "mail-client-read" {
		t.Fatalf("fields: %+v", cfg)
	}
}

func TestLoadFromMissingIsNotError(t *testing.T) {
	cfg, ok, err := LoadFrom(filepath.Join(t.TempDir(), "nope.toml"))
	if err != nil {
		t.Fatalf("missing file should not error: %v", err)
	}
	if ok {
		t.Fatal("ok should be false for missing file")
	}
	if cfg == nil {
		t.Fatal("cfg should be non-nil zero value")
	}
}

func TestPathRespectsXDG(t *testing.T) {
	t.Setenv("XDG_CONFIG_HOME", "/tmp/xdgtest")
	got := Path()
	want := "/tmp/xdgtest/erickaldama-mail/config.toml"
	if got != want {
		t.Fatalf("Path() = %q, want %q", got, want)
	}
}
```

- [ ] **Step 3: Run → FAIL** (paquete no existe)

Run: `go test ./internal/config/`
Expected: FAIL (no Go files / undefined).

- [ ] **Step 4: Implementar config.go**

```go
// Package config loads the optional ~/.config/erickaldama-mail/config.toml (XDG path).
package config

import (
	"errors"
	"io/fs"
	"os"
	"path/filepath"

	"github.com/BurntSushi/toml"
)

// Config mirrors config.toml. All fields optional; mailboxes drives the multi-mailbox `ls`.
type Config struct {
	Mailboxes   []string `toml:"mailboxes"`
	DefaultFrom string   `toml:"default_from"`
	ReadProfile string   `toml:"read_profile"`
	SendProfile string   `toml:"send_profile"`
}

// Path resolves the config path via XDG explicitly. NOT os.UserConfigDir() — on macOS that returns
// ~/Library/Application Support, not ~/.config (audit B-1b).
func Path() string {
	if xdg := os.Getenv("XDG_CONFIG_HOME"); xdg != "" {
		return filepath.Join(xdg, "erickaldama-mail", "config.toml")
	}
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".config", "erickaldama-mail", "config.toml")
}

// Load reads the config at the XDG path. Returns (cfg, ok=false, nil) if absent (not an error).
func Load() (*Config, bool, error) { return LoadFrom(Path()) }

// LoadFrom is the pure, testable form. Missing file → (zero cfg, false, nil). Malformed → error with position.
func LoadFrom(path string) (*Config, bool, error) {
	var cfg Config
	if _, err := toml.DecodeFile(path, &cfg); err != nil {
		if errors.Is(err, fs.ErrNotExist) {
			return &Config{}, false, nil
		}
		return nil, false, err
	}
	return &cfg, true, nil
}
```

- [ ] **Step 5: Run → PASS**

Run: `go test -count=1 ./internal/config/`
Expected: PASS (3 tests).

- [ ] **Step 6: Commit**

```bash
git add go.mod go.sum internal/config/
git commit -m "feat(config): config.toml loader with explicit XDG path"
```

---

## Task 2: `message.Build` — Cc/Bcc + destinations (envelope privacy)

**Files:** Modify `internal/message/build.go`, `internal/message/build_test.go`.

**Interfaces:**
- Consumes: enmime `MailBuilder.CC(name, addr string)`.
- Produces: `BuildOpts` += `Cc, Bcc string`; `func Build(opt BuildOpts) (raw []byte, destinations []string, err error)`;
  `func splitAddrs(s string) []string`.

- [ ] **Step 1: Escribir el test que falla** (incluye el invariante BCC)

Añadir a `internal/message/build_test.go`:
```go
func TestBuildCcInHeaderBccNot(t *testing.T) {
	raw, dests, err := Build(BuildOpts{
		From: "me@erickaldama.com", To: "to@x.com",
		Cc: "cc1@x.com, cc2@x.com", Bcc: "secret@x.com",
		Subject: "hi", Body: "body",
	})
	if err != nil {
		t.Fatal(err)
	}
	s := string(raw)
	if !strings.Contains(s, "cc1@x.com") || !strings.Contains(s, "Cc:") {
		t.Fatalf("Cc must be in header:\n%s", s)
	}
	// PRIVACY INVARIANT: Bcc must NOT appear in the raw MIME — only in the SES envelope.
	if strings.Contains(s, "Bcc:") || strings.Contains(s, "secret@x.com") {
		t.Fatalf("BCC leaked into the raw MIME:\n%s", s)
	}
	// destinations = To + Cc + Bcc (the envelope SES delivers to)
	want := []string{"to@x.com", "cc1@x.com", "cc2@x.com", "secret@x.com"}
	if !slices.Equal(dests, want) {
		t.Fatalf("destinations = %v, want %v", dests, want)
	}
}

func TestSplitAddrs(t *testing.T) {
	got := splitAddrs(" a@x.com ,, b@y.com ")
	want := []string{"a@x.com", "b@y.com"}
	if !slices.Equal(got, want) {
		t.Fatalf("splitAddrs = %v, want %v", got, want)
	}
	if len(splitAddrs("")) != 0 {
		t.Fatal("empty → empty slice")
	}
}
```
(añadir imports `slices`, `strings` si faltan)

- [ ] **Step 2: Run → FAIL** (Build retorna 2 valores, no 3; sin Cc/Bcc)

Run: `go test ./internal/message/ -run 'TestBuildCc|TestSplitAddrs'`
Expected: FAIL (assignment mismatch / undefined fields).

- [ ] **Step 3: Implementar en build.go**

Modificar `BuildOpts` y `Build`:
```go
type BuildOpts struct {
	From, To, Subject, Body string
	Cc, Bcc                 string // comma-separated, plain addresses (no "Name <addr>")
	InReplyTo, References    string
	MessageID               string
	Attachments             []FileAttach
}

// splitAddrs splits a comma-separated address list, trimming spaces and dropping empties.
func splitAddrs(s string) []string {
	var out []string
	for _, p := range strings.Split(s, ",") {
		if a := strings.TrimSpace(p); a != "" {
			out = append(out, a)
		}
	}
	return out
}

// Build assembles outbound MIME via enmime. Returns the raw bytes AND the envelope destinations
// (To+Cc+Bcc) for SES SendRawEmail. The BCC is deliberately NOT added to the builder (and enmime
// would ignore it in Build() anyway) so it never lands in the header — privacy invariant.
func Build(opt BuildOpts) (raw []byte, destinations []string, err error) {
	if opt.MessageID == "" {
		opt.MessageID = NewMessageID()
	}
	b := enmime.Builder().
		From("", opt.From).
		To("", opt.To).
		Subject(opt.Subject).
		Text([]byte(opt.Body)).
		Header("Message-ID", opt.MessageID)
	cc := splitAddrs(opt.Cc)
	for _, addr := range cc {
		b = b.CC("", addr)
	}
	if opt.InReplyTo != "" {
		b = b.Header("In-Reply-To", opt.InReplyTo)
	}
	if opt.References != "" {
		b = b.Header("References", opt.References)
	}
	for _, a := range opt.Attachments {
		b = b.AddFileAttachment(a.Path)
	}
	root, err := b.Build()
	if err != nil {
		return nil, nil, err
	}
	var buf bytes.Buffer
	if err := root.Encode(&buf); err != nil {
		return nil, nil, err
	}
	// Envelope destinations: To + Cc + Bcc. The Bcc is here ONLY (never in the header above).
	destinations = append([]string{opt.To}, cc...)
	destinations = append(destinations, splitAddrs(opt.Bcc)...)
	return buf.Bytes(), destinations, nil
}
```
NOTA: verificar el patrón real de serialización del Build actual (`b.Build()` → `*enmime.Part`, luego `.Encode`).
Si el Build actual ya devuelve `[]byte` directamente con otro método, adaptar manteniendo el retorno de 3 valores.
El `bytes` import puede ya estar; si no, añadirlo.

- [ ] **Step 4: Run → PASS**

Run: `go test -count=1 ./internal/message/ -run 'TestBuildCc|TestSplitAddrs'`
Expected: PASS. **El test del invariante BCC (no `Bcc:` en el raw) DEBE pasar.**

- [ ] **Step 5: Run la suite de message completa** (no romper los tests existentes de Build)

Run: `go test -count=1 ./internal/message/`
Expected: PASS (los tests viejos de Build que esperaban 2 retornos — si los hay — se actualizan aquí; buscar
`Build(` en build_test.go y ajustar a 3 retornos).

- [ ] **Step 6: Commit**

```bash
git add internal/message/build.go internal/message/build_test.go
git commit -m "feat(message): Cc/Bcc in Build + envelope destinations (Bcc never in header)"
```

---

## Task 3: `Sender.Send` — pasar Destinations al envelope SES

**Files:** Modify `internal/mailbox/sender.go`, `internal/mailbox/sender_test.go`.

**Interfaces:**
- Consumes: `ses.SendRawEmailInput.Destinations []string`.
- Produces: `func (s *Sender) Send(ctx context.Context, raw []byte, destinations []string) (string, error)`.

- [ ] **Step 1: Extender el mock + escribir el test que falla**

El mock real es `fakeRaw struct { id string; err error }` con `SendRawEmail(ctx, in, ...)`; el constructor es
`NewSender(fakeRaw{id:...}, fakeAcct{prod:...})`. Añadir un campo de captura al `fakeRaw` y el test:
```go
// en sender_test.go, extender el struct existente:
type fakeRaw struct {
	id        string
	err       error
	gotDests  []string // capture for the Destinations test
}

func (f *fakeRaw) SendRawEmail(ctx context.Context, in *ses.SendRawEmailInput, _ ...func(*ses.Options)) (*ses.SendRawEmailOutput, error) {
	f.gotDests = in.Destinations
	if f.err != nil {
		return nil, f.err
	}
	return &ses.SendRawEmailOutput{MessageId: aws.String(f.id)}, nil
}
// NOTA: cambiar el receiver de `f fakeRaw` a `f *fakeRaw` para que gotDests persista; actualizar los call-sites
// existentes (TestSendOK etc.) a `NewSender(&fakeRaw{id:"mid-1"}, ...)`.

func TestSendPassesDestinations(t *testing.T) {
	fr := &fakeRaw{id: "mid-1"}
	s := NewSender(fr, fakeAcct{prod: false})
	_, err := s.Send(context.Background(), []byte("raw"), []string{"to@x", "bcc@x"})
	if err != nil {
		t.Fatal(err)
	}
	if !slices.Equal(fr.gotDests, []string{"to@x", "bcc@x"}) {
		t.Fatalf("Destinations not passed: %v", fr.gotDests)
	}
}
```
(añadir import `slices`)

- [ ] **Step 2: Run → FAIL** (Send toma 2 args, no 3)

Run: `go test ./internal/mailbox/ -run TestSendPassesDestinations`
Expected: FAIL (too many arguments / Destinations not set).

- [ ] **Step 3: Implementar en sender.go**

Cambiar la firma y el input:
```go
func (s *Sender) Send(ctx context.Context, raw []byte, destinations []string) (string, error) {
	if len(raw) > MaxRawBytes {
		return "", fmt.Errorf("message is %d bytes; SES v1 SendRawEmail caps at %d (10MB, inadjustable)", len(raw), MaxRawBytes)
	}
	out, err := s.raw.SendRawEmail(ctx, &ses.SendRawEmailInput{
		RawMessage:   &sestypes.RawMessage{Data: raw},
		Destinations: destinations, // To+Cc+Bcc; SES delivers here. Source nil → uses From in the raw.
	})
	// ... resto igual (ErrSandboxRecipient via errors.As + DetectSandbox)
}
```
(mantener el manejo de error `ErrSandboxRecipient`/`DetectSandbox` tal cual — solo cambia la firma + Destinations)

- [ ] **Step 4: Run → PASS + suite mailbox**

Run: `go test -count=1 ./internal/mailbox/`
Expected: PASS (el test nuevo + los existentes; los tests viejos de Send se actualizan a 3 args aquí).

- [ ] **Step 5: Commit**

```bash
git add internal/mailbox/sender.go internal/mailbox/sender_test.go
git commit -m "feat(mailbox): Send passes explicit SES Destinations (BCC envelope)"
```

---

## Task 4: `message.Parse` — Cc/To para reply-all

**Files:** Modify `internal/message/parse.go`, `internal/message/parse_test.go`.

**Interfaces:**
- Produces: `Parsed` += `Cc, To string`.

- [ ] **Step 1: Escribir el test que falla**

Añadir a `internal/message/parse_test.go`:
```go
func TestParseCapturesCcAndTo(t *testing.T) {
	raw := "From: a@x.com\r\nTo: to@x.com\r\nCc: cc1@x.com, cc2@x.com\r\nSubject: hi\r\n\r\nbody\r\n"
	p, err := Parse(strings.NewReader(raw))
	if err != nil {
		t.Fatal(err)
	}
	if p.To != "to@x.com" {
		t.Fatalf("To = %q", p.To)
	}
	if !strings.Contains(p.Cc, "cc1@x.com") || !strings.Contains(p.Cc, "cc2@x.com") {
		t.Fatalf("Cc = %q", p.Cc)
	}
}
```

- [ ] **Step 2: Run → FAIL** (Parsed sin Cc/To)

Run: `go test ./internal/message/ -run TestParseCapturesCcAndTo`
Expected: FAIL (undefined field p.To / p.Cc).

- [ ] **Step 3: Implementar en parse.go**

Añadir a `Parsed` y capturar en `Parse` vía `env.GetHeader`:
```go
type Parsed struct {
	Subject     string
	From        string
	To          string // for reply-all
	Cc          string // for reply-all
	Date        string
	TextPlain   string
	TextHTML    string
	MessageID   string
	InReplyTo   string
	References  string
	Attachments []Attachment
}
```
Y en `Parse`, tras crear el envelope: `parsed.To = env.GetHeader("To")` y `parsed.Cc = env.GetHeader("Cc")`
(usar el mismo patrón con que ya se llena `From`/`Subject` — leer parse.go para el nombre exacto del envelope).

- [ ] **Step 4: Run → PASS + suite**

Run: `go test -count=1 ./internal/message/`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/message/parse.go internal/message/parse_test.go
git commit -m "feat(message): Parse captures To/Cc for reply-all"
```

---

## Task 5: CLI — ls multi-mailbox (sort SK) + flags cc/bcc + 4 call-sites + fallback error

**Files:** Modify `cmd/mail/main.go`.

**Interfaces:**
- Consumes: `config.Load()`, `message.Build (3 retornos)`, `Sender.Send(ctx, raw, dests)`, `Header.SK`.

- [ ] **Step 1: ls multi-mailbox + fallback error**

En el `RunE` de `ls`: cargar config (`config.Load()`). Lógica:
```go
cfg, hasCfg, _ := config.Load()
mailboxes := []string{}
if cmd.Flags().Changed("mailbox") {
	mailboxes = []string{mailboxName}      // override explícito
} else if hasCfg && len(cfg.Mailboxes) > 0 {
	mailboxes = cfg.Mailboxes              // todos los del config
} else {
	fmt.Fprintln(os.Stderr, "no hay config; crea ~/.config/erickaldama-mail/config.toml con tus mailboxes, o usa --mailbox <dirección>")
	return fmt.Errorf("no mailbox specified and no config")   // exit≠0 (B-5)
}
var all []mailbox.Header
for _, mb := range mailboxes {
	hs, _, err := r.List(ctx, mb, int32(count), nil)   // limit COMPLETO por mailbox (B-2b)
	if err != nil {
		fmt.Fprintf(os.Stderr, "error listing %s: %v\n", mb, err)
		continue                                        // no abortar todo
	}
	all = append(all, hs...)
}
// ordenar por SK descendente (ISO8601, B-2a — NO por Date)
slices.SortFunc(all, func(a, b mailbox.Header) int { return strings.Compare(b.SK, a.SK) })
if len(all) > count {
	all = all[:count]                                   // truncar el merge a limit
}
return renderList(os.Stdout, all, jsonFlag)
```

- [ ] **Step 2: flags --cc/--bcc en send + reply + actualizar los 4 call-sites**

Añadir flags `--cc`/`--bcc` a sendCmd y replyCmd (con nota en el Short/help: "comma-separated, plain addresses").
Actualizar los 4 call-sites:
- `send` Build (~159): `raw, dests, err := message.Build(message.BuildOpts{From, To, Cc: sendCc, Bcc: sendBcc, Subject, Body})`
- `send` Send (~174): `msgID, err := s.Send(ctx, raw, dests)`
- `reply` Build (~241): `raw, dests, err := message.Build(...)` (con Cc del reply-all, ver Step 3)
- `reply` Send (~258): `s.Send(ctx, raw, dests)`
`--from` cae a `cfg.DefaultFrom` si está y no se pasó.

- [ ] **Step 3: reply-all en el CLI**

En `reply`: tras parsear el mensaje original, si el usuario NO pasó `--cc`, pre-rellenar Cc con `To`+`Cc` del original
MENOS la propia identidad (`--from`/`cfg.DefaultFrom`). Helper:
```go
// replyAllCc returns the original To+Cc addresses minus self, comma-joined.
func replyAllCc(parsedTo, parsedCc, self string) string {
	seen := map[string]bool{strings.ToLower(self): true}
	var out []string
	for _, a := range append(splitAddrsExported(parsedTo), splitAddrsExported(parsedCc)...) {
		la := strings.ToLower(a)
		if !seen[la] {
			seen[la] = true
			out = append(out, a)
		}
	}
	return strings.Join(out, ",")
}
```
(exportar `splitAddrs` desde message como `message.SplitAddrs`, o duplicar el helper trivial en main — decisión:
exportar `message.SplitAddrs` para DRY, ya que el CLI lo necesita.)

- [ ] **Step 4: Build + vet + suite CLI**

Run:
```bash
go build ./... && go vet ./cmd/mail/ && gofmt -l cmd/mail/
go test -count=1 ./cmd/mail/
```
Expected: build OK, vet OK, gofmt vacío, tests PASS.

- [ ] **Step 5: Smoke local read-only (mailbox real)**

Run: `go run ./cmd/mail ls --mailbox erick@erickaldama.com`
Expected: lista el correo real (1 fila). `go run ./cmd/mail ls` sin config → el error claro de Step 1.

- [ ] **Step 6: Commit**

```bash
git add cmd/mail/main.go internal/message/build.go
git commit -m "feat(mail): ls multi-mailbox (sort by SK) + --cc/--bcc + reply-all CLI"
```

---

## Task 6: TUI composer multi-campo (textinput) + reply-all

**Files:** Modify `cmd/mail-tui/model.go`, `cmd/mail-tui/model_test.go`.

**Interfaces:**
- Consumes: `bubbles/textinput`, `message.Build (3 retornos)`, `Sender.Send(ctx, raw, dests)`.

- [ ] **Step 1: go mod tidy para promover bubbles a directo**

Run: (tras importar textinput en model.go en Step 2) `go mod tidy && grep "charmbracelet/bubbles" go.mod`
Expected: `bubbles` sin `// indirect`.

- [ ] **Step 2: Reescribir el state del composer a campos textinput**

En `model.go`, reemplazar `composeDraft string` por:
```go
import "github.com/charmbracelet/bubbles/textinput"

type composer struct {
	inputs  []textinput.Model // [To, Cc, Bcc, Subject]
	active  int               // índice del input enfocado
	body    string            // editado vía $EDITOR (flujo actual)
}
// índices
const (cTo = 0; cCc = 1; cBcc = 2; cSubject = 3)

func newComposer() composer {
	labels := []string{"To", "Cc", "Bcc", "Subject"}
	ins := make([]textinput.Model, len(labels))
	for i, l := range labels {
		ti := textinput.New()
		ti.Prompt = l + ": "
		ins[i] = ti
	}
	ins[0].Focus()
	return composer{inputs: ins, active: 0}
}
```
En `Init()` del modelo: incluir `textinput.Blink` como cmd. En `handleComposerKey`: Tab/Shift-Tab cambian `active`
(Blur el viejo, Focus el nuevo), Ctrl-E abre $EDITOR para `body` (flujo `editorCmd` actual), Ctrl-S → confirm `y/n`
(gate de envío preservado — H-2). Las demás teclas se enrutan a `inputs[active].Update(msg)`.

- [ ] **Step 3: Send desde el composer usa Build(3 retornos) + Send(dests)**

Donde el composer enviaba: `raw, dests, err := message.Build(message.BuildOpts{From, To: c.inputs[cTo].Value(),
Cc: c.inputs[cCc].Value(), Bcc: c.inputs[cBcc].Value(), Subject: c.inputs[cSubject].Value(), Body: c.body})`;
luego `s.Send(ctx, raw, dests)`.

- [ ] **Step 4: reply-all en el TUI**

Al pulsar 'r' en la lista: abrir+parsear el mensaje seleccionado (S3 → `message.Parse`), pre-rellenar
`inputs[cTo]=parsed.From`, `inputs[cCc]=replyAllCc(parsed.To, parsed.Cc, self)`, `inputs[cSubject]="Re: ..."`.
(El flujo actual solo usaba el Header; ahora necesita el mensaje parseado — añadir el Open+Parse al handler de 'r'.)

- [ ] **Step 5: Actualizar los 3 tests rotos + View**

`model_test.go`: `TestComposerSendRequiresConfirmation` (inicializar el composer con `newComposer()`),
`TestSentMsgClearsConfirmState` (cambiar el assert `composeDraft != ""` por el nuevo state — ej. `c.inputs[cTo].Value() == ""`
o un flag de reset), `TestReplyDraftPrePopulates` (adaptar al nuevo `replyDraft`/reply-all que llena los inputs).
Actualizar `View()` del composer para renderizar los 4 inputs + preview del body.

- [ ] **Step 6: Build + vet + suite TUI**

Run:
```bash
go build ./... && go vet ./cmd/mail-tui/ && gofmt -l cmd/mail-tui/
go test -count=1 ./cmd/mail-tui/
```
Expected: build OK, vet OK, gofmt vacío, tests PASS (los 3 actualizados + cualquier nuevo).

- [ ] **Step 7: Commit**

```bash
git add cmd/mail-tui/model.go cmd/mail-tui/model_test.go go.mod go.sum
git commit -m "feat(mail-tui): multi-field composer (To/Cc/Bcc/Subject) + reply-all"
```

---

## Task 7: Documentación + smoke real + cierre

**Files:** Modify `docs/SP-4-DEPLOY.md`, `CHANGELOG.md`.

- [ ] **Step 1: Suite completa + vet + gofmt**

Run: `go build ./... && go test -count=1 ./... && go vet ./... && gofmt -l internal/ cmd/`
Expected: todo verde, gofmt vacío.

- [ ] **Step 2: config.toml de ejemplo + CC/BCC en el runbook**

En `docs/SP-4-DEPLOY.md`: añadir una sección con el config.toml de ejemplo (mailboxes, default_from) + ejemplos de
`mail send --cc ... --bcc ...` + nota de privacidad del BCC (envelope, no header) + nota de direcciones peladas.

- [ ] **Step 3: GATE HUMANO — smoke real de CC/BCC**

El usuario (o el agente con perfil send, si está autorizado) envía un correo de prueba con --cc y --bcc a direcciones
suyas y verifica: el destinatario y el CC ven el header con To+Cc; el BCC recibe pero NO aparece en el header. Comando:
```bash
mail send --from erick@erickaldama.com --to erick@erickaldama.com --cc test@erickaldama.com --bcc <otra> --subject "v0.2 cc/bcc smoke" --body "test"
mail ls --mailbox erick@erickaldama.com   # ver que llegó; abrir y confirmar el header
```
(SES sigue en sandbox — los destinatarios deben estar verificados o usar el simulador.)

- [ ] **Step 4: CHANGELOG + gate NDA**

CHANGELOG: entrada `[mail v0.2]` (config.toml, ls multi-mailbox sort-by-SK, CC/BCC con envelope privacy, reply-all,
composer multi-campo). Gate NDA: `grep -rIE "esagiosapp|yaan|roatech|MercadoPago|476114125529|288761749126|468227865963"`
sobre los archivos tocados → 0 hits.

- [ ] **Step 5: Commit**

```bash
git add docs/SP-4-DEPLOY.md CHANGELOG.md
git commit -m "docs(v0.2): config.toml + CC/BCC + reply-all in runbook & CHANGELOG"
```

---

## Cierre

Tras Task 7: review final whole-branch → PR a develop con CI verde (Git Flow). El CD correrá el job `diff` en el PR.
Recordar el quirk de `gh pr merge` (verificar state=MERGED). Reinstalar binarios (`go install ./cmd/mail ./cmd/mail-tui`)
tras el merge. Persistencia: checkboxes + ledger + task #29. RETOMAR-AQUI al día. 2º fogueo de subproject-delivery-canonical.
